---
phase: 12-advanced-seo
plan: 04
name: Performance SEO
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/components/admin/seo/PerformanceSeoForm.tsx
  - src/components/admin/seo/CoreWebVitalsCards.tsx
  - src/app/(admin)/admin/settings/seo/performance/page.tsx
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05
  - PERF-06

must_haves:
  truths:
    - "Admin can toggle Critical CSS extraction"
    - "Admin can toggle JS defer loading strategy"
    - "Admin can toggle HTML and CSS minification"
    - "Admin can configure CDN integration URL"
    - "Admin can configure cache control settings (max-age, stale-while-revalidate)"
    - "Admin sees Core Web Vitals monitor cards with placeholder values and API note"
  artifacts:
    - path: "src/components/admin/seo/PerformanceSeoForm.tsx"
      provides: "3 toggle switches, CDN URL input, cache settings inputs, save button"
      min_lines: 60
    - path: "src/components/admin/seo/CoreWebVitalsCards.tsx"
      provides: "5 CWV metric cards with placeholder values and descriptions"
      min_lines: 50
    - path: "src/app/(admin)/admin/settings/seo/performance/page.tsx"
      provides: "Performance SEO settings page replacing placeholder"
  key_links:
    - from: "src/components/admin/seo/PerformanceSeoForm.tsx"
      to: "src/app/(admin)/actions/admin-seo.ts"
      via: "getSeoSettings and saveSeoSettings for performance SEO keys"
      pattern: "getSeoSettings|saveSeoSettings"
    - from: "src/app/(admin)/admin/settings/seo/performance/page.tsx"
      to: "src/components/admin/seo/CoreWebVitalsCards.tsx"
      via: "Renders CWV cards and performance form"
      pattern: "CoreWebVitalsCards|PerformanceSeoForm"
---

<objective>
Build Performance SEO settings page with config-only toggles, CDN/cache configuration, and placeholder Core Web Vitals monitor cards.

Purpose: Per D-06, performance toggles save config only. Per D-07, CWV cards show placeholder "--" values with a note about connecting the PageSpeed Insights API.

Output: Performance SEO settings page at /admin/settings/seo/performance with toggles, CDN/cache inputs, and CWV placeholder cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-advanced-seo/12-CONTEXT.md
@.planning/phases/12-advanced-seo/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/seo-keys.ts:
```typescript
export const SEO_KEYS = [ /* ... existing keys ... */ ] as const;
// New keys: seo_perf_critical_css, seo_perf_js_defer, seo_perf_minification, seo_perf_cdn_url, seo_perf_cache_settings
```

From src/app/(admin)/actions/admin-seo.ts:
```typescript
export async function getSeoSettings(keys?: string[]): Promise<SeoSettingsData>
export async function saveSeoSettings(data: SeoSettingsData): Promise<{ success: boolean }>
```

From src/components/form/switch/Switch.tsx:
```typescript
// Used throughout: <Switch label="..." defaultChecked={bool} onChange={(checked) => fn} />
```

From src/components/form/input/InputField.tsx:
```typescript
// Used in BlogPostForm: <InputField placeholder="..." defaultValue="..." onChange={(e) => fn} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Performance SEO keys and build PerformanceSeoForm component</name>
  <files>
    src/components/admin/seo/PerformanceSeoForm.tsx
  </files>
  <action>
Per D-06: Config-only toggles for performance SEO. CDN URL and cache settings are text inputs.

**seo-keys.ts** -- Keys already added by Plan 12-01 Task 1 (seo_perf_critical_css, seo_perf_js_defer, seo_perf_minification, seo_perf_cdn_url, seo_perf_cache_settings in PERFORMANCE_SEO_KEYS slice). No modifications needed in this plan.

**PerformanceSeoForm.tsx** (`src/components/admin/seo/PerformanceSeoForm.tsx`):
1. `"use client"` component. No props -- self-contained.
2. State: criticalCss (boolean), jsDefer (boolean), minification (boolean), cdnUrl (string), cacheMaxAge (string), cacheStaleWhileRevalidate (string), isPending, message, loading.
3. On mount: call `getSeoSettings(["seo_perf_critical_css", "seo_perf_js_defer", "seo_perf_minification", "seo_perf_cdn_url", "seo_perf_cache_settings"])`. Parse booleans. Parse cache settings from JSON string `{ maxAge: "3600", staleWhileRevalidate: "86400" }` or default values.
4. Render inside ComponentCard titled "Performance Optimization", desc="Configure performance settings. These toggles save configuration preferences for build-time optimization.":
   - **Toggle section** (3 Switch components):
     - Critical CSS Extraction -- "Extract and inline critical CSS for above-the-fold content"
     - JS Defer Loading -- "Defer non-critical JavaScript execution"
     - HTML/CSS Minification -- "Minify HTML and CSS output for smaller page sizes"
   - **CDN Configuration** section:
     - CDN URL input (InputField) -- "https://cdn.example.com" -- "Set your CDN URL for static asset delivery"
   - **Cache Settings** section:
     - Max-Age input (InputField, type number) -- "Cache-Control max-age in seconds (default: 3600)"
     - Stale-While-Revalidate input (InputField, type number) -- "stale-while-revalidate in seconds (default: 86400)"
   - Note: "These settings are configuration flags. Actual optimization requires build pipeline integration."
5. Save button: serialize and call `saveSeoSettings({ seo_perf_critical_css: String(criticalCss), ..., seo_perf_cache_settings: JSON.stringify({ maxAge: cacheMaxAge, staleWhileRevalidate: cacheStaleWhileRevalidate }) })`.
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Performance SEO keys available via PERFORMANCE_SEO_KEYS slice (added by Plan 12-01)
    - PerformanceSeoForm renders 3 toggle switches, CDN URL input, 2 cache inputs
    - Settings persist across page reloads
    - `pnpm build` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Build CoreWebVitalsCards and wire the Performance SEO page</name>
  <files>
    src/components/admin/seo/CoreWebVitalsCards.tsx,
    src/app/(admin)/admin/settings/seo/performance/page.tsx
  </files>
  <action>
Per D-07: 5 CWV cards with placeholder "--" values and note about connecting PageSpeed Insights API.

**CoreWebVitalsCards.tsx** (`src/components/admin/seo/CoreWebVitalsCards.tsx`):
1. Regular component (no state needed). Render 5 metric cards in a grid (3 cols on lg, 2 on sm, 1 on mobile) plus an overall score card spanning full width below.
2. The 5 metrics (per PERF-06):
   - LCP (Largest Contentful Paint) -- icon: Clock, desc "Measures loading performance. Target: under 2.5s", value "--"
   - CLS (Cumulative Layout Shift) -- icon: Move, desc "Measures visual stability. Target: under 0.1", value "--"
   - INP (Interaction to Next Paint) -- icon: MousePointerClick, desc "Measures interactivity. Target: under 200ms", value "--"
   - TTFB (Time to First Byte) -- icon: Server, desc "Measures server response time. Target: under 800ms", value "--"
   - Overall Score -- icon: Gauge, desc "Combined performance score. Target: 90+", value "--"
3. Each card: rounded border, icon, metric name (bold), value in large text ("--" per D-07), description in small text, colored border-top (gray for placeholder state).
4. Below the grid: info banner with blue/gray background: "Connect Google PageSpeed Insights API for real monitoring data. Current values are placeholders."
5. Wrap all in ComponentCard titled "Core Web Vitals", desc="Monitor your site's performance metrics."

**Performance SEO page** (`src/app/(admin)/admin/settings/seo/performance/page.tsx`):
1. Replace placeholder with a page rendering two ComponentCards:
   - First: CoreWebVitalsCards (static, no data loading)
   - Second: PerformanceSeoForm (self-contained)
2. No wrapper state needed.
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - /admin/settings/seo/performance shows CWV cards with "--" placeholder values
    - Info banner explains need for PageSpeed Insights API connection
    - Performance Optimization form has 3 toggles, CDN URL, and cache settings
    - All settings persist after save
    - `pnpm build` passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> admin-seo.ts | Toggle values and CDN URL cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Tampering | admin-seo.ts perf toggles | mitigate | requireAdmin() guard; audit logging |
| T-12-11 | Spoofing | seo_perf_cdn_url field | mitigate | Validate URL format on save; reject non-URL values. CDN URL affects asset loading -- malicious URL could redirect resources |
</threat_model>

<verification>
1. `pnpm build` passes
2. Navigate to /admin/settings/seo/performance -- see CWV cards with "--" and API note
3. Toggle performance settings, enter CDN URL, set cache values, save
4. Verify all settings persist on page reload
5. Verify CDN URL validation rejects invalid inputs
</verification>

<success_criteria>
- Performance SEO page fully functional with 3 config-only toggle switches
- CDN URL input and cache settings (max-age, stale-while-revalidate) save correctly
- Core Web Vitals cards show 5 metrics with placeholder "--" values (per D-07)
- Info banner explains PageSpeed Insights API connection needed for real data
- All settings persist to settings table
</success_criteria>

<output>
After completion, create `.planning/phases/12-advanced-seo/12-04-SUMMARY.md`
</output>
