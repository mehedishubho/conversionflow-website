---
phase: 12-advanced-seo
plan: 03
name: Image SEO
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/lib/seo-keys.ts
  - src/app/(admin)/actions/admin-seo.ts
  - src/components/admin/seo/ImageSeoForm.tsx
  - src/components/admin/seo/ImageStatsCards.tsx
  - src/app/(admin)/admin/settings/seo/image-seo/page.tsx
autonomous: true
requirements:
  - IMGS-01
  - IMGS-02
  - IMGS-03
  - IMGS-04
  - IMGS-05

must_haves:
  truths:
    - "Admin can toggle auto ALT text generation for images"
    - "Admin can toggle WebP conversion for uploaded images"
    - "Admin can toggle lazy loading for images across the site"
    - "Admin can toggle image compression"
    - "Admin sees image performance statistics cards with placeholder values"
  artifacts:
    - path: "src/components/admin/seo/ImageSeoForm.tsx"
      provides: "4 toggle switches for image SEO settings with save button"
      min_lines: 50
    - path: "src/components/admin/seo/ImageStatsCards.tsx"
      provides: "3 stat cards showing total images, optimized count, savings with placeholder values"
      min_lines: 30
    - path: "src/app/(admin)/admin/settings/seo/image-seo/page.tsx"
      provides: "Image SEO settings page replacing placeholder"
  key_links:
    - from: "src/components/admin/seo/ImageSeoForm.tsx"
      to: "src/app/(admin)/actions/admin-seo.ts"
      via: "getSeoSettings and saveSeoSettings for image SEO keys"
      pattern: "getSeoSettings|saveSeoSettings"
    - from: "src/app/(admin)/admin/settings/seo/image-seo/page.tsx"
      to: "src/components/admin/seo/ImageSeoForm.tsx"
      via: "Renders form and stats cards"
      pattern: "ImageSeoForm|ImageStatsCards"
---

<objective>
Build Image SEO settings page with config-only toggle switches and placeholder statistics cards.

Purpose: Per D-06, Image SEO toggles save to DB settings only -- no real server-side processing. Per IMGS-05, image statistics show placeholder values with a note that stats require server-side integration.

Output: Image SEO settings page at /admin/settings/seo/image-seo with 4 toggle switches and 3 placeholder stat cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-advanced-seo/12-CONTEXT.md
@.planning/phases/12-advanced-seo/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/seo-keys.ts:
```typescript
export const SEO_KEYS = [ /* ... 26 existing keys ... */ ] as const;
export type SeoKey = (typeof SEO_KEYS)[number];
// New keys will be appended: seo_image_auto_alt, seo_image_webp, seo_image_lazy_loading, seo_image_compression
```

From src/app/(admin)/actions/admin-seo.ts:
```typescript
export async function getSeoSettings(keys?: string[]): Promise<SeoSettingsData>
export async function saveSeoSettings(data: SeoSettingsData): Promise<{ success: boolean }>
// Pattern: requireAdmin() -> db read/write -> createAuditLog()
```

From src/components/common/ComponentCard.tsx:
```typescript
interface ComponentCardProps { title: string; children: React.ReactNode; className?: string; desc?: string; }
```

From src/components/form/switch/Switch.tsx:
```typescript
// Used by AiBotCards.tsx: <Switch label="Allowed/Blocked" defaultChecked={bool} onChange={(checked) => fn} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Image SEO keys and build ImageSeoForm component</name>
  <files>
    src/lib/seo-keys.ts,
    src/components/admin/seo/ImageSeoForm.tsx
  </files>
  <action>
Per D-06: Config-only toggles for image SEO -- save to DB settings, no real processing.

**seo-keys.ts additions** (append to SEO_KEYS array after the AI SEO keys added in 12-02):
1. Add 4 new keys:
   - `"seo_image_auto_alt"` -- toggle for auto ALT text generation
   - `"seo_image_webp"` -- toggle for WebP conversion
   - `"seo_image_lazy_loading"` -- toggle for lazy loading
   - `"seo_image_compression"` -- toggle for image compression
2. Add slice export: `export const IMAGE_SEO_KEYS: readonly SeoKey[] = SEO_KEYS.slice(N, N+4)` where N is the correct index.

**ImageSeoForm.tsx** (`src/components/admin/seo/ImageSeoForm.tsx`):
1. `"use client"` component. No props -- loads data internally.
2. State: autoAlt (boolean), webp (boolean), lazyLoading (boolean), compression (boolean), isPending, message, loading.
3. On mount: call `getSeoSettings(["seo_image_auto_alt", "seo_image_webp", "seo_image_lazy_loading", "seo_image_compression"])`. Parse each as boolean (value === "true"). Default all to false.
4. Render inside ComponentCard titled "Image Optimization", desc="Configure image SEO settings. These toggles save configuration preferences for future infrastructure integration.":
   - 4 toggle rows using Switch component (same as AiBotCards pattern):
     - Auto ALT Text (Switch) -- "Automatically generate alt text for images without alt attributes"
     - WebP Conversion (Switch) -- "Convert uploaded images to WebP format for smaller file sizes"
     - Lazy Loading (Switch) -- "Enable native lazy loading for images across the site"
     - Image Compression (Switch) -- "Apply lossless compression to reduce image file sizes"
   - Each row: label text (left), switch (right), with description below.
   - Note at bottom: "These settings are configuration flags. Actual image optimization requires server-side integration."
5. Save button: call `saveSeoSettings({ seo_image_auto_alt: String(autoAlt), ... })`. Show success/error message.
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 4 image SEO keys added to seo-keys.ts
    - ImageSeoForm renders 4 toggle switches with save functionality
    - Settings persist across page reloads
    - `pnpm build` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Build ImageStatsCards and wire the Image SEO page</name>
  <files>
    src/components/admin/seo/ImageStatsCards.tsx,
    src/app/(admin)/admin/settings/seo/image-seo/page.tsx
  </files>
  <action>
Per D-06: Image statistics show placeholder values. Per IMGS-05: total images, optimized count, savings.

**ImageStatsCards.tsx** (`src/components/admin/seo/ImageStatsCards.tsx`):
1. Regular React component (no "use client" needed -- no interactivity). Or use "use client" if it needs state.
2. Render 3 stat cards in a 3-column grid (1 col on mobile):
   - Card 1: "Total Images" -- value "--", icon: Image icon from lucide-react
   - Card 2: "Optimized" -- value "--", icon: CheckCircle icon
   - Card 3: "Savings" -- value "--", icon: TrendingDown icon
3. Each card: rounded border, centered content, large value text, small label below. Placeholder values shown as "--" (per D-06).
4. Below the grid: info banner with blue/gray background: "Image statistics require server-side integration. Connect your image processing pipeline to see real data."
5. Wrap all in ComponentCard titled "Image Statistics", desc="Overview of image optimization performance."

**Image SEO page** (`src/app/(admin)/admin/settings/seo/image-seo/page.tsx`):
1. Replace placeholder with a simple page rendering two ComponentCards:
   - First: ImageStatsCards (static, no data loading needed)
   - Second: ImageSeoForm (self-contained, loads and saves its own data)
2. No wrapper state needed -- each component manages itself.
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - /admin/settings/seo/image-seo shows Image Statistics cards with placeholder "--" values
    - Info banner explains stats require server-side integration
    - Image Optimization form has 4 working toggle switches
    - Toggle changes persist after save and page reload
    - `pnpm build` passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> admin-seo.ts | Toggle values cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Tampering | admin-seo.ts image toggles | mitigate | requireAdmin() guard; audit logging; values validated as "true"/"false" strings |
</threat_model>

<verification>
1. `pnpm build` passes
2. Navigate to /admin/settings/seo/image-seo -- see stat cards with "--" values and info banner
3. Toggle each image SEO switch and save -- verify persisted on reload
4. Verify placeholder note is visible explaining stats need server integration
</verification>

<success_criteria>
- Image SEO page fully functional with 4 config-only toggle switches
- Image statistics show placeholder "--" values per D-06
- Info banner explains that real data requires server-side integration
- All toggles persist to settings table via existing getSeoSettings/saveSeoSettings pattern
</success_criteria>

<output>
After completion, create `.planning/phases/12-advanced-seo/12-03-SUMMARY.md`
</output>
