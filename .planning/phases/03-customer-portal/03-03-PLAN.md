---
phase: 03-customer-portal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/portal/InvoiceTable.tsx
  - src/components/portal/DownloadsList.tsx
  - src/components/portal/ChangelogExpandable.tsx
  - src/app/(portal)/billing/page.tsx
  - src/app/(portal)/downloads/page.tsx
autonomous: true
requirements:
  - PORT-03
  - PORT-04

must_haves:
  truths:
    - "Customer sees their invoices in a table with status badges and payment method labels"
    - "Customer sees the latest plugin version in a featured card with download button"
    - "Customer sees version history with expandable changelogs"
  artifacts:
    - path: "src/components/portal/InvoiceTable.tsx"
      provides: "Invoice table with status badges and payment method display"
      min_lines: 30
    - path: "src/components/portal/DownloadsList.tsx"
      provides: "Featured latest version card + version history table"
      min_lines: 30
    - path: "src/components/portal/ChangelogExpandable.tsx"
      provides: "Expand/collapse changelog per version"
      contains: "isExpanded"
    - path: "src/app/(portal)/billing/page.tsx"
      provides: "Billing page with order query"
      contains: "orders"
    - path: "src/app/(portal)/downloads/page.tsx"
      provides: "Downloads page with downloads query"
      contains: "downloads"
  key_links:
    - from: "src/app/(portal)/billing/page.tsx"
      to: "src/lib/db/schema.ts"
      via: "orders table query"
      pattern: "eq\(orders\.userId"
    - from: "src/app/(portal)/downloads/page.tsx"
      to: "src/lib/db/schema.ts"
      via: "downloads table query"
      pattern: "eq\(downloads\.userId"
---

<objective>
Build the Billing page (invoice table with status badges per D-11) and the Downloads page (featured latest version card + version history with expandable changelogs per D-16, D-17). Both pages follow the established server-component-with-DB-query pattern.

Purpose: Customers view their payment history and download plugin files. These are simpler data-display pages compared to licenses and support tickets.
Output: Billing page, Downloads page, InvoiceTable, DownloadsList, ChangelogExpandable components
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-customer-portal/03-CONTEXT.md
@.planning/phases/03-customer-portal/03-RESEARCH.md
@.planning/phases/03-customer-portal/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/db/schema.ts:
```typescript
export const orders = pgTable("orders", {
  id: uuid("id").defaultRandom().primaryKey(),
  userId: text("user_id").notNull(),
  productId: text("product_id").notNull(),
  plan: text("plan").notNull(),
  amount: integer("amount").notNull(),
  currency: text("currency").notNull().default("BDT"),
  paymentMethod: paymentMethodEnum("payment_method"),
  status: orderStatusEnum("status").notNull().default("pending"),
  createdAt: timestamp("created_at").notNull().defaultNow(),
});

export const downloads = pgTable("downloads", {
  id: uuid("id").defaultRandom().primaryKey(),
  userId: text("user_id").notNull(),
  productId: text("product_id").notNull(),
  version: text("version").notNull(),
  fileName: text("file_name").notNull(),
  downloadToken: text("download_token").notNull().unique(),
  expiresAt: timestamp("expires_at").notNull(),
  downloadedAt: timestamp("downloaded_at"),
  createdAt: timestamp("created_at").notNull().defaultNow(),
});

export const orderStatusEnum = pgEnum("order_status", ["pending", "completed", "failed", "refunded"]);
export const paymentMethodEnum = pgEnum("payment_method", ["bkash", "nagad", "rocket", "bank_transfer", "ssl_commerz"]);
```

From UI-SPEC -- Order status badge colors:
```
completed -> success
pending -> warning
failed -> error
refunded -> light
```

Payment method display map:
```
bkash -> "bKash"
nagad -> "Nagad"
rocket -> "Rocket"
bank_transfer -> "Bank Transfer"
ssl_commerz -> "SSL Commerce"
```

From src/components/common/PageBreadCrumb.tsx (updated in Plan 01):
```typescript
// Props: pageTitle: string; basePath?: string (defaults to "/")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InvoiceTable component and Billing page</name>
  <files>src/components/portal/InvoiceTable.tsx, src/app/(portal)/billing/page.tsx</files>
  <read_first>
    - src/components/ui/badge/Badge.tsx (Badge component)
    - src/components/ui/table/index.tsx (Table primitives)
    - src/components/common/ComponentCard.tsx (card wrapper)
    - src/components/common/PageBreadCrumb.tsx (breadcrumb)
    - src/lib/db/schema.ts (orders table)
    - src/app/(portal)/dashboard/page.tsx (auth guard pattern)
  </read_first>
  <action>
1. **Create `src/components/portal/InvoiceTable.tsx`** (server component, NO "use client"):
   - Import `Badge` from `@/components/ui/badge/Badge`
   - Import `{ Table, TableHeader, TableBody, TableRow, TableCell }` from `@/components/ui/table`
   - Import `format` from `date-fns`
   - Define local type:
     ```typescript
     type OrderRow = {
       id: string;
       plan: string;
       amount: number;
       currency: string;
       paymentMethod: "bkash" | "nagad" | "rocket" | "bank_transfer" | "ssl_commerz" | null;
       status: "pending" | "completed" | "failed" | "refunded";
       createdAt: Date;
     };
     ```
   - Props: `{ orders: OrderRow[]; emptyMessage?: string }`
   - Define maps:
     - `statusBadgeMap: Record<string, "success" | "warning" | "error" | "light">` = `{ completed: "success", pending: "warning", failed: "error", refunded: "light" }`
     - `paymentMethodMap: Record<string, string>` = `{ bkash: "bKash", nagad: "Nagad", rocket: "Rocket", bank_transfer: "Bank Transfer", ssl_commerz: "SSL Commerce" }`
   - Render:
     - `<div className="overflow-x-auto">`
     - `<Table className="min-w-[600px]">`
     - TableHeader with 6 columns: "Order ID", "Plan", "Amount", "Payment Method", "Date", "Status"
       - Header cells: `px-5 py-3 text-left text-sm font-medium text-gray-500 dark:text-gray-400 border-b border-gray-100 dark:border-gray-800`
     - TableBody:
       - Empty state: single row with `<TableCell colSpan={6}>` showing `{emptyMessage}` centered with `px-5 py-8 text-center text-sm text-gray-500`
       - For each order: `<TableRow className="border-b border-gray-100 dark:border-gray-800 last:border-0">`
         - Order ID: `{order.id.slice(0, 8)}...` with `font-mono text-sm`
         - Plan: `{order.plan}` plain text
         - Amount: formatted with `new Intl.NumberFormat("en-BD", { style: "currency", currency: order.currency }).format(order.amount)`
         - Payment Method: `{paymentMethodMap[order.paymentMethod ?? ""] ?? order.paymentMethod ?? "N/A"}`
         - Date: `{format(new Date(order.createdAt), "MMM d, yyyy")}`
         - Status: `<Badge variant="light" color={statusBadgeMap[order.status]}>{order.status}</Badge>`
   - Export: `export function InvoiceTable`

2. **Create `src/app/(portal)/billing/page.tsx`** (server component):
   - Same auth guard pattern (getSession, redirect, admin role check)
   - Import `db` from `@/lib/db`
   - Import `{ orders }` from `@/lib/db/schema`
   - Import `{ eq, desc }` from `drizzle-orm`
   - Import `PageBreadcrumb` from `@/components/common/PageBreadCrumb`
   - Import `ComponentCard` from `@/components/common/ComponentCard`
   - Import `InvoiceTable` from `@/components/portal/InvoiceTable`
   - Query: `const userOrders = await db.select().from(orders).where(eq(orders.userId, userId)).orderBy(desc(orders.createdAt))`
   - Render:
     - `<PageBreadcrumb pageTitle="Billing" basePath="/dashboard" />`
     - `<ComponentCard title="Payment History" desc="View your invoices, payment history, and refund status.">`
     - `<InvoiceTable orders={userOrders} emptyMessage="No invoices yet. Your payment history will appear here after your first purchase." />`
     - `</ComponentCard>`
  </action>
  <acceptance_criteria>
    - `src/components/portal/InvoiceTable.tsx` does NOT have `"use client"`
    - InvoiceTable uses Badge with status color map: completed=success, pending=warning, failed=error, refunded=light
    - InvoiceTable has paymentMethodMap converting enum values to display text (bKash, Nagad, etc.)
    - `src/app/(portal)/billing/page.tsx` queries orders table filtered by userId
    - Billing page renders PageBreadcrumb with basePath="/dashboard"
    - Empty state message shows when no orders
  </acceptance_criteria>
  <verify>
    <automated>cd "D:\Devsroom-Work\conversionflow-website" && grep -c 'orders' src/app/\(portal\)/billing/page.tsx && grep -c 'Badge' src/components/portal/InvoiceTable.tsx</automated>
  </verify>
  <done>Billing page shows invoice table with order ID, plan, amount (BDT formatted), payment method, date, and status badges. Empty state shown when no orders.</done>
</task>

<task type="auto">
  <name>Task 2: Create ChangelogExpandable client component, DownloadsList, and Downloads page</name>
  <files>src/components/portal/ChangelogExpandable.tsx, src/components/portal/DownloadsList.tsx, src/app/(portal)/downloads/page.tsx</files>
  <read_first>
    - src/components/ui/badge/Badge.tsx (Badge for "Latest" badge)
    - src/components/common/PageBreadCrumb.tsx (breadcrumb)
    - src/components/common/ComponentCard.tsx (card wrapper)
    - src/lib/db/schema.ts (downloads table)
    - src/app/(portal)/dashboard/page.tsx (auth guard pattern)
    - src/styles/dashboard.css (brand tokens for featured card)
  </read_first>
  <action>
1. **Create `src/components/portal/ChangelogExpandable.tsx`** (client component, `"use client"`):
   - Import `useState` from `react`
   - Import `ChevronDown` from `lucide-react`
   - Props: `{ changelog: string | null }`
   - State: `const [isExpanded, setIsExpanded] = useState(false)`
   - If `!changelog`, return a `<span className="text-sm text-gray-400">No changelog</span>`
   - Render:
     - Container `div`
     - Toggle button: `<button onClick={() => setIsExpanded(!isExpanded)} className="flex items-center gap-1 text-sm text-brand-500 hover:text-brand-600 dark:text-brand-400">`
       - Text: `{isExpanded ? "Hide changelog" : "View changelog"}`
       - Icon: `<ChevronDown className="w-4 h-4 transition-transform duration-200" style={{ transform: isExpanded ? "rotate(180deg)" : "rotate(0deg)" }} />`
     - Expanded content (conditionally rendered): `{isExpanded && (<div className="mt-2 rounded-lg bg-gray-50 dark:bg-white/5 p-4 text-sm text-gray-600 dark:text-gray-400 whitespace-pre-wrap">{changelog}</div>)}`
   - Export: `export function ChangelogExpandable`

2. **Create `src/components/portal/DownloadsList.tsx`** (server component, NO "use client"):
   - Import `Download` from `lucide-react`
   - Import `Badge` from `@/components/ui/badge/Badge`
   - Import `{ ChangelogExpandable }` from `@/components/portal/ChangelogExpandable` -- NOTE: since this is a server component importing a client component, this works fine in Next.js (client components can be rendered inside server components)
   - Import `format` from `date-fns`
   - Define local type:
     ```typescript
     type DownloadRow = {
       id: string;
       version: string;
       fileName: string;
       downloadToken: string;
       createdAt: Date;
       changelog?: string | null;
     };
     ```
   - Props: `{ downloads: DownloadRow[]; emptyMessage?: string }`
   - Logic: `const latest = downloads.length > 0 ? downloads[0] : null; const older = downloads.slice(1)`
   - Render:
     - If `!latest && downloads.length === 0`: render empty state paragraph with `{emptyMessage}` styled `text-center py-8 text-sm text-gray-500`
     - Otherwise:
       - **Featured latest version card** (if `latest` exists):
         - Container: `div` with `rounded-2xl border-2 border-brand-200 bg-white p-6 dark:border-brand-500/30 dark:bg-white/[0.03] mb-6`
         - Flex row: `flex items-center justify-between`
         - Left side: `div`
           - Version row: `<span className="text-2xl font-bold text-gray-800 dark:text-white/90">{latest.version}</span>` + `<Badge variant="solid" color="primary" size="sm" className="ml-2">Latest</Badge>`
           - Release date: `<p className="text-sm text-gray-500 dark:text-gray-400 mt-1">{format(new Date(latest.createdAt), "MMM d, yyyy")}</p>`
           - File name: `<p className="text-sm font-mono text-gray-400 dark:text-gray-500 mt-1">{latest.fileName}</p>`
         - Right side: download button (a plain `<a>` link or styled button that links to the download -- since we have no download API route yet, render as `<button disabled>` for now with class `btn btn-primary` and "Download Latest" text)
       - **Version history** (if `older.length > 0`):
         - Heading: `<h3 className="text-lg font-medium text-gray-800 dark:text-white/90 mb-4">Version History</h3>`
         - `<div className="space-y-3">` with each older version as a card:
           - Container: `div` with `rounded-xl border border-gray-200 bg-white p-4 dark:border-gray-800 dark:bg-white/[0.03]`
           - Flex row: `flex items-center justify-between`
           - Left: version text `font-medium text-gray-800 dark:text-white/90` + release date `text-sm text-gray-500` + filename `text-sm font-mono text-gray-400`
           - Right: download icon button (disabled for now, same reason) + ChangelogExpandable
           - Changelog row below: `{download.changelog && <div className="mt-3 pt-3 border-t border-gray-100 dark:border-gray-800"><ChangelogExpandable changelog={download.changelog} /></div>}`
   - Export: `export function DownloadsList`

3. **Create `src/app/(portal)/downloads/page.tsx`** (server component):
   - Same auth guard pattern
   - Import `db` from `@/lib/db`
   - Import `{ downloads }` from `@/lib/db/schema`
   - Import `{ eq, desc }` from `drizzle-orm`
   - Import `PageBreadcrumb` from `@/components/common/PageBreadCrumb`
   - Import `DownloadsList` from `@/components/portal/DownloadsList`
   - Query: `const userDownloads = await db.select().from(downloads).where(eq(downloads.userId, userId)).orderBy(desc(downloads.createdAt))`
   - Render:
     - `<PageBreadcrumb pageTitle="Downloads" basePath="/dashboard" />`
     - `<DownloadsList downloads={userDownloads} emptyMessage="No downloads available yet. Plugin versions will appear here when released." />`

**Important note about changelog data:** The downloads table schema does not have a `changelog` column. The DownloadsList component accepts an optional `changelog` field on the download type. Since the DB query will not return changelog data, the expandable changelog UI will show "No changelog" for each row. When a changelog column is added to the schema later (or data is sourced elsewhere), the UI will work without changes. This follows the empty-state approach from D-01/D-04.
  </action>
  <acceptance_criteria>
    - `src/components/portal/ChangelogExpandable.tsx` starts with `"use client"`
    - ChangelogExpandable toggles `isExpanded` state
    - ChangelogExpandable rotates ChevronDown icon 180 degrees on expand
    - `src/components/portal/DownloadsList.tsx` does NOT have `"use client"`
    - DownloadsList has featured card with "Latest" Badge
    - DownloadsList has version history section for older downloads
    - `src/app/(portal)/downloads/page.tsx` queries downloads table filtered by userId
    - Downloads page renders PageBreadcrumb with basePath="/dashboard"
    - Billing page queries orders table filtered by userId
    - `pnpm build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd "D:\Devsroom-Work\conversionflow-website" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Billing page shows invoice table with BDT amounts, payment methods, status badges. Downloads page shows featured latest version card and version history with expandable changelogs. Both pages handle empty states. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL -> Billing page | Authenticated user could attempt to view billing page |
| Download token -> File access | Download tokens could be shared or guessed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Billing queries | mitigate | Every query filters by `eq(orders.userId, session.user.id)` |
| T-03-06 | Information Disclosure | Downloads queries | mitigate | Every query filters by `eq(downloads.userId, session.user.id)` |
| T-03-07 | Spoofing | Download tokens | accept | Download buttons disabled for now (no file serving route exists). Token-based download security deferred to when download API route is created. |
</threat_model>

<verification>
1. `pnpm build` passes with no errors
2. Billing page queries with `userId` filter on orders table
3. Downloads page queries with `userId` filter on downloads table
4. Invoice status badges use correct color mapping
5. Featured card shows "Latest" badge
6. ChangelogExpandable toggles with chevron rotation
7. Both pages have empty state messages
</verification>

<success_criteria>
- Customer sees billing table with order IDs, BDT amounts, payment method labels, date, and status badges
- Customer sees featured latest version card with version number, release date, and "Latest" badge
- Customer sees version history with expandable changelogs
- Empty states display correctly when no data exists
- All queries filter by authenticated user ID
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-customer-portal/03-03-SUMMARY.md`
</output>
