---
phase: 17-billing-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/app/api/ssl-commerz/ipn/route.ts
  - src/app/(admin)/actions/admin-orders.ts
autonomous: true
requirements:
  - ARCH-06
  - ARCH-08

must_haves:
  truths:
    - "IPN handler no longer imports from central-api.ts"
    - "Admin verify no longer imports from central-api.ts"
    - "IPN handler calls OrderService.completeOrder() for order completion"
    - "Admin verify calls OrderService.completeOrder() for order completion"
    - "Neither file sets centralOrderId, centralLicenseId, or centralUserId for new orders"
    - "No central API inbound webhook handlers remain in the codebase"
  artifacts:
    - path: "src/app/api/ssl-commerz/ipn/route.ts"
      provides: "Refactored IPN handler using Billing Context"
      contains: "OrderService"
      not_contains: ["importOrderToCentral", "mockImportOrderToCentral", "central-api"]
    - path: "src/app/(admin)/actions/admin-orders.ts"
      provides: "Refactored admin verify using Billing Context"
      contains: "OrderService"
      not_contains: ["importOrderToCentral", "mockImportOrderToCentral", "central-api"]
  key_links:
    - from: "src/app/api/ssl-commerz/ipn/route.ts"
      to: "src/modules/billing/application/services/OrderService.ts"
      via: "import { OrderService }"
      pattern: "import.*OrderService.*billing"
    - from: "src/app/(admin)/actions/admin-orders.ts"
      to: "src/modules/billing/application/services/OrderService.ts"
      via: "import { OrderService }"
      pattern: "import.*OrderService.*billing"
---

<objective>
Refactor the IPN handler and admin verify action to use the Billing Context instead of the central API.

Purpose: This is the actual cutover (per D-10). Removes the `importOrderToCentral()` and `mockImportOrderToCentral()` calls from both consumers, replacing them with `OrderService.completeOrder()`. Also removes the email sending from both files since the `OrderCompletedHandler` now handles it.

Output: Two files completely disconnected from `src/lib/central-api.ts`. The central API file stays (Phase 20 deletes it), but is no longer imported by any active flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-billing-integration/17-CONTEXT.md
@.planning/phases/17-billing-integration/17-RESEARCH.md
@.planning/phases/17-billing-integration/17-01-SUMMARY.md

<interfaces>
<!-- The executor must read the actual current files to understand exact line numbers -->

From Plan 01 output (executor reads SUMMARY for details):
- `OrderService.completeOrder(orderId: string, userId: string): Promise<void>` — marks order completed + publishes OrderCompleted event
- `registerBillingHandlers()` — must be called at app startup before first IPN/verify

Key DDD decisions:
- D-04: `BillingService.completeOrder()` is the single entry point
- D-06: IPN handler and admin verify become thin delegates
- D-10: Immediate cutover, no feature flag
- D-11: central-api.ts stays but no longer imported
- D-12: No centralOrderId/centralLicenseId/centralUserId for new orders
- D-13: Remove central API inbound webhook handlers (these are the importOrderToCentral calls)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor IPN handler and admin verify to use OrderService</name>
  <files>
    src/app/api/ssl-commerz/ipn/route.ts
    src/app/(admin)/actions/admin-orders.ts
  </files>
  <read_first>
    src/app/api/ssl-commerz/ipn/route.ts
    src/app/(admin)/actions/admin-orders.ts
    src/modules/billing/application/services/OrderService.ts
    src/modules/billing/application/handlers/OrderCompletedHandler.ts
    src/lib/central-api.ts
  </read_first>
  <action>
    **Refactor `src/app/api/ssl-commerz/ipn/route.ts`:**

    1. **Remove imports**: Delete `import { importOrderToCentral, mockImportOrderToCentral } from "@/lib/central-api"` (line 7-9)
    2. **Remove unused imports**: Remove `licenses` and `user` from the `import { orders, licenses, user } from "@/lib/db/schema"` import (line 3) — the handler no longer needs them. Keep `orders`.
    3. **Remove email import**: Delete `import { sendOrderConfirmationEmail } from "@/lib/emails/order-confirmation"` (line 11) — the `OrderCompletedHandler` now sends the email.
    4. **Add new import**: `import { OrderService } from "@/modules/billing/application/services/OrderService"`
    5. **Keep steps 1-6 unchanged** (validation, find order, idempotency check, update status, audit)
    6. **Replace step 7 (lines 99-106)**: Remove the user fetch (`db.select().from(user)...`) — no longer needed here since the handler does it.
    7. **Replace step 8 (lines 108-177)**: Remove the ENTIRE central API block (centralPayload construction, importOrderToCentral/mockImportOrderToCentral call, centralOrderId/centralUserId/license insert/central audit). Replace with:
       ```
       // 7. Trigger license generation via Billing Context
       try {
         const orderService = new OrderService();
         await orderService.completeOrder(order.id, order.userId);
       } catch (completionError) {
         console.error(`[IPN] Order completion failed for ${order.id}:`, completionError);
         // Order is marked completed, but license generation failed
         // SSL Commerce will retry the IPN — idempotency handles it
       }
       ```
       NOTE: Wait — step 5 (line 73-80) ALREADY updates the order status to "completed". But `OrderService.completeOrder()` ALSO updates the order status. This is a double update. The solution: **REMOVE step 5's status update** and let `OrderService.completeOrder()` handle it. Move the audit log (step 6, lines 82-97) to AFTER the `completeOrder()` call, or remove it since the handler creates its own audit log.
       
       **Correct approach**: Remove BOTH the status update (lines 73-80) and the order audit (lines 82-97). The `OrderService.completeOrder()` handles the status update, and the `OrderCompletedHandler` creates the `license.created` audit. However, the `order.status_changed` audit is still valuable. Move it to happen AFTER `completeOrder()` as a separate log, or keep it before the call (it logs the FROM->TO transition which is still accurate). Decision: **Keep the order.status_changed audit log BEFORE completeOrder()** since it records who triggered it (system for IPN), but move the status update INTO completeOrder().
       
       Final structure of the refactored IPN handler POST function:
       - Steps 1-4: Unchanged (validation, find order, idempotency check)
       - Step 5 (was status update): REMOVED — completeOrder() does this
       - Step 6 (was audit): KEEP — audit log for `order.status_changed` from `order.status` to `"completed"` (this is still useful for tracking when the IPN triggered the change)
       - Step 7: `await orderService.completeOrder(order.id, order.userId)` — wrapped in try/catch
       - Step 8 (was email): REMOVED — OrderCompletedHandler does this
       - Return `NextResponse.json({ ok: true })`

    8. **Remove `sendOrderConfirmationEmail` import** since the handler now sends the email.

    **Refactor `src/app/(admin)/actions/admin-orders.ts`:**

    1. **Remove imports**: Delete `import { importOrderToCentral, mockImportOrderToCentral, type ImportOrderPayload } from "@/lib/central-api"` (lines 10-14)
    2. **Remove unused imports**: Remove `licenses` from the `import { orders, licenses, user } from "@/lib/db/schema"` import. Keep `orders`. Keep `user` only if `verifyOrder` still needs it for the audit log actor info — actually `requireAdmin()` provides `userId` and `role`, so `user` import can be removed too.
    3. **Remove email import**: Delete `import { sendOrderConfirmationEmail } from "@/lib/emails/order-confirmation"` (line 16)
    4. **Add new import**: `import { OrderService } from "@/modules/billing/application/services/OrderService"`
    5. **In `verifyOrder()` function**:
       - Keep lines up to the status check (`if (order.status !== "pending") return { error: "Only pending orders can be verified." }`)
       - Keep the order.status_changed audit log (lines 72-79) — it records the admin actor
       - **REMOVE lines 82-181**: The entire central API block (user fetch, payload construction, importOrderToCentral/mock call, centralOrderId update, user centralUserId update, license insert, license audit, email send). Replace with:
         ```
         // Trigger license generation via Billing Context
         try {
           const orderService = new OrderService();
           await orderService.completeOrder(orderId, order.userId);
         } catch (completionError) {
           console.error(`[Admin] Order completion failed for ${orderId}:`, completionError);
           return { error: "Order completed but license generation failed. Check logs." };
         }
         ```
       - **Remove the status update** (lines 66-69 `db.update(orders).set({ status: "completed" })`) — `OrderService.completeOrder()` now handles this. BUT wait — the audit log at lines 72-79 records `from: "pending", to: "completed"`. If we remove the status update, the audit log still records the intended transition (accurate). And `completeOrder()` does the actual DB update. This is fine.
       - Keep `return { success: true }` at the end.
    6. **`rejectOrder()` and `issueRefund()`**: Leave completely unchanged. These don't touch the central API.

    **Verify no remaining central-api imports:**
    After refactoring, run: `grep -r "central-api" src/app/` — should return zero results for both files.
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && npx tsc --noEmit --pretty 2>&1 | head -30 && echo "---TYPE CHECK DONE---" && echo "=== Checking IPN handler ===" && grep -c "central-api" src/app/api/ssl-commerz/ipn/route.ts && grep -c "importOrderToCentral" src/app/api/ssl-commerz/ipn/route.ts && grep "OrderService" src/app/api/ssl-commerz/ipn/route.ts && echo "=== Checking admin-orders ===" && grep -c "central-api" src/app/(admin)/actions/admin-orders.ts && grep -c "importOrderToCentral" src/app/(admin)/actions/admin-orders.ts && grep "OrderService" src/app/(admin)/actions/admin-orders.ts && echo "=== Central API import count ===" && grep -rn "from.*central-api" src/ --include="*.ts" --include="*.tsx" 2>/dev/null | grep -v "node_modules" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "central-api" src/app/api/ssl-commerz/ipn/route.ts` returns exit code 1 (no match)
    - `grep "central-api" src/app/(admin)/actions/admin-orders.ts` returns exit code 1 (no match)
    - `grep "importOrderToCentral" src/app/api/ssl-commerz/ipn/route.ts` returns exit code 1 (no match)
    - `grep "importOrderToCentral" src/app/(admin)/actions/admin-orders.ts` returns exit code 1 (no match)
    - `grep "OrderService" src/app/api/ssl-commerz/ipn/route.ts` returns a match
    - `grep "OrderService" src/app/(admin)/actions/admin-orders.ts` returns a match
    - IPN handler NO LONGER contains `db.insert(licenses)` — license creation is via GenerateLicenseHandler
    - Admin verify NO LONGER contains `db.insert(licenses)` — license creation is via GenerateLicenseHandler
    - IPN handler NO LONGER calls `sendOrderConfirmationEmail` directly
    - Admin verify NO LONGER calls `sendOrderConfirmationEmail` directly
    - `grep -rn "from.*central-api" src/` returns matches ONLY in `src/lib/central-api.ts` itself (self-reference) and any other files NOT touched by Phase 17 (none expected)
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>
    Both the IPN handler and admin verify are refactored to use `OrderService.completeOrder()`. The central API client (`src/lib/central-api.ts`) is no longer imported by any active flow. No `importOrderToCentral` or `mockImportOrderToCentral` calls remain. No direct license inserts or email sends in either file — all handled by the `OrderCompletedHandler`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSL Commerce -> IPN handler | External payment gateway sends untrusted form data. Payment validated server-to-server before `completeOrder()`. |
| Admin -> verifyOrder | Admin user action. Protected by `requireAdmin()` guard. |
| Both -> OrderService | Internal trusted call. OrderService handles status update + event emission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Tampering | IPN duplicate handling | mitigate | Order-level idempotency (existing check at line 69-71) + license-level idempotency in OrderCompletedHandler |
| T-17-06 | Spoofing | SSL Commerce IPN | mitigate | Server-to-server validation via `validateSSLPayment(valId)` — unchanged by this refactoring |
| T-17-07 | Elevation of Privilege | Admin verify | mitigate | `requireAdmin()` guard checks session + role — unchanged |
</threat_model>

<verification>
1. TypeScript compilation passes: `npx tsc --noEmit`
2. `grep -rn "from.*central-api" src/app/` returns zero results
3. `grep "OrderService" src/app/api/ssl-commerz/ipn/route.ts` returns match
4. `grep "OrderService" src/app/(admin)/actions/admin-orders.ts` returns match
5. `grep "sendOrderConfirmationEmail" src/app/api/ssl-commerz/ipn/route.ts` returns exit code 1 (no match)
6. `grep "sendOrderConfirmationEmail" src/app/(admin)/actions/admin-orders.ts` returns exit code 1 (no match)
</verification>

<success_criteria>
- IPN handler uses `OrderService.completeOrder()` instead of central API
- Admin verify uses `OrderService.completeOrder()` instead of central API
- Zero references to `central-api` in `src/app/` directory
- Both files are significantly shorter (removed 70-100 lines of central API logic each)
- TypeScript compiles cleanly
- `rejectOrder()` and `issueRefund()` functions are completely unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/17-billing-integration/17-02-SUMMARY.md`
</output>
