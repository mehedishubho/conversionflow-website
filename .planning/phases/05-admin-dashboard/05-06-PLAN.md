---
phase: 05-admin-dashboard
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/LicensesCSVExportButton.tsx
  - src/app/(admin)/admin/licenses/page.tsx
autonomous: true
requirements: [ADMIN-CSV, ADMIN-LICENSES]
gap_closure: true

must_haves:
  truths:
    - "CSV export button on activity page downloads a dated CSV file"
    - "CSV export button on licenses page downloads a dated CSV file"
    - "Licenses page renders a read-only table with License Key, Customer, Plan, Status, Activations, Created columns"
    - "Status badges show correct colors: active=green, expired=yellow, revoked=red, suspended=gray"
  artifacts:
    - path: "src/components/admin/LicensesCSVExportButton.tsx"
      provides: "Client-side CSV export button with columns defined client-side"
      exports: ["default LicensesCSVExportButton"]
    - path: "src/app/(admin)/admin/licenses/page.tsx"
      provides: "Licenses admin page passing only serializable props to client components"
      contains: "LicensesCSVExportButton"
  key_links:
    - from: "src/app/(admin)/admin/licenses/page.tsx"
      to: "src/components/admin/LicensesCSVExportButton.tsx"
      via: "import and JSX render"
      pattern: "LicensesCSVExportButton"
    - from: "src/components/admin/LicensesCSVExportButton.tsx"
      to: "src/lib/utils/csv-export.ts"
      via: "exportToCSV function call"
      pattern: "exportToCSV"
---

<objective>
Fix server/client boundary serialization error on the Licenses admin page.

Purpose: Functions (arrow functions in csvColumns.accessor) defined in a server component cannot be serialized and passed to a client component. This prevents the entire licenses page from rendering and blocks CSV export on both activity and licenses pages.

Output: Licenses page renders correctly with CSV export working.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LicensesCSVExportButton client component</name>
  <files>src/components/admin/LicensesCSVExportButton.tsx</files>
  <action>
Create a new client component `src/components/admin/LicensesCSVExportButton.tsx` with `"use client"` directive.

This component is a self-contained CSV export button for the licenses page. It defines `csvColumns` with accessor functions INTERNALLY (client-side), so no functions cross the server/client boundary.

Structure:
```
"use client";

import { Download } from "lucide-react";
import { exportToCSV, type CSVColumn } from "@/lib/utils/csv-export";

const csvColumns: CSVColumn[] = [
  { header: "License Key", accessor: (r) => r.licenseKey as string },
  { header: "Customer", accessor: (r) => (r.userName as string) || "Unknown" },
  { header: "Plan", accessor: (r) => r.plan as string },
  { header: "Status", accessor: (r) => r.status as string },
  { header: "Activations", accessor: (r) => `${r.currentActivations}/${r.maxActivations}` },
  { header: "Created", accessor: (r) => new Date(r.createdAt as Date).toLocaleDateString("en-BD") },
];

interface LicensesCSVExportButtonProps {
  rows: Record<string, unknown>[];
  filename: string;
}

export default function LicensesCSVExportButton({ rows, filename }: LicensesCSVExportButtonProps) {
  // Same button styling as CSVExportButton but columns are internal
  // On click: exportToCSV(csvColumns, rows, filename)
}
```

Key points:
- `csvColumns` is defined INSIDE this client component file, not passed as a prop
- Props are only plain serializable data: `rows` (array of plain objects) and `filename` (string)
- Button styling matches the existing `CSVExportButton` exactly (same classes, same icon)
- Uses the same `exportToCSV` utility from `@/lib/utils/csv-export`
  </action>
  <verify>File exists at src/components/admin/LicensesCSVExportButton.tsx with "use client" directive, csvColumns defined internally, no accessor functions in props interface.</verify>
  <done>LicensesCSVExportButton.tsx created as a client component that owns its column definitions internally. Only accepts serializable props (rows array and filename string).</done>
</task>

<task type="auto">
  <name>Task 2: Update licenses page to use LicensesCSVExportButton</name>
  <files>src/app/(admin)/admin/licenses/page.tsx</files>
  <action>
Modify `src/app/(admin)/admin/licenses/page.tsx`:

1. REMOVE the `csvColumns` constant definition (lines 21-28) entirely -- these accessor functions were the serialization error cause.

2. CHANGE the import:
   - REMOVE: `import CSVExportButton from "@/components/admin/CSVExportButton";`
   - ADD: `import LicensesCSVExportButton from "@/components/admin/LicensesCSVExportButton";`

3. UPDATE the JSX (lines 65-69):
   Replace:
   ```
   <CSVExportButton
     columns={csvColumns}
     rows={licenseRows as unknown as Record<string, unknown>[]}
     filename="licenses"
   />
   ```
   With:
   ```
   <LicensesCSVExportButton
     rows={licenseRows as unknown as Record<string, unknown>[]}
     filename="licenses"
   />
   ```

4. Verify no other references to `csvColumns` or `CSVExportButton` remain in this file.

The page remains a server component. It now only passes plain serializable data (row objects and a string) to the client component, which handles column definitions internally.
  </action>
  <verify>
    1. `pnpm build` succeeds without serialization errors
    2. No `csvColumns` definition in licenses/page.tsx
    3. No import of CSVExportButton in licenses/page.tsx
    4. LicensesCSVExportButton is imported and used with only `rows` and `filename` props
  </verify>
  <done>Licenses page passes only serializable data to client components. Build succeeds. Page renders without "Functions cannot be passed" error.</done>
</task>

</tasks>

<verification>
1. Run `pnpm build` -- must succeed with no errors related to serialization or server/client boundaries
2. Verify `csvColumns` no longer exists in `src/app/(admin)/admin/licenses/page.tsx`
3. Verify `LicensesCSVExportButton` is imported in the licenses page and receives only `rows` and `filename` props
4. Verify ActivityFeedFull.tsx was NOT modified (it was already correct -- csvColumns defined client-side)
</verification>

<success_criteria>
- `pnpm build` succeeds with zero errors
- Licenses page (`/admin/licenses`) renders without the "Functions cannot be passed" error
- CSV export button on licenses page triggers a CSV download when clicked
- CSV export button on activity page still works (unchanged, already correct)
- Status badges on licenses table show correct colors (active=green, expired=yellow, revoked=red, suspended=gray)
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-dashboard/05-06-SUMMARY.md`
</output>
