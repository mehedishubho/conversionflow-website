---
phase: 05-admin-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/invoices/page.tsx
  - src/app/(admin)/admin/users/page.tsx
  - src/components/admin/InvoiceActions.tsx
autonomous: true
requirements:
  - ADMN-05

must_haves:
  truths:
    - "Admin can filter invoices by status using tabs (All/Paid/Pending/Failed)"
    - "Admin can mark a pending order as paid which triggers license creation and confirmation email"
    - "Admin can send a payment reminder email to customers with pending orders"
    - "Users list page has a View link on each row navigating to /admin/users/[id]"
  artifacts:
    - path: "src/app/(admin)/admin/invoices/page.tsx"
      provides: "Enhanced invoices page with filter tabs and action buttons"
      contains: "InvoiceActions"
    - path: "src/components/admin/InvoiceActions.tsx"
      provides: "Client component for invoice filter tabs, mark as paid, send reminder"
      contains: "markAsPaid"
    - path: "src/app/(admin)/admin/users/page.tsx"
      provides: "Enhanced users list with View link column"
      contains: "/admin/users/"
  key_links:
    - from: "src/components/admin/InvoiceActions.tsx"
      to: "src/app/(admin)/actions/admin-orders.ts"
      via: "verifyOrder server action for mark-as-paid"
      pattern: "verifyOrder"
    - from: "src/app/(admin)/admin/invoices/page.tsx"
      to: "src/components/admin/InvoiceActions.tsx"
      via: "component import"
      pattern: "InvoiceActions"
---

<objective>
Enhance the invoices page with status filter tabs, mark-as-paid, and send-reminder actions. Add a View link to the users list for navigation to user detail pages.

Purpose: Admin operators can manage invoice lifecycles (filter, approve, remind) and navigate to user details from the users list.
Output: Enhanced invoices page with filters and actions. Enhanced users list with View links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-dashboard/05-CONTEXT.md
@.planning/phases/05-admin-dashboard/05-RESEARCH.md
@.planning/phases/05-admin-dashboard/05-UI-SPEC.md
@.planning/phases/04-checkout-payments/04-04-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/app/(admin)/actions/admin-orders.ts:
```typescript
export async function verifyOrder(orderId: string): Promise<{ success?: boolean; error?: string }>
```

From src/lib/db/schema.ts:
```typescript
export const orderStatusEnum = pgEnum("order_status", ["pending", "completed", "failed", "refunded"]);
```

From src/lib/emails/order-confirmation.ts (pattern for email templates):
```typescript
const resend = new Resend(process.env.RESEND_API_KEY);
export async function sendOrderConfirmationEmail(params: OrderConfirmationParams): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance users list with View link column</name>
  <files>
    src/app/(admin)/admin/users/page.tsx
  </files>
  <read_first>
    src/app/(admin)/admin/users/page.tsx
  </read_first>
  <action>
    **Modify `src/app/(admin)/admin/users/page.tsx`** (per D-11):

    Add a "View" column to the existing users table:

    1. Add a new `<th>` header column after "Joined": `<th className="text-left py-3 px-4 font-medium text-gray-500 dark:text-gray-400">Action</th>`.
    2. Update the empty state `colSpan` from `6` to `7`.
    3. In each user row, add a new `<td>` after the "Joined" cell:
       ```tsx
       <td className="py-3 px-4">
         <Link href={`/admin/users/${u.id}`} className="text-sm font-medium text-brand-500 hover:text-brand-600">
           View
         </Link>
       </td>
       ```
    4. Import `Link` from `next/link` at the top of the file (add `import Link from "next/link"`).
    5. No other changes to the existing page structure -- keep the 3 summary cards, table, and badge logic intact.
  </action>
  <verify>
    <automated>cd "D:\Devsroom-Work\conversionflow-website" && grep "View" src/app/\(admin\)/admin/users/page.tsx | grep -c "Link" && grep "admin/users/" src/app/\(admin\)/admin/users/page.tsx</automated>
  </verify>
  <done>
    - Users table has an "Action" column header
    - Each user row has a "View" link pointing to /admin/users/{userId}
    - colSpan updated to 7 for empty state
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance invoices page with filter tabs, mark as paid, and send reminder</name>
  <files>
    src/app/(admin)/admin/invoices/page.tsx,
    src/components/admin/InvoiceActions.tsx,
    src/lib/emails/payment-reminder.ts
  </files>
  <read_first>
    src/app/(admin)/admin/invoices/page.tsx
    src/app/(admin)/actions/admin-orders.ts
    src/lib/emails/order-confirmation.ts
    src/components/ui/modal/index.tsx
    src/components/common/ComponentCard.tsx
  </read_first>
  <action>
    **1. Create `src/lib/emails/payment-reminder.ts`** — Payment reminder email template (per D-16):

    - Follow the same pattern as `order-confirmation.ts`: use Resend client, same app URL pattern.
    - Function: `sendPaymentReminderEmail(params: { to: string; orderNumber: string; planName: string; amount: number; currency: string; paymentMethod: string })`.
    - Email subject: "Payment Reminder - Order #{orderNumber}".
    - HTML body: ConversionFlow branded email (same structure as order-confirmation):
      - Header with ConversionFlow logo/name
      - "Your payment is pending" heading
      - Order details: plan, amount (formatted as BDT), payment method
      - "Please complete your payment to activate your license" message
      - Link to the checkout/order page (if applicable)
      - Footer with support contact
    - Wrap `resend.emails.send()` in try/catch with `console.error` on failure (same error handling as order-confirmation).

    **2. Create `src/components/admin/InvoiceActions.tsx`** — Client component (per D-14, D-15, D-16):

    - `"use client"` directive.
    - Props: `{ orders: InvoiceOrder[] }` where InvoiceOrder has fields: `id, userName, userEmail, plan, amount, currency, paymentMethod, couponCode, discountAmount, taxAmount, status, createdAt`.
    - State: `activeTab` (string: "all" | "completed" | "pending" | "failed"), `actionError` (string), `confirmModal` (null | { type: "markPaid" | "sendReminder", orderId: string, email: string }).

    **Filter tabs** (per D-14):
    - Render horizontal tab bar above the table: "All ({count})", "Paid ({completedCount})", "Pending ({pendingCount})", "Failed ({failedCount})".
    - Active tab style: `bg-brand-50 text-brand-500 border-brand-500`.
    - Inactive: `bg-white text-gray-500 border-gray-200 dark:bg-gray-800 dark:text-gray-400`.
    - Filter `orders` by `activeTab` matching status (map "paid" -> "completed").

    **Table rendering:**
    - Same columns as current invoices page: Invoice#, Customer, Plan, Amount, Discount, VAT, Method, Date, Actions.
    - For pending orders, the Actions column shows two buttons:
      - "Mark as Paid" button: `bg-brand-500 text-white` style. On click, sets confirmModal with type "markPaid".
      - "Send Reminder" button: outline style `border border-gray-200 text-gray-700`. On click, sets confirmModal with type "sendReminder".
    - For non-pending orders, Actions column shows existing "View" link.

    **Confirmation modal** (per D-15):
    - When confirmModal.type === "markPaid": Show modal "Mark this order as paid?" with body "This will trigger license creation and send a confirmation email to the customer."
    - On confirm: Call `verifyOrder(orderId)` from admin-orders.ts. On success, `router.refresh()`. On error, set `actionError`.
    - When confirmModal.type === "sendReminder": Show modal "Send payment reminder?" with body "A payment reminder will be sent to {email}."
    - On confirm: Call a new `sendPaymentReminder(orderId)` server action (defined inline in this file or imported). On success, show feedback "Payment reminder sent to {email}". On error, set `actionError`.

    **3. Create `sendPaymentReminder` server action** (add to a new file or to InvoiceActions inline):
    - Add as a server action in `src/app/(admin)/actions/admin-invoices.ts` (new file):
      - `"use server"` directive.
      - Copy `requireAdmin()` pattern.
      - Import `sendPaymentReminderEmail` from payment-reminder.ts.
      - `sendPaymentReminder(orderId: string)`:
        - Call requireAdmin().
        - Fetch order by ID with user join to get email, plan, amount, currency, paymentMethod.
        - Validate order status is "pending" (can only remind on pending orders).
        - Call `sendPaymentReminderEmail({ to, orderNumber, planName, amount, currency, paymentMethod })`.
        - Create audit log: `action: "order.reminder_sent", targetType: "order", targetId: orderId`.
        - Return `{ success: true }` or `{ error: string }`.

    **4. Replace `src/app/(admin)/admin/invoices/page.tsx`** (per D-14):

    - Keep auth check pattern and `export const dynamic = "force-dynamic"`.
    - Change the data query: Instead of filtering by `eq(orders.status, "completed")`, query ALL orders (remove the where clause for status), joining user for name/email.
    - Pass the full order list to `InvoiceActions` component.
    - Remove the existing table JSX (it now lives inside InvoiceActions).
    - Keep PageBreadcrumb and ComponentCard wrapper.
  </action>
  <verify>
    <automated>cd "D:\Devsroom-Work\conversionflow-website" && grep -c "markAsPaid\|verifyOrder" src/components/admin/InvoiceActions.tsx && grep "sendPaymentReminder" src/app/\(admin\)/actions/admin-invoices.ts && grep "payment-reminder" src/lib/emails/payment-reminder.ts && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
    - Invoice page shows filter tabs (All/Paid/Pending/Failed) with counts
    - Pending orders show "Mark as Paid" and "Send Reminder" buttons
    - Mark as Paid calls verifyOrder (triggers license creation + confirmation email)
    - Send Reminder sends payment reminder email via Resend
    - Confirmation modals for both actions
    - Payment reminder email template exists
    - admin-invoices.ts has sendPaymentReminder server action with requireAdmin guard
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client component -> server action | InvoiceActions calls verifyOrder and sendPaymentReminder with orderId from UI |
| Email sending | Resend API call with customer email address |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T | markAsPaid | mitigate | verifyOrder already validates order is pending before completing; requireAdmin guard |
| T-05-05 | T | sendPaymentReminder | mitigate | Server action validates orderId exists and order is pending before sending email |
| T-05-06 | I | Invoice data | accept | Admin-only page, all data is business records admin is authorized to manage |
</threat_model>

<verification>
- Visit /admin/invoices: filter tabs render with counts
- Click "Pending" tab: only pending orders shown
- Click "Mark as Paid" on a pending order: confirmation modal appears
- Click "Send Reminder": confirmation modal appears
- Visit /admin/users: each row has "View" link
- TypeScript compiles: `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Invoice page has 4 filter tabs with dynamic counts
- Mark as Paid reuses verifyOrder logic (license creation + email)
- Send Reminder sends email via new payment-reminder.ts template
- Users list has View link navigating to /admin/users/[id]
- All actions have confirmation modals with clear descriptions
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-dashboard/05-02-SUMMARY.md`
</output>
