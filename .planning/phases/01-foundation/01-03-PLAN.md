---
phase: 1
plan: 03
type: foundation
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/components/layout/Navbar.tsx
autonomous: true
requirements:
  - FOUND-01
  - FOUND-06
---

<objective>
Fix the Navbar hydration flash by rendering an invisible spacer div before mount instead of `null`, then verify the full build succeeds with `pnpm build`. This plan depends on 01-01 (cn utility) and 01-02 (Footer + button CSS) being complete first — it runs the final build verification that proves all Phase 1 fixes work together.
</objective>

<tasks>

<task id="1" type="edit" files="src/components/layout/Navbar.tsx">
  <action>
  In `src/components/layout/Navbar.tsx`, replace line 33:
  ```tsx
  if (!mounted) return null;
  ```
  with:
  ```tsx
  if (!mounted) {
    return (
      <div className="fixed top-4 left-0 right-0 z-[900] flex justify-center px-5 pointer-events-none">
        <div className="max-w-[1160px] w-full h-[56px]" />
      </div>
    );
  }
  ```

  This renders an invisible spacer div that matches the outer wrapper positioning of the navbar (`fixed top-4 left-0 right-0 z-[900] flex justify-center px-5 pointer-events-none`) with an inner div that occupies the same width (`max-w-[1160px] w-full`) and approximate height (`h-[56px]`) as the rendered navbar. The spacer is invisible (no background, no border) but prevents layout shift when the real navbar mounts and takes its place.

  The height `56px` approximates the navbar's rendered height: `py-2` (8px top + 8px bottom) + content height (~40px for the logo/buttons). If the actual rendered height differs, adjust `h-[56px]` accordingly.
  </action>
  <read_first>
  - `src/components/layout/Navbar.tsx` (full file — confirm line 33 has `if (!mounted) return null;`)
  - `src/app/layout.tsx` (confirm Navbar is rendered inside body, understand surrounding layout context)
  </read_first>
  <acceptance_criteria>
  - `src/components/layout/Navbar.tsx` does NOT contain `return null` anywhere
  - `src/components/layout/Navbar.tsx` contains a spacer div with `h-[56px]` returned when `!mounted`
  - The spacer div uses same outer positioning classes as the real navbar wrapper: `fixed top-4 left-0 right-0 z-[900]`
  - The spacer is visually invisible (no `bg-*`, no `border-*` on the spacer itself)
  - No other logic changed in the component — `useState`, `useEffect`, scroll handler, and all JSX after the mount guard remain identical
  </acceptance_criteria>
</task>

<task id="2" type="verify" files="">
  <action>
  Run the following verification commands to confirm all Phase 1 fixes work together:

  1. `pnpm build` — must complete with zero errors. This validates:
     - `src/lib/utils.ts` exists and exports `cn()` (plan 01-01)
     - `src/components/layout/Footer.tsx` exists and exports `Footer` (plan 01-02)
     - All imports resolve correctly
     - TypeScript compilation passes
     - Next.js static generation succeeds

  2. `pnpm lint` — must pass with zero errors. This validates:
     - ESLint rules satisfied across all modified/created files
     - No unused imports or variables

  If `pnpm build` fails, analyze the error output and fix the issue. Common failure modes:
  - Import resolution errors: check file paths and exports
  - TypeScript errors: check type annotations and usage
  - CSS errors: check Tailwind class validity in globals.css
  </action>
  <read_first>
  - `src/lib/utils.ts` (created in plan 01-01 — verify it exists)
  - `src/components/layout/Footer.tsx` (created in plan 01-02 — verify it exists)
  - `src/components/layout/Navbar.tsx` (modified in plans 01-01 and 01-03 — verify both changes applied)
  - `src/app/globals.css` (modified in plan 01-02 — verify button classes added)
  - `src/app/layout.tsx` (imports Navbar, Footer, ThemeProvider — all must resolve)
  </read_first>
  <acceptance_criteria>
  - `pnpm build` exits with code 0 (success)
  - `pnpm build` output does not contain "Error:" or "Failed to compile"
  - `pnpm build` output does not contain "Module not found"
  - `pnpm build` output does not contain "Type error"
  - `pnpm lint` exits with code 0 (success)
  - `.next/` directory is created (build artifacts exist)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `grep -c "return null" src/components/layout/Navbar.tsx` — returns 0
2. `grep "h-\[56px\]" src/components/layout/Navbar.tsx` — spacer height present
3. `pnpm build` — exits 0 with no errors
4. `pnpm lint` — exits 0 with no errors
5. `ls .next/` — build output directory exists
</verification>

<success_criteria>
- [ ] Navbar renders invisible spacer before mount (no layout shift)
- [ ] Navbar no longer returns `null` before mount
- [ ] `pnpm build` completes successfully with zero errors
- [ ] `pnpm lint` passes with zero errors
- [ ] All Phase 1 requirements verified: FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06, NAV-02
</success_criteria>

<must_haves>
- Navbar skeleton spacer prevents layout shift
- `pnpm build` succeeds (FOUND-01)
- `pnpm lint` passes
</must_haves>
