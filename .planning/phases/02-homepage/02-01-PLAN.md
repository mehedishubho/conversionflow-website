---
phase: 02-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/context/ThemeContext.tsx
  - src/components/common/ThemeToggleButton.tsx
  - src/components/common/ThemeTogglerTwo.tsx
  - src/data/dashboard-nav.ts
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "Old ThemeContext is deleted and no files import from it"
    - "ThemeToggleButton uses next-themes useTheme instead of custom context"
    - "NavItem type and both nav configs (customer + admin) exist in dashboard-nav.ts"
    - "lucide-react icons are used for all nav items"
  artifacts:
    - path: "src/data/dashboard-nav.ts"
      provides: "NavItem type, customerNavItems, adminNavItems"
      exports: ["NavItem", "customerNavItems", "adminNavItems"]
    - path: "src/components/common/ThemeToggleButton.tsx"
      provides: "Theme toggle using next-themes"
      contains: "useTheme"
    - path: "src/components/common/ThemeTogglerTwo.tsx"
      provides: "Alternate theme toggle using next-themes"
      contains: "useTheme"
  key_links:
    - from: "src/components/common/ThemeToggleButton.tsx"
      to: "next-themes"
      via: "import { useTheme } from next-themes"
      pattern: "import.*useTheme.*from.*next-themes"
    - from: "src/data/dashboard-nav.ts"
      to: "lucide-react"
      via: "icon imports"
      pattern: "import.*from.*lucide-react"
---

<objective>
Clean up the old dual-theme system and create the nav data configuration that downstream components depend on.

Purpose: The project has two competing theme systems (custom ThemeContext vs next-themes). The custom one must be removed before the dashboard header can work. The nav data file defines the shared types that AppSidebar and DashboardShell consume -- it is the interface contract for the entire phase.

Output: Deleted ThemeContext, rewritten theme toggles, new dashboard-nav.ts with typed nav configs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-homepage/02-CONTEXT.md
@.planning/phases/02-homepage/02-RESEARCH.md

@src/context/ThemeContext.tsx
@src/components/common/ThemeToggleButton.tsx
@src/components/common/ThemeTogglerTwo.tsx
@src/styles/dashboard.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old ThemeContext and rewrite theme toggles for next-themes</name>
  <files>src/context/ThemeContext.tsx, src/components/common/ThemeToggleButton.tsx, src/components/common/ThemeTogglerTwo.tsx</files>
  <read_first>
    - src/context/ThemeContext.tsx (the file being deleted)
    - src/components/common/ThemeToggleButton.tsx (the file being rewritten)
    - src/components/common/ThemeTogglerTwo.tsx (the file being rewritten)
    - src/components/layout/Navbar.tsx (reference for how next-themes is used in the project)
  </read_first>
  <acceptance_criteria>
    - File src/context/ThemeContext.tsx does NOT exist on disk
    - Grep for "from.*@/context/ThemeContext" returns ZERO matches across all .tsx/.ts files
    - Grep for "from.*next-themes" in ThemeToggleButton.tsx returns at least 1 match
    - Grep for "from.*next-themes" in ThemeTogglerTwo.tsx returns at least 1 match
    - Both files contain "useTheme" usage
    - ThemeToggleButton.tsx uses Sun and Moon from lucide-react (not inline SVGs)
    - ThemeTogglerTwo.tsx uses Sun and Moon from lucide-react (not inline SVGs)
    - pnpm build succeeds
  </acceptance_criteria>
  <action>
    Per D-12 and DASH-03, delete the old custom ThemeContext and migrate both theme toggle components to next-themes.

    Step 1: Delete src/context/ThemeContext.tsx entirely.

    Step 2: Rewrite src/components/common/ThemeToggleButton.tsx as a "use client" component that:
    - Imports `useTheme` from "next-themes" (NOT from @/context/ThemeContext)
    - Imports `Sun` and `Moon` from "lucide-react" (per D-12, replace inline SVGs)
    - Renders a `<button>` with onClick that calls `setTheme(theme === "dark" ? "light" : "dark")`
    - Uses conditional rendering: `{theme === "dark" ? <Sun /> : <Moon />}`
    - Styling uses dashboard.css tokens: `text-text2 bg-surface border border-border rounded-full h-11 w-11 hover:bg-accent-light transition-colors`
    - Includes a hydration guard: only render icon after `mounted` state is true (use `useState(false)` + `useEffect(() => setMounted(true), [])`)

    Step 3: Rewrite src/components/common/ThemeTogglerTwo.tsx identically to ThemeToggleButton (same logic, same lucide-react icons) but with the larger rounded-full styling from the original (size-14, bg-accent text-white hover:bg-accent-2). This component is a variant used in different contexts.

    Step 4: Grep the entire src/ directory for any remaining imports of `@/context/ThemeContext` and fix them. The only consumers should be ThemeToggleButton and ThemeTogglerTwo -- verify no other files import from it.
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && node -e "const fs=require('fs'); if(fs.existsSync('src/context/ThemeContext.tsx')){console.log('FAIL: ThemeContext.tsx still exists');process.exit(1)}else{console.log('PASS: ThemeContext.tsx deleted')}" && grep -r "from.*@/context/ThemeContext" src/ --include="*.tsx" --include="*.ts" -l || echo "PASS: No ThemeContext imports remain" && grep "from.*next-themes" src/components/common/ThemeToggleButton.tsx && grep "from.*next-themes" src/components/common/ThemeTogglerTwo.tsx && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>ThemeContext.tsx deleted, both theme toggles use next-themes with lucide-react icons, no remaining imports of old ThemeContext, pnpm build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create nav data config with typed customer and admin nav items</name>
  <files>src/data/dashboard-nav.ts</files>
  <read_first>
    - src/data/navigation.ts (existing nav data pattern reference)
    - src/context/SidebarContext.tsx (for understanding submenu support)
  </read_first>
  <acceptance_criteria>
    - File src/data/dashboard-nav.ts exists
    - Exports `NavItem` type with fields: name (string), icon (LucideIcon), path (string), subItems (optional array of {name, path})
    - Exports `customerNavItems` array with exactly 6 items
    - Exports `adminNavItems` array with exactly 6 items
    - Customer nav item names (in order): Dashboard, Licenses, Billing, Downloads, Support, Account
    - Admin nav item names (in order): Overview, Sales, Users, Invoices, Licenses, Settings
    - All icons imported from lucide-react (no SVG imports)
    - Customer paths start with /dashboard (except the root /dashboard)
    - Admin paths start with /admin
  </acceptance_criteria>
  <action>
    Per D-09, D-10, D-11, and D-12, create the shared nav configuration data file.

    Create src/data/dashboard-nav.ts with:

    1. Import `type LucideIcon` from "lucide-react"
    2. Import specific icons: `LayoutDashboard, Key, CreditCard, Download, MessageSquare, UserCog, BarChart3, Users, FileText, Settings` from "lucide-react"
    3. Export the `NavItem` type:
       ```
       export type NavItem = {
         name: string;
         icon: LucideIcon;
         path: string;
         subItems?: { name: string; path: string }[];
       };
       ```
    4. Export `customerNavItems: NavItem[]` with these 6 items (per D-10):
       - `{ name: "Dashboard", icon: LayoutDashboard, path: "/dashboard" }`
       - `{ name: "Licenses", icon: Key, path: "/dashboard/licenses" }`
       - `{ name: "Billing", icon: CreditCard, path: "/dashboard/billing" }`
       - `{ name: "Downloads", icon: Download, path: "/dashboard/downloads" }`
       - `{ name: "Support", icon: MessageSquare, path: "/dashboard/support" }`
       - `{ name: "Account", icon: UserCog, path: "/dashboard/account" }`
       No subItems on any customer nav item.
    5. Export `adminNavItems: NavItem[]` with these 6 items (per D-11):
       - `{ name: "Overview", icon: BarChart3, path: "/admin/dashboard" }`
       - `{ name: "Sales", icon: CreditCard, path: "/admin/sales" }`
       - `{ name: "Users", icon: Users, path: "/admin/users" }`
       - `{ name: "Invoices", icon: FileText, path: "/admin/invoices" }`
       - `{ name: "Licenses", icon: Key, path: "/admin/licenses" }`
       - `{ name: "Settings", icon: Settings, path: "/admin/settings" }`
       No subItems on any admin nav item.

    The subItems field exists on the type for future phases but is not used in Phase 2 (per D-10/D-11 -- flat navs, no submenus needed).
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && node -e "const m=require('./src/data/dashboard-nav.ts'); console.log('customer:', m.customerNavItems?.length, 'admin:', m.adminNavItems?.length)" 2>/dev/null; test -f src/data/dashboard-nav.ts && echo "PASS: File exists" && grep "customerNavItems" src/data/dashboard-nav.ts && grep "adminNavItems" src/data/dashboard-nav.ts && grep "lucide-react" src/data/dashboard-nav.ts</automated>
  </verify>
  <done>dashboard-nav.ts exists with NavItem type, 6 customer nav items, 6 admin nav items, all using lucide-react icons, no subItems on any item.</done>
</task>

</tasks>

<verification>
- ThemeContext.tsx deleted, no remaining imports
- Both theme toggle components use next-themes and lucide-react
- dashboard-nav.ts exports NavItem type + both nav configs
- pnpm build succeeds
</verification>

<success_criteria>
- Old ThemeContext fully removed from codebase
- Theme toggles work with next-themes (unified theme system per DASH-03)
- Nav data types and configs available for Plan 02 components
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-01-SUMMARY.md`
</output>
