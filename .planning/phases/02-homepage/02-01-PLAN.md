---
phase: 2
plan: 01
type: component
wave: 1
depends_on: []
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/components/sections/FeaturesBento.tsx
  - src/app/globals.css
autonomous: true
requirements: [HOME-01, HOME-02, HOME-05]
---

## Objective

Create the Hero section and Features Bento Grid section as React components, porting from `woobooster-v2.html` lines 452-568. These are the first two sections of the homepage and cover requirements HOME-01 (hero with headline, subheadline, CTA) and HOME-02 (features preview with icons). HOME-05 (visual fidelity) applies to all sections.

---

## Tasks

### Task 1: Add CSS keyframes and section styles to globals.css

<read_first>
- `src/app/globals.css` — current state, existing keyframes, `@layer utilities` structure
- `woobooster-v2.html` lines 88-172 — hero, dashboard mock, trust bar, bento grid CSS
</read_first>

<action>
Add the following CSS keyframes and section-specific styles to `src/app/globals.css` after the existing `meshFloat` keyframe (line 168):

1. **`pulseDot` keyframe** — for animated dots in eyebrow badge and trust pills:
```css
@keyframes pulseDot {
  0%, 100% { transform: scale(1); opacity: 1; }
  50% { transform: scale(1.5); opacity: .4; }
}
```

2. **`underlineIn` keyframe** — for hero headline highlight underline:
```css
@keyframes underlineIn {
  to { transform: scaleX(1); }
}
```

3. **`mockFloat` keyframe** — for dashboard mockup floating animation:
```css
@keyframes mockFloat {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-8px); }
}
```

4. **`wordUp` keyframe** — for word-by-word reveal:
```css
@keyframes wordUp {
  to { opacity: 1; transform: none; }
}
```

5. **`ringPulse` keyframe** — for video section play button rings:
```css
@keyframes ringPulse {
  0% { transform: scale(.7); opacity: .8; }
  100% { transform: scale(1.4); opacity: 0; }
}
```

6. Add these CSS classes in the `@layer utilities` block:

**Hero-specific classes:**
```css
.hero-eyebrow {
  @apply inline-flex items-center gap-2 bg-accent-light text-accent text-[11.5px] font-bold py-1.5 px-3.5 rounded-full border border-[rgba(0,71,255,0.15)] mb-5 tracking-[0.4px];
}

.eyebrow-dot {
  @apply w-1.5 h-1.5 rounded-full bg-accent;
  animation: pulseDot 2s infinite;
}

.hero-sub {
  @apply text-base text-text2 leading-[1.8] mb-8 max-w-[490px];
}

.trust-pill {
  @apply flex items-center gap-2 text-[12.5px] font-semibold text-muted;
}

.trust-dot {
  @apply w-[5px] h-[5px] rounded-full bg-green;
}
```

**Dashboard mockup classes:**
```css
.dash-mock {
  @apply bg-surface border border-[--border2] rounded-[22px] shadow-lg overflow-hidden;
  animation: mockFloat 6s ease-in-out infinite;
}

.mock-top {
  @apply bg-surface-2 py-3 px-4 flex items-center gap-2 border-b border-[--border];
}

.mock-circles {
  @apply flex gap-1.5;
}

.mc {
  @apply w-[9px] h-[9px] rounded-full;
}

.mock-title-bar {
  @apply text-[10.5px] font-bold text-muted ml-2 font-mono;
}

.mock-body {
  @apply p-3.5;
}

.mstats {
  @apply grid grid-cols-3 gap-2 mb-2.5;
}

.ms {
  @apply bg-[--bg2] rounded-[10px] py-2.5 px-3 border border-[--border];
}

.ms-l {
  @apply text-[8.5px] font-bold text-muted uppercase tracking-[0.6px] mb-1;
}

.ms-v {
  @apply text-xl font-black tracking-[-1px];
}

.ms-v.g { @apply text-green; }
.ms-v.b { @apply text-accent; }
.ms-v.r { @apply text-red; }

.chart-box {
  @apply bg-[--bg2] rounded-[10px] p-3 border border-[--border] mb-2.5;
}

.chart-lbl {
  @apply text-[8.5px] font-bold text-text2 mb-2;
}

.chart-bars {
  @apply flex items-end gap-1.5 h-[52px];
}

.cb {
  @apply flex-1 rounded-t-[3px];
}

.cb.blue { @apply bg-accent; }
.cb.green { @apply bg-green; }
.cb.dim { @apply opacity-20; }

.order-list {
  @apply bg-[--bg2] rounded-[10px] border border-[--border] overflow-hidden;
}

.orow {
  @apply flex items-center justify-between py-[7px] px-3 border-b border-[--border] text-[9.5px];
}

.orow:last-child { @apply border-b-0; }

.oid {
  @apply font-mono font-bold text-foreground text-[9.5px];
}

.badge {
  @apply py-0.5 px-2 rounded-full text-[8px] font-extrabold;
}

.bd-ok { @apply bg-green-lt text-green; }
.bd-sh { @apply bg-accent-light text-accent; }
.bd-rt { @apply bg-red-lt text-red; }
.bd-pn { @apply bg-orange-lt text-orange; }
```

**Bento grid classes:**
```css
.bento {
  @apply grid grid-cols-3 gap-4;
}

.bc {
  @apply bg-surface border border-[--border] rounded-[14px] py-7 px-6 transition-all duration-[250ms] cursor-default;
}

.bc:hover {
  @apply border-accent shadow-lg;
}

.bc.w2 { @apply col-span-2; }

.bc-icon {
  @apply w-[46px] h-[46px] rounded-[13px] bg-accent-light border border-[--border] grid place-items-center text-2xl mb-5 transition-transform duration-300;
}

.bc:hover .bc-icon {
  @apply scale-112 -rotate-5;
}

.bc-title {
  @apply font-syne text-base font-extrabold text-foreground mb-2;
}

.bc-desc {
  @apply text-[13.5px] text-text2 leading-[1.65];
}

.tags {
  @apply flex flex-wrap gap-1.5 mt-3.5;
}

.tag {
  @apply text-[10.5px] font-bold py-[3px] px-2.5 rounded-full bg-[--bg2] text-muted border border-[--border];
}
```

**Section common classes:**
```css
.sec {
  @apply py-25;
}

.sec-bg { @apply bg-surface; }

.eyebrow {
  @apply text-[11px] font-extrabold tracking-[2px] uppercase text-accent mb-3;
}

.sec-title {
  @apply font-syne text-[clamp(26px,3.2vw,44px)] font-extrabold tracking-[-1.2px] text-foreground mb-4 leading-[1.1];
}

.sec-sub {
  @apply text-[15.5px] text-text2 leading-[1.8];
}

.sh { @apply mb-[60px]; }
.sh.center { @apply text-center; }
.sh.center .sec-sub { @apply max-w-[560px] mx-auto; }
```

**Scroll reveal classes:**
```css
.sr {
  @apply opacity-0 translate-y-8;
  transition: opacity 0.65s ease, transform 0.65s cubic-bezier(.22,1,.36,1);
}

.sr.vis {
  @apply opacity-100 translate-y-0;
}

.sr-d1 { transition-delay: .08s; }
.sr-d2 { transition-delay: .16s; }
.sr-d3 { transition-delay: .24s; }
.sr-d4 { transition-delay: .32s; }
```
</action>

<acceptance_criteria>
- `globals.css` contains `@keyframes pulseDot` with `scale(1)` to `scale(1.5)` values
- `globals.css` contains `@keyframes mockFloat` with `translateY(0)` to `translateY(-8px)` values
- `globals.css` contains `@keyframes wordUp` with `opacity: 1; transform: none` target
- `globals.css` contains `@keyframes ringPulse` with `scale(.7)` to `scale(1.4)` values
- `globals.css` contains `@keyframes underlineIn` with `scaleX(1)` target
- `globals.css` contains class `.dash-mock` with `animation: mockFloat 6s ease-in-out infinite`
- `globals.css` contains class `.bento` with `grid grid-cols-3`
- `globals.css` contains class `.bc.w2` with `col-span-2`
- `globals.css` contains `.bd-ok`, `.bd-sh`, `.bd-rt`, `.bd-pn` badge classes
- `pnpm build` succeeds after changes
</acceptance_criteria>

---

### Task 2: Create HeroSection client component

<read_first>
- `woobooster-v2.html` lines 452-506 — Hero section HTML template
- `woobooster-v2.html` lines 88-113 — Hero CSS styles
- `woobooster-v2.html` lines 1107-1143 — Word-by-word reveal JS animation logic
- `src/components/layout/Navbar.tsx` — Framer Motion patterns (motion.div, initial/animate/transition)
- `src/app/globals.css` — Updated state after Task 1
- `src/lib/utils.ts` — `cn()` function signature
</read_first>

<action>
Create `src/components/sections/HeroSection.tsx` as a **client component** (`"use client"`).

The component renders the full hero section from the HTML reference (lines 452-506). Structure:

1. **Outer section** — `<section className="hero min-h-screen bg-[--hero-g] flex items-center py-[110px] pb-20 relative overflow-hidden">`
   - Apply `.hero-mesh` class to a child div for gradient orbs
   - Apply a dots background div with `opacity-[.035]` and `bg-[radial-gradient(circle,var(--accent)_1.5px,transparent_1.5px)] bg-[length:40px_40px]`

2. **Inner container** — `<div className="max-w-[1160px] mx-auto px-7 relative z-10">`

3. **Two-column grid** — `<div className="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">`

4. **Left column** (text content):
   - Eyebrow badge: `<div className="hero-eyebrow">` with `<div className="eyebrow-dot" />` and text `v0.0.14 — Analytics Suite Live`
   - Headline: `<h1>` with `font-syne text-[clamp(34px,4.5vw,58px)] font-black leading-[1.06] tracking-[-2px] text-foreground mb-5`. Text: "Run Your WooCommerce Store on " + `<span className="hl">` with `text-accent relative inline-block` and `::after` underline animation via inline Tailwind/CSS. The word "Autopilot" gets the highlight.
   - Subheadline: `<p className="hero-sub">` with exact text from HTML reference (line 462)
   - CTA buttons: `<div className="flex gap-3 flex-wrap mb-9">`
     - `<Link href="/pricing" className="btn btn-primary btn-lg">Get WooBooster — ৳3,499</Link>`
     - `<Link href="/features" className="btn btn-outline btn-lg">See Features</Link>`
   - Trust pills: `<div className="flex flex-wrap gap-5">` with 3 trust pills matching HTML reference text

5. **Right column** (dashboard mockup) — hidden on `< lg` breakpoint (`hidden lg:block`):
   - `.dash-mock` div with `.mock-top` (colored circles + title bar), `.mock-body` (mstats grid, chart-bars, order-list)
   - Stats: Revenue ৳42L (green), Orders 834 (accent), Blocked 12 (red)
   - Chart bars: 7 bars with heights 38%, 52%, 46%, 68%, 60%, 82%, 100% — first 4 `dim`, last 3 not dim (5th green, 6th-7th blue)
   - Order rows: #8821 Steadfast Delivered, #8820 Pathao In Transit, #8819 RedX Returned, #8818 Steadfast Pending

6. **Framer Motion animations:**
   - Wrap the entire section in a `<motion.div>` (or use individual motion elements)
   - Eyebrow: `initial={{ opacity: 0, y: 16 }}`, `whileInView={{ opacity: 1, y: 0 }}`, `viewport={{ once: true }}`, `transition={{ duration: 0.5, ease: [0.22, 1, 0.36, 1] }}`
   - Headline: Same but with `transition.delay: 0.08`
   - Sub: delay 0.16
   - CTAs: delay 0.24
   - Trust pills: delay 0.32
   - Dashboard mockup: `initial={{ opacity: 0, y: 32 }}`, `whileInView={{ opacity: 1, y: 0 }}`, delay 0.16
   - Use `viewport={{ once: true }}` on all animated elements

Export as named function: `export function HeroSection()`

Import `Link` from `next/link`, `motion` from `framer-motion`.
</action>

<acceptance_criteria>
- File exists at `src/components/sections/HeroSection.tsx`
- First line is `"use client";`
- Component exports `export function HeroSection()`
- Contains text "Run Your WooCommerce Store on" and "Autopilot"
- Contains text "v0.0.14 — Analytics Suite Live" in eyebrow badge
- Contains subheadline text "Automated courier sync with Steadfast, Pathao & RedX"
- Contains Link to `/pricing` with text "Get WooBooster — ৳3,499"
- Contains Link to `/features` with text "See Features"
- Contains trust pills: "500+ Active Stores", "30-Day Money Back", "bKash & Nagad Ready"
- Dashboard mockup contains "woobooster-dashboard.php" title bar text
- Dashboard mockup contains order rows: #8821, #8820, #8819, #8818
- Dashboard mockup is hidden below `lg` breakpoint (`hidden lg:block` or equivalent)
- Uses `motion` from `framer-motion` for entrance animations
- Uses `whileInView` with `viewport={{ once: true }}`
- `pnpm build` succeeds
</acceptance_criteria>

---

### Task 3: Create FeaturesBento server component

<read_first>
- `woobooster-v2.html` lines 522-568 — Features bento grid HTML template
- `woobooster-v2.html` lines 163-172 — Bento grid CSS styles
- `src/components/sections/HeroSection.tsx` — Created in Task 2 (for pattern reference)
- `src/app/globals.css` — Updated state with bento classes from Task 1
</read_first>

<action>
Create `src/components/sections/FeaturesBento.tsx` as a **server component** (no `"use client"` directive).

The component renders the features bento grid from the HTML reference (lines 522-568). Structure:

1. **Section wrapper** — `<section className="sec">` with `<div className="max-w-[1160px] mx-auto px-7">`

2. **Section header** — `<div className="sh center">` containing:
   - Eyebrow: `<div className="eyebrow">Everything You Need</div>`
   - Title: `<div className="sec-title">6 Powerful Modules. One Plugin.</div>`
   - Subtitle: `<p className="sec-sub">WooBooster replaces 6 separate tools with one seamlessly integrated plugin built specifically for Bangladeshi WooCommerce stores.</p>`

3. **Bento grid** — `<div className="bento">` containing 6 `.bc` cards:

   **Card 1** (spans 2 columns — `.bc.w2`):
   - Icon: 🚿
   - Title: "Automated Courier Sync"
   - Desc: "Automatically polls Steadfast, Pathao, and RedX in the background and updates your WooCommerce order statuses in real-time. Zero manual tracking."
   - Tags: Steadfast, Pathao, RedX, Auto Poll, Status Mapping

   **Card 2**:
   - Icon: 📊
   - Title: "Advanced Analytics"
   - Desc: "Revenue trends, courier performance charts, and live-polling stats — all in one beautiful dashboard."
   - Tags: Live Data, Charts

   **Card 3**:
   - Icon: 🛡️
   - Title: "Fraud Shield"
   - Desc: "Block bad actors by phone, IP, or email. Velocity limits stop automated abuse before orders are placed."
   - Tags: Blacklist, IP Block

   **Card 4**:
   - Icon: 📈
   - Title: "Meta Pixel + CAPI"
   - Desc: "Hybrid browser + server-side tracking that survives iOS 14 and ad-blockers. 100% accurate conversions."
   - Tags: CAPI, iOS 14+, GA4

   **Card 5**:
   - Icon: 🛒
   - Title: "Lead Recovery"
   - Desc: "Capture checkout fields in real-time as users type. Convert incomplete orders into paying customers."
   - Tags: Leads, Abandoned Cart

   **Card 6**:
   - Icon: 💎
   - Title: "Premium UI System"
   - Desc: "Light & dark admin themes with glassmorphism, emerald accents, and micro-animations."
   - Tags: Dark Mode, Animations

4. Each card structure:
```tsx
<div className="bc w2">
  <div className="bc-icon">🚚</div>
  <div className="bc-title">Automated Courier Sync</div>
  <div className="bc-desc">...</div>
  <div className="tags">
    <span className="tag">Steadfast</span>
    ...
  </div>
</div>
```

Export as named function: `export function FeaturesBento()`

No imports needed (pure JSX server component).
</action>

<acceptance_criteria>
- File exists at `src/components/sections/FeaturesBento.tsx`
- No `"use client"` directive (server component)
- Component exports `export function FeaturesBento()`
- Contains exactly 6 `.bc` cards
- First card has `w2` class (spans 2 columns)
- Contains eyebrow text "Everything You Need"
- Contains title "6 Powerful Modules. One Plugin."
- Contains all 6 feature titles: "Automated Courier Sync", "Advanced Analytics", "Fraud Shield", "Meta Pixel + CAPI", "Lead Recovery", "Premium UI System"
- Each card has icon, title, description, and tags
- Card 1 has tags: Steadfast, Pathao, RedX, Auto Poll, Status Mapping
- `pnpm build` succeeds
</acceptance_criteria>

---

## Verification

1. Run `pnpm build` — must complete with zero errors
2. Run `pnpm lint` — must pass with zero errors
3. Verify `globals.css` has all 5 new keyframes (pulseDot, mockFloat, wordUp, ringPulse, underlineIn)
4. Verify `HeroSection.tsx` is a client component with Framer Motion animations
5. Verify `FeaturesBento.tsx` is a server component with no JS interactivity
6. Verify both components use `@/` imports and existing design tokens

## Must Haves

- Hero section displays with headline, subheadline, and CTA button (HOME-01)
- Features preview section shows 6 product capabilities with icons (HOME-02)
- Visual design matches `woobooster-v2.html` reference (HOME-05)
- All CSS keyframes and utility classes added to support these sections
- Build passes after all changes
