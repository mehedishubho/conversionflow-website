---
phase: 2
plan: 02
type: component
wave: 1
depends_on: []
files_modified:
  - src/components/sections/TrustBar.tsx
  - src/components/sections/VideoSection.tsx
  - src/components/sections/BDSection.tsx
  - src/components/sections/HowItWorks.tsx
  - src/components/sections/Testimonials.tsx
  - src/components/sections/CTASection.tsx
  - src/app/globals.css
autonomous: true
requirements: [HOME-03, HOME-04, HOME-05]
---

## Objective

Create the remaining 6 homepage section components: Trust Bar, Video Section, BD Section, How It Works, Testimonials, and CTA Banner. These cover HOME-03 (social proof — trust bar + testimonials), HOME-04 (CTA section), and HOME-05 (visual fidelity). All components are ported from `woobooster-v2.html` lines 509-683.

---

## Tasks

### Task 1: Add remaining section CSS classes to globals.css

<read_first>
- `src/app/globals.css` — current state after 02-01 plan (should have hero, bento, keyframes)
- `woobooster-v2.html` lines 145-150 (trust bar), 175-211 (video section), 243-263 (BD section / courier cards / status flow), 266-271 (how it works), 274-283 (testimonials), 316-323 (CTA banner)
</read_first>

<action>
Add the following CSS classes to `src/app/globals.css` inside the `@layer utilities` block:

**Trust Bar:**
```css
.trust-bar {
  @apply py-7 bg-surface border-t border-[--border] border-b border-[--border];
}

.trust-bar-inner {
  @apply flex items-center justify-center gap-0 flex-wrap;
}

.tstat {
  @apply text-center py-2 px-10;
}

.tstat + .tstat {
  @apply border-l border-[--border];
}

.tstat-n {
  @apply font-syne text-[26px] font-black text-foreground tracking-[-1px];
}

.tstat-l {
  @apply text-[11px] text-muted font-semibold mt-0.5;
}
```

**Video Section:**
```css
.video-sec {
  @apply py-[90px] bg-foreground relative overflow-hidden;
}

.dark .video-sec {
  background: #020812;
}

.video-sec::before {
  content: '';
  @apply absolute inset-0;
  background: radial-gradient(ellipse at 60% 50%, rgba(0,71,255,.12) 0%, transparent 65%);
}

.vs-label {
  @apply text-[11px] font-extrabold tracking-[2px] uppercase text-white/50 mb-3;
}

.vs-title {
  @apply font-syne text-[clamp(26px,3vw,42px)] font-black text-white tracking-[-1px] mb-3 leading-[1.1];
}

.vs-sub {
  @apply text-[15px] text-white/55 mb-12;
}

.video-player {
  @apply relative rounded-[20px] overflow-hidden border border-white/12 shadow-[0_24px_80px_rgba(0,0,0,0.6)] cursor-pointer transition-transform duration-300;
}

.video-player:hover {
  @apply scale-[1.01];
}

.video-thumb {
  @apply w-full aspect-video relative flex items-center justify-center overflow-hidden;
  background: linear-gradient(135deg, #0A1040 0%, #0D1650 40%, #091235 100%);
}

.vt-grid {
  @apply absolute inset-0 opacity-[.06];
  background-image: linear-gradient(rgba(77,138,255,1) 1px, transparent 1px), linear-gradient(90deg, rgba(77,138,255,1) 1px, transparent 1px);
  background-size: 50px 50px;
}

.vt-ui {
  @apply absolute inset-5 bg-white/[.04] rounded-xl border border-white/[.07] overflow-hidden;
}

.vt-ui-bar {
  @apply bg-white/[.06] h-8 flex items-center px-3.5 gap-2 border-b border-white/[.06];
}

.vt-ui-dot {
  @apply w-[7px] h-[7px] rounded-full;
}

.vt-ui-body {
  @apply p-3.5 grid grid-cols-3 gap-2;
}

.vt-card {
  @apply bg-white/[.05] border border-white/[.07] rounded-lg p-2.5 aspect-square;
}

.vt-card-num {
  @apply font-syne text-lg font-black text-accent opacity-80;
}

.vt-card-lbl {
  @apply text-[8px] text-white/30 mt-1 font-semibold;
}

.play-btn {
  @apply relative z-10 w-20 h-20 grid place-items-center;
}

.play-inner {
  @apply w-16 h-16 rounded-full bg-white/95 grid place-items-center relative z-10 transition-all duration-250;
}

.play-inner:hover {
  @apply scale-110 shadow-[0_0_0_8px_rgba(255,255,255,0.2)];
}

.play-inner::after {
  content: '▶';
  @apply text-[22px] text-accent ml-[3px];
}

.play-ring {
  @apply absolute rounded-full border-2 border-white/30;
  animation: ringPulse 2.5s ease-out infinite;
}

.play-ring:nth-child(1) { @apply w-20 h-20; animation-delay: 0s; }
.play-ring:nth-child(2) { @apply w-[100px] h-[100px]; animation-delay: .6s; }
.play-ring:nth-child(3) { @apply w-[124px] h-[124px]; animation-delay: 1.2s; }

.video-caption {
  @apply text-center mt-5 text-[13.5px] text-white/40 font-medium;
}
```

**BD Section — Courier Cards & Status Flow:**
```css
.cc {
  @apply bg-surface border border-[--border] rounded-[13px] py-4 px-5 flex items-center justify-between transition-all duration-250;
}

.cc:hover {
  @apply border-accent translate-x-1;
}

.cc-l {
  @apply flex items-center gap-3;
}

.cc-icon {
  @apply w-10 h-10 rounded-[11px] bg-accent-light grid place-items-center text-xl;
}

.cc-name {
  @apply text-sm font-extrabold text-foreground font-syne;
}

.cc-sub {
  @apply text-[11px] text-muted mt-0.5;
}

.live-chip {
  @apply flex items-center gap-1.5 text-[10.5px] font-extrabold text-green bg-green-lt py-1 px-2.5 rounded-full border border-green;
}

.live-d {
  @apply w-[5px] h-[5px] rounded-full bg-green;
  animation: pulseDot 1.5s infinite;
}

.bd-pay-note {
  @apply py-4 px-5 bg-accent-light border border-accent rounded-xl text-center;
}

.bd-pay-note strong {
  @apply block text-[11px] text-accent uppercase tracking-[1.2px] font-extrabold mb-1;
}

.bd-pay-note p {
  @apply text-[12px] text-text2;
}

.flow {
  @apply flex items-center gap-2 flex-wrap my-5;
}

.sn {
  @apply py-[7px] px-4 rounded-full text-[11.5px] font-extrabold border-2;
}

.sn-p { @apply bg-orange-lt text-orange border-orange; }
.sn-s { @apply bg-accent-light text-accent border-accent; }
.sn-d { @apply bg-green-lt text-green border-green; }
.sn-r { @apply bg-red-lt text-red border-red; }

.arrow-ch {
  @apply text-muted font-bold;
}

.checks {
  @apply list-none flex flex-col gap-3 mt-5;
}

.checks li {
  @apply flex items-start gap-2.5 text-sm text-text2 leading-[1.55];
}

.ck {
  @apply w-5 h-5 rounded-full bg-green-lt text-green grid place-items-center text-[10px] font-black flex-shrink-0 mt-0.5;
}
```

**How It Works:**
```css
.steps-grid {
  @apply grid grid-cols-1 lg:grid-cols-3 gap-5;
}

.step-card {
  @apply bg-surface border border-[--border] rounded-[14px] py-8 px-6 relative transition-all duration-[250ms];
}

.step-card:hover {
  @apply border-accent -translate-y-1 shadow-lg;
}

.step-n {
  @apply font-syne text-[56px] font-black text-accent opacity-10 leading-none mb-2.5;
}

.step-t {
  @apply font-syne text-[17px] font-extrabold text-foreground mb-2;
}

.step-d {
  @apply text-[13.5px] text-text2 leading-[1.7];
}
```

**Testimonials:**
```css
.tgrid {
  @apply grid grid-cols-1 lg:grid-cols-3 gap-5;
}

.tcard {
  @apply bg-surface border border-[--border] rounded-[14px] py-7 px-7 transition-all duration-250 relative overflow-hidden;
}

.tcard::before {
  content: '❝';
  @apply absolute top-4 right-5 text-[52px] text-accent opacity-[.06] font-syne leading-none;
}

.tcard:hover {
  @apply border-accent -translate-y-[3px] shadow;
}

.stars {
  @apply text-[#F59E0B] text-[13px] mb-3.5;
}

.tquote {
  @apply text-[13.5px] text-text2 leading-[1.8] mb-5;
}

.tauthor {
  @apply flex items-center gap-2.5;
}

.tav {
  @apply w-[38px] h-[38px] rounded-full grid place-items-center text-sm font-black text-white;
}

.tname {
  @apply font-syne text-[13.5px] font-extrabold text-foreground;
}

.tstore {
  @apply text-[11.5px] text-muted;
}
```

**CTA Banner:**
```css
.cta-wrap {
  @apply bg-accent rounded-[22px] py-20 px-[60px] text-center relative overflow-hidden;
}

.cta-wrap::before {
  content: '';
  @apply absolute inset-0;
  background: radial-gradient(ellipse at top, rgba(255,255,255,.1) 0%, transparent 60%);
}

.cta-wrap h2 {
  @apply font-syne text-[clamp(28px,3.5vw,46px)] font-black text-white tracking-[-1.3px] mb-3.5 leading-[1.1];
}

.cta-wrap p {
  @apply text-base text-white/75 mb-9;
}

.cta-bd-tag {
  @apply inline-flex items-center gap-2 bg-white/15 text-white text-[11px] font-extrabold py-1.5 px-3.5 rounded-full border border-white/20 mb-5 tracking-[0.5px];
}

.cta-note {
  @apply mt-4 text-[12px] text-white/50 font-medium;
}
```

**BD section layout:**
```css
.bd-layout {
  @apply grid grid-cols-1 lg:grid-cols-2 gap-[60px] items-start;
}
```

**Responsive overrides** (add after existing styles):
```css
@media(max-width:960px) {
  .bento { @apply grid-cols-2; }
  .bc.w2 { @apply col-span-2; }
  .bd-layout { @apply grid-cols-1 gap-10; }
}

@media(max-width:640px) {
  .sec { @apply py-[72px]; }
  .bento { @apply grid-cols-1; }
  .bc.w2 { @apply col-span-1; }
  .trust-bar-inner { @apply gap-4; }
  .tstat { @apply py-1.5 px-[18px]; }
  .cta-wrap { @apply py-12 px-7; }
}
```
</action>

<acceptance_criteria>
- `globals.css` contains `.trust-bar` class with `bg-surface`
- `globals.css` contains `.video-sec` class with `bg-foreground`
- `globals.css` contains `.play-ring` with `ringPulse` animation
- `globals.css` contains `.cc` (courier card) class with hover `translate-x-1`
- `globals.css` contains `.live-chip` class with `text-green bg-green-lt`
- `globals.css` contains `.sn-p`, `.sn-s`, `.sn-d`, `.sn-r` status node classes
- `globals.css` contains `.step-card` with hover `-translate-y-1`
- `globals.css` contains `.tcard` with `::before` pseudo-element containing `❝`
- `globals.css` contains `.cta-wrap` with `bg-accent`
- `globals.css` contains responsive overrides at 960px and 640px for bento, steps, tgrid, trust-bar
- `pnpm build` succeeds
</acceptance_criteria>

---

### Task 2: Create TrustBar client component

<read_first>
- `woobooster-v2.html` lines 509-519 — Trust bar HTML template
- `src/components/layout/Navbar.tsx` — Framer Motion `whileInView` pattern reference
- `src/app/globals.css` — Updated with trust bar classes
</read_first>

<action>
Create `src/components/sections/TrustBar.tsx` as a **client component** (`"use client"`).

The component renders the trust bar from the HTML reference (lines 509-519). Structure:

1. **Wrapper** — `<div className="trust-bar">` with `<div className="max-w-[1160px] mx-auto px-7">`
2. **Inner** — `<div className="trust-bar-inner">` containing 5 `.tstat` items:

   | Number | Label | Animation |
   |--------|-------|-----------|
   | 500+ | Active Stores | Count-up from 0 to 500 (1800ms, IntersectionObserver-triggered) |
   | 3 | BD Couriers | Static |
   | 6 | Tracking Platforms | Static |
   | 100% | CAPI Accuracy | Static |
   | ৳৳৳ | BDT Pricing | Static |

3. **Count-up animation** for the first stat:
   - Use `useRef` + `useEffect` + `IntersectionObserver` (matching the HTML reference's `requestAnimationFrame` approach)
   - When the element enters viewport, animate from 0 to 500 over 1800ms using `requestAnimationFrame`
   - Display as "500+" (append "+" after reaching target)
   - Use `useInView` from framer-motion or raw IntersectionObserver
   - After animation completes, unobserve (matching HTML's `unobserve` behavior)

4. Wrap each stat in a `motion.div` with fade-up entrance:
   - `initial={{ opacity: 0, y: 16 }}`
   - `whileInView={{ opacity: 1, y: 0 }}`
   - `viewport={{ once: true }}`
   - Staggered delays: 0, 0.08, 0.16, 0.24, 0.32

Export as named function: `export function TrustBar()`

Imports: `useState`, `useEffect`, `useRef` from `react`, `motion` from `framer-motion`.
</action>

<acceptance_criteria>
- File exists at `src/components/sections/TrustBar.tsx`
- First line is `"use client";`
- Component exports `export function TrustBar()`
- Contains 5 stat items with correct labels: "Active Stores", "BD Couriers", "Tracking Platforms", "CAPI Accuracy", "BDT Pricing"
- First stat displays count-up animation from 0 to 500+
- Count-up triggers on viewport intersection (not on mount)
- Other 4 stats are static (no animation)
- Uses `motion` from `framer-motion` for entrance animations
- `pnpm build` succeeds
</acceptance_criteria>

---

### Task 3: Create VideoSection server component

<read_first>
- `woobooster-v2.html` lines 571-603 — Video section HTML template
- `woobooster-v2.html` lines 175-211 — Video section CSS
- `src/app/globals.css` — Updated with video section classes
</read_first>

<action>
Create `src/components/sections/VideoSection.tsx` as a **server component** (no `"use client"`).

The component renders the video section from the HTML reference (lines 571-603). Structure:

1. **Wrapper** — `<div className="video-sec">` with `<div className="max-w-[1160px] mx-auto px-7">`

2. **Header** — centered text block:
   - Label: `<div className="vs-label">Watch it in action</div>`
   - Title: `<div className="vs-title">See WooBooster Transform Your Store</div>`
   - Sub: `<div className="vs-sub">A 3-minute overview of every module — no fluff, just results.</div>`

3. **Video player** — `<div className="video-player">` containing:
   - `.video-thumb` with:
     - `.vt-grid` — grid overlay
     - `.vt-ui` — UI mockup with `.vt-ui-bar` (3 colored dots: #FF5F57, #FFBD2E, #28C840) and `.vt-ui-body` (3 cards: 834 Orders Synced, ৳4.2L Revenue, 12 Fraud Blocked)
     - `.play-btn` with 3 `.play-ring` divs + `.play-inner` div

4. **Caption** — `<div className="video-caption">▶ WooBooster Full Overview — 3 min · See how 500+ BD stores use it</div>`

No onClick handler (decorative only per D-14). No Framer Motion needed (CSS-only animations).

Export as named function: `export function VideoSection()`

No imports needed.
</action>

<acceptance_criteria>
- File exists at `src/components/sections/VideoSection.tsx`
- No `"use client"` directive
- Component exports `export function VideoSection()`
- Contains text "Watch it in action"
- Contains text "See WooBooster Transform Your Store"
- Contains play button with 3 `.play-ring` elements
- Contains video UI mockup with 3 stat cards (834, ৳4.2L, 12)
- Contains caption text "WooBooster Full Overview"
- No onClick or JavaScript event handlers
- `pnpm build` succeeds
</acceptance_criteria>

---

### Task 4: Create BDSection server component

<read_first>
- `woobooster-v2.html` lines 606-639 — BD section HTML template
- `woobooster-v2.html` lines 243-263 — Courier cards, status flow, BD section CSS
- `src/app/globals.css` — Updated with BD section classes
</read_first>

<action>
Create `src/components/sections/BDSection.tsx` as a **server component** (no `"use client"`).

The component renders the "Built for Bangladesh" section from the HTML reference (lines 606-639). Structure:

1. **Wrapper** — `<section className="sec sec-bg">` with `<div className="max-w-[1160px] mx-auto px-7">`

2. **Section header** — `<div className="sh">`:
   - Eyebrow: `<div className="eyebrow">🇧🇩 Built for Bangladesh</div>`
   - Title: `<div className="sec-title">The Only WooCommerce Plugin<br/>Built for BD Sellers</div>`
   - Sub: `<p className="sec-sub">Deep native integration with Bangladesh's top 3 courier services. Auto-synced, auto-updated — no manual dashboard checking ever again.</p>`

3. **Two-column layout** — `<div className="bd-layout">`:

   **Left column:**
   - Heading: `<h3>` with `font-syne text-[22px] font-black text-foreground tracking-[-0.5px] mb-2.5` — "Automated Order Flow"
   - Description: `<p>` with `text-sm text-text2 leading-[1.8] mb-5` — "Stop checking courier dashboards manually 10 times a day. WooBooster polls all three in the background and updates WooCommerce automatically."
   - Status flow: `<div className="flow">` with 4 `.sn` badges (Pending → Shipped → Delivered / Returned) and `.arrow-ch` separators
   - Check list: `<ul className="checks">` with 5 items matching HTML reference text exactly

   **Right column** — `<div className="courier-cards">`:
   - 3 courier cards (`.cc`):
     - Steadfast: 📦 icon, "834 orders synced today", Live badge
     - Pathao: 🛵 icon, "421 orders synced today", Live badge
     - RedX: 🔴 icon, "198 orders synced today", Live badge
   - Payment note: `.bd-pay-note` with "🏦 bKash · Nagad · Bank Transfer" and "Pay in BDT. Invoices in Taka. Dedicated BD support line."

Each courier card structure:
```tsx
<div className="cc">
  <div className="cc-l">
    <div className="cc-icon">📦</div>
    <div>
      <div className="cc-name">Steadfast Courier</div>
      <div className="cc-sub">834 orders synced today</div>
    </div>
  </div>
  <div className="live-chip"><div className="live-d" />Live</div>
</div>
```

Export as named function: `export function BDSection()`

No imports needed.
</action>

<acceptance_criteria>
- File exists at `src/components/sections/BDSection.tsx`
- No `"use client"` directive
- Component exports `export function BDSection()`
- Contains text "Built for Bangladesh"
- Contains text "The Only WooCommerce Plugin"
- Contains status flow with "Pending", "Shipped", "Delivered", "Returned"
- Contains 3 courier cards: "Steadfast Courier", "Pathao Courier", "RedX Courier"
- Each courier card has "Live" badge
- Contains 5 checkmark items
- Contains payment note with "bKash · Nagad · Bank Transfer"
- `pnpm build` succeeds
</acceptance_criteria>

---

### Task 5: Create HowItWorks server component

<read_first>
- `woobooster-v2.html` lines 642-655 — How It Works HTML template
- `woobooster-v2.html` lines 266-271 — Steps grid CSS
- `src/app/globals.css` — Updated with step-card classes
</read_first>

<action>
Create `src/components/sections/HowItWorks.tsx` as a **server component** (no `"use client"`).

The component renders the "How It Works" section from the HTML reference (lines 642-655). Structure:

1. **Wrapper** — `<section className="sec">` with `<div className="max-w-[1160px] mx-auto px-7">`

2. **Section header** — `<div className="sh center">`:
   - Eyebrow: `<div className="eyebrow">Getting Started</div>`
   - Title: `<div className="sec-title">Up and Running in 10 Minutes</div>`
   - Sub: `<p className="sec-sub">No developer needed. Install, activate, and WooBooster handles everything automatically.</p>`

3. **Steps grid** — `<div className="steps-grid">` with 3 `.step-card` items:

   **Step 1:**
   - Number: "01"
   - Title: "Install & Activate License"
   - Desc: "Upload the plugin, activate, enter your license key from the purchase email. Done in under 2 minutes."

   **Step 2:**
   - Number: "02"
   - Title: "Connect Couriers & Pixels"
   - Desc: "Paste your Steadfast / Pathao / RedX API keys and Meta Pixel ID + CAPI token. Simple copy-paste."

   **Step 3:**
   - Number: "03"
   - Title: "Watch It Run on Autopilot"
   - Desc: "WooBooster polls couriers, updates statuses, fires tracking events, and blocks fraud — 24/7, automatically."

Each step card structure:
```tsx
<div className="step-card">
  <div className="step-n">01</div>
  <div className="step-t">Install & Activate License</div>
  <div className="step-d">Upload the plugin, activate, enter your license key from the purchase email. Done in under 2 minutes.</div>
</div>
```

Export as named function: `export function HowItWorks()`

No imports needed.
</action>

<acceptance_criteria>
- File exists at `src/components/sections/HowItWorks.tsx`
- No `"use client"` directive
- Component exports `export function HowItWorks()`
- Contains text "Getting Started"
- Contains text "Up and Running in 10 Minutes"
- Contains 3 step cards with numbers "01", "02", "03"
- Step titles: "Install & Activate License", "Connect Couriers & Pixels", "Watch It Run on Autopilot"
- `pnpm build` succeeds
</acceptance_criteria>

---

### Task 6: Create Testimonials server component

<read_first>
- `woobooster-v2.html` lines 658-670 — Testimonials HTML template
- `woobooster-v2.html` lines 274-283 — Testimonial card CSS
- `src/app/globals.css` — Updated with tcard classes
</read_first>

<action>
Create `src/components/sections/Testimonials.tsx` as a **server component** (no `"use client"`).

The component renders the testimonials section from the HTML reference (lines 658-670). Structure:

1. **Wrapper** — `<section className="sec sec-bg">` with `<div className="max-w-[1160px] mx-auto px-7">`

2. **Section header** — `<div className="sh center">`:
   - Eyebrow: `<div className="eyebrow">Real Store Owners</div>`
   - Title: `<div className="sec-title">Loved by BD WooCommerce Sellers</div>`

3. **Testimonial grid** — `<div className="tgrid">` with 3 `.tcard` items:

   **Card 1:**
   - Stars: ★★★★★
   - Quote: "আমাদের প্রতিদিন ৩০০+ অর্ডার আসে। Steadfast আর Pathao manually চেক করা অনেক সময় নষ্ট করতো। WooBooster সব auto করে দিচ্ছে।"
   - Avatar: "RA", background `bg-accent`
   - Name: "Rahim Ahmed"
   - Store: "StyleBD.com · Dhaka"

   **Card 2:**
   - Stars: ★★★★★
   - Quote: "The Fraud Shield alone saved us ৳25,000 last month. Blacklisted a repeat scammer's number — haven't seen a fake order since."
   - Avatar: "TH", background `bg-green`
   - Name: "Tanvir Hossain"
   - Store: "TechGear.com.bd · Chittagong"

   **Card 3:**
   - Stars: ★★★★★
   - Quote: "Meta CAPI changed our ad results completely. ROAS improved 40% — the algorithm finally had proper delivery and return data."
   - Avatar: "SK", background `bg-orange`
   - Name: "Sumaiya Khan"
   - Store: "HomeDecorBD · Sylhet"

Each card structure:
```tsx
<div className="tcard">
  <div className="stars">★★★★★</div>
  <div className="tquote">"..."</div>
  <div className="tauthor">
    <div className="tav bg-accent">RA</div>
    <div>
      <div className="tname">Rahim Ahmed</div>
      <div className="tstore">StyleBD.com · Dhaka</div>
    </div>
  </div>
</div>
```

Export as named function: `export function Testimonials()`

No imports needed.
</action>

<acceptance_criteria>
- File exists at `src/components/sections/Testimonials.tsx`
- No `"use client"` directive
- Component exports `export function Testimonials()`
- Contains text "Real Store Owners"
- Contains text "Loved by BD WooCommerce Sellers"
- Contains 3 testimonial cards with star ratings
- Card 1 contains Bangla text "আমাদের প্রতিদিন ৩০০+"
- Card 2 contains text "Fraud Shield alone saved us"
- Card 3 contains text "ROAS improved 40%"
- Author initials: "RA", "TH", "SK"
- `pnpm build` succeeds
</acceptance_criteria>

---

### Task 7: Create CTASection server component

<read_first>
- `woobooster-v2.html` lines 673-683 — CTA banner HTML template
- `woobooster-v2.html` lines 316-323 — CTA banner CSS
- `src/app/globals.css` — Updated with cta-wrap classes
</read_first>

<action>
Create `src/components/sections/CTASection.tsx` as a **server component** (no `"use client"`).

The component renders the CTA banner from the HTML reference (lines 673-683). Structure:

1. **Wrapper** — `<section className="sec">` with `<div className="max-w-[1160px] mx-auto px-7">`

2. **CTA card** — `<div className="cta-wrap">` containing:
   - BD tag: `<div className="cta-bd-tag">🇧🇩 Made for Bangladeshi Sellers</div>`
   - Headline: `<h2>Start Automating Your Store Today</h2>`
   - Sub: `<p>Join 500+ WooCommerce store owners who save hours every week with WooBooster.</p>`
   - Button: `<Link href="/pricing" className="btn-white">Get WooBooster Now — ৳3,499 →</Link>`
   - Note: `<div className="cta-note">One-time payment · 30-day refund · bKash / Nagad · Instant delivery</div>`

Import `Link` from `next/link`.

Export as named function: `export function CTASection()`
</action>

<acceptance_criteria>
- File exists at `src/components/sections/CTASection.tsx`
- No `"use client"` directive
- Component exports `export function CTASection()`
- Contains text "Made for Bangladeshi Sellers"
- Contains text "Start Automating Your Store Today"
- Contains Link to `/pricing` with text "Get WooBooster Now — ৳3,499 →"
- Contains note text "One-time payment · 30-day refund"
- Uses `Link` from `next/link`
- `pnpm build` succeeds
</acceptance_criteria>

---

## Verification

1. Run `pnpm build` — must complete with zero errors
2. Run `pnpm lint` — must pass with zero errors
3. Verify all 6 new component files exist in `src/components/sections/`
4. Verify `TrustBar.tsx` is the only client component (others are server components)
5. Verify all CSS classes from the HTML reference are present in `globals.css`
6. Verify responsive overrides exist at 960px and 640px breakpoints

## Must Haves

- Social proof section displays trust signals (testimonials, stats) (HOME-03)
- Call-to-action section at bottom with conversion-focused messaging (HOME-04)
- All sections match visual design from `woobooster-v2.html` (HOME-05)
- All section CSS classes available for rendering
- Build passes after all changes
