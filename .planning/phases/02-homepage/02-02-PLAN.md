---
phase: 02-homepage
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/dashboard/AppSidebar.tsx
  - src/components/dashboard/AppHeader.tsx
  - src/components/dashboard/Backdrop.tsx
  - src/components/dashboard/DashboardShell.tsx
  - src/components/dashboard/UserDropdown.tsx
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "Sidebar renders nav items from the navItems prop with correct icons and labels"
    - "Sidebar is 290px when expanded, 90px when collapsed, hidden on mobile with slide-in drawer"
    - "Hovering the collapsed sidebar expands it to full width temporarily"
    - "Header shows hamburger toggle, theme toggle, notification bell icon, and user dropdown"
    - "User dropdown shows avatar initials, name, role badge, and Profile/Settings/Sign Out options"
    - "Mobile backdrop overlay appears when sidebar is open on small screens"
    - "DashboardShell composes all pieces: SidebarProvider + AppSidebar + Backdrop + AppHeader + children"
  artifacts:
    - path: "src/components/dashboard/AppSidebar.tsx"
      provides: "Sidebar with 3 states (expanded/collapsed/mobile), nav items from prop, hover flyout"
      exports: ["AppSidebar"]
      min_lines: 80
    - path: "src/components/dashboard/AppHeader.tsx"
      provides: "Header with toggle, theme toggle, bell, user dropdown"
      exports: ["AppHeader"]
      min_lines: 60
    - path: "src/components/dashboard/Backdrop.tsx"
      provides: "Mobile overlay"
      exports: ["Backdrop"]
      min_lines: 10
    - path: "src/components/dashboard/DashboardShell.tsx"
      provides: "Shared layout wrapper composing sidebar + header + backdrop"
      exports: ["DashboardShell"]
      min_lines: 40
    - path: "src/components/dashboard/UserDropdown.tsx"
      provides: "User dropdown with session data and sign out"
      exports: ["UserDropdown"]
      min_lines: 50
  key_links:
    - from: "src/components/dashboard/DashboardShell.tsx"
      to: "src/context/SidebarContext.tsx"
      via: "SidebarProvider and useSidebar import"
      pattern: "import.*SidebarProvider.*from.*@/context/SidebarContext"
    - from: "src/components/dashboard/AppSidebar.tsx"
      to: "src/data/dashboard-nav.ts"
      via: "NavItem type import"
      pattern: "import.*NavItem.*from.*@/data/dashboard-nav"
    - from: "src/components/dashboard/UserDropdown.tsx"
      to: "src/lib/auth-client.ts"
      via: "useSession and signOut imports"
      pattern: "import.*useSession.*from.*@/lib/auth-client"
    - from: "src/components/dashboard/AppHeader.tsx"
      to: "src/components/dashboard/UserDropdown.tsx"
      via: "UserDropdown component import"
      pattern: "import.*UserDropdown"
---

<objective>
Create all dashboard shell components: AppSidebar, AppHeader, Backdrop, UserDropdown, and the DashboardShell wrapper.

Purpose: These are the core layout components that compose into the dashboard experience. They accept a navItems prop making them reusable for both customer portal and admin dashboard (per D-09). They depend on the nav data types from Plan 01.

Output: Five new components in src/components/dashboard/ that form the complete dashboard shell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-homepage/02-CONTEXT.md
@.planning/phases/02-homepage/02-RESEARCH.md
@.planning/phases/02-homepage/02-01-SUMMARY.md

@backenddashboard/src/layout/AppSidebar.tsx
@backenddashboard/src/layout/AppHeader.tsx
@backenddashboard/src/layout/Backdrop.tsx
@backenddashboard/src/app/(admin)/layout.tsx
@src/context/SidebarContext.tsx
@src/styles/dashboard.css
@src/lib/auth-client.ts
@src/lib/utils.ts
@src/components/ui/dropdown/Dropdown.tsx
@src/components/ui/dropdown/DropdownItem.tsx
@src/components/auth/LogoutButton.tsx

<interfaces>
<!-- Types from Plan 01 that these components consume -->

From src/data/dashboard-nav.ts:
```typescript
import type { LucideIcon } from "lucide-react";

export type NavItem = {
  name: string;
  icon: LucideIcon;
  path: string;
  subItems?: { name: string; path: string }[];
};

export const customerNavItems: NavItem[];
export const adminNavItems: NavItem[];
```

From src/context/SidebarContext.tsx:
```typescript
type SidebarContextType = {
  isExpanded: boolean;
  isMobileOpen: boolean;
  isHovered: boolean;
  activeItem: string | null;
  openSubmenu: string | null;
  toggleSidebar: () => void;
  toggleMobileSidebar: () => void;
  setIsHovered: (isHovered: boolean) => void;
  setActiveItem: (item: string | null) => void;
  toggleSubmenu: (item: string) => void;
};
export const useSidebar: () => SidebarContextType;
export const SidebarProvider: React.FC<{ children: React.ReactNode }>;
```

From src/lib/auth-client.ts:
```typescript
export const { useSession, signOut } = authClient;
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Backdrop, AppSidebar, and UserDropdown components</name>
  <files>src/components/dashboard/Backdrop.tsx, src/components/dashboard/AppSidebar.tsx, src/components/dashboard/UserDropdown.tsx</files>
  <read_first>
    - backenddashboard/src/layout/Backdrop.tsx (direct port reference)
    - backenddashboard/src/layout/AppSidebar.tsx (sidebar structure reference)
    - src/context/SidebarContext.tsx (context API these components consume)
    - src/data/dashboard-nav.ts (nav config types -- created in Plan 01)
    - src/components/ui/dropdown/Dropdown.tsx (existing dropdown component to reuse)
    - src/components/ui/dropdown/DropdownItem.tsx (existing dropdown item)
    - src/components/auth/LogoutButton.tsx (signOut pattern reference)
    - src/lib/auth-client.ts (useSession and signOut exports)
    - src/styles/dashboard.css (design token class names)
    - src/lib/utils.ts (cn utility)
  </read_first>
  <acceptance_criteria>
    - File src/components/dashboard/Backdrop.tsx exists with "use client" directive
    - File src/components/dashboard/AppSidebar.tsx exists with "use client" directive
    - File src/components/dashboard/UserDropdown.tsx exists with "use client" directive
    - AppSidebar accepts `{ navItems: NavItem[] }` prop
    - AppSidebar imports useSidebar from @/context/SidebarContext
    - AppSidebar imports NavItem type from @/data/dashboard-nav
    - AppSidebar imports from lucide-react (MoreHorizontal for section headers)
    - AppSidebar uses cn() from @/lib/utils
    - AppSidebar has className with "w-[290px]" and "w-[90px]" for expanded/collapsed states
    - AppSidebar has onMouseEnter/onMouseLeave for hover flyout (per D-05)
    - AppSidebar has lg:translate-x-0 and conditional -translate-x-full for mobile (per D-01)
    - AppSidebar renders nav items with {nav.icon} component, not inline SVGs
    - AppSidebar shows "ConversionFlow" text when expanded and "CF" when collapsed
    - AppSidebar does NOT render SidebarWidget (per D-03)
    - Backdrop renders conditional overlay with z-40 and lg:hidden
    - Backdrop calls toggleMobileSidebar on click
    - UserDropdown imports useSession and signOut from @/lib/auth-client
    - UserDropdown imports Dropdown from @/components/ui/dropdown/Dropdown
    - UserDropdown imports DropdownItem from @/components/ui/dropdown/DropdownItem
    - UserDropdown shows user initials circle when no image available
    - UserDropdown has Sign Out option that calls signOut()
    - UserDropdown has Profile and Settings options (per D-07)
    - UserDropdown shows role badge (per D-07)
    - All three files use dashboard.css tokens (text-text2, bg-surface, border-border, etc.)
  </acceptance_criteria>
  <action>
    Create three components in src/components/dashboard/. All are "use client" components.

    **Backdrop.tsx** -- Direct port from backenddashboard, adapted to dashboard.css tokens:
    - Import `useSidebar` from `@/context/SidebarContext`
    - Named export `Backdrop`
    - If `!isMobileOpen` return null
    - Render `<div className="fixed inset-0 z-40 bg-black/50 lg:hidden" onClick={toggleMobileSidebar} />`
    - No changes needed from backenddashboard pattern -- this is straightforward

    **AppSidebar.tsx** -- Port from backenddashboard/src/layout/AppSidebar.tsx, adapted per decisions:
    - Import `useSidebar` from `@/context/SidebarContext`
    - Import `usePathname` from `next/navigation`
    - Import `Link` from `next/link`
    - Import `NavItem` type from `@/data/dashboard-nav`
    - Import `MoreHorizontal` from `lucide-react` (for collapsed section headers)
    - Import `cn` from `@/lib/utils`
    - Named export: `export function AppSidebar({ navItems }: { navItems: NavItem[] })`
    - The component accepts navItems as a prop (per D-09 -- parameterized by nav config)

    Sidebar structure:
    - `<aside>` with fixed positioning, `flex-col`, `h-screen`, `z-50`, `transition-all duration-300 ease-in-out`
    - Background: `bg-surface`, border: `border-r border-border`
    - Width logic: `showText ? "w-[290px]" : "w-[90px]"` where `showText = isExpanded || isHovered || isMobileOpen` (per D-02)
    - Mobile: `isMobileOpen ? "translate-x-0" : "-translate-x-full"` + `"lg:translate-x-0"` (per D-01)
    - Hover flyout: `onMouseEnter={() => !isExpanded && setIsHovered(true)}` and `onMouseLeave={() => setIsHovered(false)}` (per D-05)
    - mt-16 on mobile, lg:mt-0 on desktop (matching backenddashboard)

    Logo area:
    - `<div className={cn("py-8 flex", !showText ? "lg:justify-center" : "justify-start")}>`
    - Link to "/" with "ConversionFlow" text (font-syne font-800 text-lg) when showText, "CF" when collapsed

    Nav section:
    - Section header `<h2>` with "Menu" text when showText, `<MoreHorizontal />` icon when collapsed
    - Text: "text-xs uppercase text-muted"
    - `<ul className="flex flex-col gap-4">` with nav items mapped
    - Each nav item: `<Link>` with `nav.icon` component (`<nav.icon className="w-5 h-5 flex-shrink-0" />`), label shown only when showText
    - Active state: `pathname === nav.path ? "bg-accent-light text-accent" : "text-text2 hover:bg-accent-light hover:text-foreground"`
    - Link classes: `"flex items-center gap-3 px-3 py-2 rounded-lg transition-colors"`
    - No SidebarWidget (per D-03)
    - No submenus in Phase 2 (per D-10/D-11 -- flat navs)
    - Overflow: `overflow-y-auto duration-300 ease-linear no-scrollbar` on the scrollable area

    **UserDropdown.tsx** -- New component using session data (per D-07):
    - Import `useSession`, `signOut` from `@/lib/auth-client`
    - Import `Dropdown` from `@/components/ui/dropdown/Dropdown`
    - Import `DropdownItem` from `@/components/ui/dropdown/DropdownItem`
    - Import `User`, `Settings`, `LogOut, ChevronDown` from `lucide-react`
    - Import `useRouter` from `next/navigation`
    - Named export `UserDropdown`
    - Uses `useState` for isOpen toggle

    Structure:
    - `<div className="relative">` wrapper
    - Toggle button with: avatar circle (show initials from user.name if no user.image, show Image if user.image exists), user name, role badge, chevron icon
    - Loading state: if `!session?.user` show skeleton/placeholder (per Pitfall 4 in RESEARCH)
    - Avatar: `<span className="flex items-center justify-center h-9 w-9 rounded-full bg-accent-light text-accent font-semibold text-sm">{initials}</span>` -- extract initials from name (first letter of first and last name)
    - Role badge: small span with `bg-accent-light text-accent text-xs px-2 py-0.5 rounded-full` showing the role
    - `<Dropdown>` with:
      - User info section (name + email)
      - Divider
      - Profile link (href="/dashboard/account" or "/admin/settings")
      - Settings link
      - Sign Out button that calls signOut() then router.push("/login") (matching LogoutButton pattern)
    - All items use lucide-react icons, no inline SVGs
    - Styling uses dashboard.css tokens: text-foreground, text-text2, bg-surface, border-border, etc.
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && test -f src/components/dashboard/Backdrop.tsx && echo "PASS: Backdrop exists" && test -f src/components/dashboard/AppSidebar.tsx && echo "PASS: AppSidebar exists" && test -f src/components/dashboard/UserDropdown.tsx && echo "PASS: UserDropdown exists" && grep "navItems.*NavItem" src/components/dashboard/AppSidebar.tsx && grep "from.*lucide-react" src/components/dashboard/AppSidebar.tsx && grep "w-\[290px\]" src/components/dashboard/AppSidebar.tsx && grep "w-\[90px\]" src/components/dashboard/AppSidebar.tsx && grep "from.*@/lib/auth-client" src/components/dashboard/UserDropdown.tsx && grep "signOut" src/components/dashboard/UserDropdown.tsx</automated>
  </verify>
  <done>Backdrop, AppSidebar, and UserDropdown components created. Sidebar accepts navItems prop, uses 290px/90px widths, has hover flyout, no SidebarWidget. UserDropdown uses useSession, shows avatar/role/sign-out. All use dashboard.css tokens and lucide-react.</done>
</task>

<task type="auto">
  <name>Task 2: Create AppHeader and DashboardShell components</name>
  <files>src/components/dashboard/AppHeader.tsx, src/components/dashboard/DashboardShell.tsx</files>
  <read_first>
    - backenddashboard/src/layout/AppHeader.tsx (header structure reference)
    - backenddashboard/src/app/(admin)/layout.tsx (layout composition pattern)
    - src/components/common/ThemeToggleButton.tsx (updated in Plan 01)
    - src/components/dashboard/UserDropdown.tsx (created in Task 1 of this plan)
    - src/context/SidebarContext.tsx (useSidebar API)
    - src/styles/dashboard.css (design token class names)
  </read_first>
  <acceptance_criteria>
    - File src/components/dashboard/AppHeader.tsx exists with "use client" directive
    - File src/components/dashboard/DashboardShell.tsx exists with "use client" directive
    - AppHeader imports useSidebar from @/context/SidebarContext
    - AppHeader imports ThemeToggleButton from @/components/common/ThemeToggleButton
    - AppHeader imports UserDropdown from @/components/dashboard/UserDropdown
    - AppHeader imports Bell and Menu and X from lucide-react
    - AppHeader has handleToggle function with 1024px breakpoint split (>=1024 toggleSidebar, <1024 toggleMobileSidebar)
    - AppHeader has sticky positioning with z-[99999]
    - AppHeader shows hamburger/X icon based on isMobileOpen state
    - AppHeader shows ThemeToggleButton, Bell icon (no dropdown, per D-08), and UserDropdown
    - AppHeader does NOT have search bar (per D-06)
    - DashboardShell imports SidebarProvider from @/context/SidebarContext
    - DashboardShell imports AppSidebar, AppHeader, Backdrop
    - DashboardShell accepts navItems: NavItem[] and children: React.ReactNode props
    - DashboardShell wraps children in SidebarProvider
    - DashboardShell applies correct margin to main content: lg:ml-[290px] when expanded/hovered, lg:ml-[90px] when collapsed
    - Both files use dashboard.css design tokens
    - pnpm build succeeds
  </acceptance_criteria>
  <action>
    Create two components in src/components/dashboard/. Both are "use client" components.

    **AppHeader.tsx** -- Port from backenddashboard/src/layout/AppHeader.tsx, adapted per decisions:
    - Import `useSidebar` from `@/context/SidebarContext`
    - Import `ThemeToggleButton` from `@/components/common/ThemeToggleButton`
    - Import `UserDropdown` from `@/components/dashboard/UserDropdown`
    - Import `Bell`, `Menu`, `X` from `lucide-react`
    - Import `Link` from `next/link`
    - Named export `AppHeader`

    Header structure:
    - `<header className="sticky top-0 flex w-full bg-surface border-b border-border z-[99999]">`
    - Inner `<div className="flex flex-col items-center justify-between grow lg:flex-row lg:px-6">`

    Left section (toggle + mobile logo):
    - `<div className="flex items-center justify-between w-full gap-2 px-3 py-3 border-b border-border sm:gap-4 lg:justify-normal lg:border-b-0 lg:px-0 lg:py-4">`
    - Hamburger toggle button: `onClick={handleToggle}`, shows `<X />` when `isMobileOpen`, `<Menu />` otherwise (per D-06, using lucide-react instead of inline SVGs)
    - Toggle button classes: `"flex items-center justify-center w-10 h-10 text-text2 rounded-lg z-[99999] lg:flex lg:h-11 lg:w-11 lg:border lg:border-border"`
    - Mobile logo link (lg:hidden): Link to "/" showing "CF" in syne font (matching sidebar collapsed state)
    - No search bar (per D-06 -- skip Cmd+K search)

    Right section (theme + bell + user):
    - `<div className="hidden items-center justify-between w-full gap-4 px-5 py-4 lg:flex lg:justify-end lg:px-0">`
    - ThemeToggleButton component
    - Notification bell: `<button className="relative flex items-center justify-center text-text2 bg-surface border border-border rounded-full h-11 w-11 hover:bg-accent-light transition-colors"><Bell className="w-5 h-5" /></button>` -- no dropdown, no notification dot (per D-08 -- just the icon)
    - UserDropdown component

    handleToggle function (matching backenddashboard breakpoint split per Pitfall 6 in RESEARCH):
    ```
    const handleToggle = () => {
      if (window.innerWidth >= 1024) {
        toggleSidebar();       // Desktop: expand/collapse 290px/90px
      } else {
        toggleMobileSidebar(); // Mobile/tablet: slide-in drawer
      }
    };
    ```

    **DashboardShell.tsx** -- Shared layout wrapper (per D-09):
    - Import `SidebarProvider`, `useSidebar` from `@/context/SidebarContext`
    - Import `AppSidebar` from `./AppSidebar`
    - Import `AppHeader` from `./AppHeader`
    - Import `Backdrop` from `./Backdrop`
    - Import type `NavItem` from `@/data/dashboard-nav`

    Exported component:
    ```
    export function DashboardShell({
      children,
      navItems,
    }: {
      children: React.ReactNode;
      navItems: NavItem[];
    }) {
      return (
        <SidebarProvider>
          <DashboardLayout navItems={navItems}>{children}</DashboardLayout>
        </SidebarProvider>
      );
    }
    ```

    Inner DashboardLayout function (must be inside SidebarProvider to use useSidebar):
    - Uses `useSidebar()` to get isExpanded, isHovered, isMobileOpen
    - Main content margin: `isMobileOpen ? "ml-0" : (isExpanded || isHovered) ? "lg:ml-[290px]" : "lg:ml-[90px]"`
    - Renders:
      ```
      <div className="min-h-screen xl:flex">
        <AppSidebar navItems={navItems} />
        <Backdrop />
        <div className={cn("flex-1 transition-all duration-300 ease-in-out", mainContentMargin)}>
          <AppHeader />
          <div className="p-4 mx-auto max-w-(--breakpoint-2xl) md:p-6">
            {children}
          </div>
        </div>
      </div>
      ```
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && test -f src/components/dashboard/AppHeader.tsx && echo "PASS: AppHeader exists" && test -f src/components/dashboard/DashboardShell.tsx && echo "PASS: DashboardShell exists" && grep "handleToggle" src/components/dashboard/AppHeader.tsx && grep "1024" src/components/dashboard/AppHeader.tsx && grep "Bell" src/components/dashboard/AppHeader.tsx && grep "SidebarProvider" src/components/dashboard/DashboardShell.tsx && grep "navItems" src/components/dashboard/DashboardShell.tsx && grep "lg:ml-\[290px\]" src/components/dashboard/DashboardShell.tsx && grep "lg:ml-\[90px\]" src/components/dashboard/DashboardShell.tsx && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>AppHeader and DashboardShell created. Header has hamburger toggle with 1024px breakpoint split, theme toggle, bell icon, user dropdown -- no search bar. DashboardShell wraps SidebarProvider + sidebar + backdrop + header + content with correct margin logic. pnpm build passes.</done>
</task>

</tasks>

<verification>
- All 5 dashboard components exist in src/components/dashboard/
- AppSidebar renders nav items from prop with 290px/90px states and hover flyout
- AppHeader has toggle (1024px split), theme toggle, bell icon, user dropdown
- UserDropdown uses useSession and signOut
- Backdrop renders on mobile when sidebar open
- DashboardShell composes all pieces with SidebarProvider
- pnpm build succeeds
</verification>

<success_criteria>
All dashboard shell components created and building successfully. Components are parameterized by navItems prop (per D-09). Sidebar has 3 states (per D-01). Header has core features without search (per D-06). Notification bell is present but non-functional (per D-08).
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-02-SUMMARY.md`
</output>
