---
phase: 02-homepage
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/app/(portal)/layout.tsx
  - src/app/(admin)/layout.tsx
  - src/app/(portal)/dashboard/page.tsx
  - src/app/(admin)/admin/dashboard/page.tsx
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "Customer portal /dashboard renders inside the sidebar shell with 6 nav items"
    - "Admin /admin/dashboard renders inside the sidebar shell with 6 nav items"
    - "Sidebar collapses and expands with the hamburger toggle"
    - "Dark/light theme toggle works consistently across both dashboards"
    - "Dashboard CSS does not affect marketing site"
  artifacts:
    - path: "src/app/(portal)/layout.tsx"
      provides: "Portal layout wrapping children with DashboardShell + customerNavItems"
      contains: "DashboardShell"
    - path: "src/app/(admin)/layout.tsx"
      provides: "Admin layout wrapping children with DashboardShell + adminNavItems"
      contains: "DashboardShell"
    - path: "src/app/(portal)/dashboard/page.tsx"
      provides: "Portal dashboard page rendering inside the shell"
    - path: "src/app/(admin)/admin/dashboard/page.tsx"
      provides: "Admin dashboard page rendering inside the shell"
  key_links:
    - from: "src/app/(portal)/layout.tsx"
      to: "src/components/dashboard/DashboardShell.tsx"
      via: "DashboardShell import"
      pattern: "import.*DashboardShell.*from.*@/components/dashboard/DashboardShell"
    - from: "src/app/(portal)/layout.tsx"
      to: "src/data/dashboard-nav.ts"
      via: "customerNavItems import"
      pattern: "import.*customerNavItems.*from.*@/data/dashboard-nav"
    - from: "src/app/(admin)/layout.tsx"
      to: "src/components/dashboard/DashboardShell.tsx"
      via: "DashboardShell import"
      pattern: "import.*DashboardShell.*from.*@/components/dashboard/DashboardShell"
    - from: "src/app/(admin)/layout.tsx"
      to: "src/data/dashboard-nav.ts"
      via: "adminNavItems import"
      pattern: "import.*adminNavItems.*from.*@/data/dashboard-nav"
---

<objective>
Wire the dashboard shell into both route group layouts and update dashboard pages to render properly inside the new shell.

Purpose: This is the final integration step -- connecting the shared DashboardShell component (from Plan 02) with the two route group layouts and their respective nav configs. This makes the dashboard shell visible and functional for both customer and admin users.

Output: Updated (portal)/layout.tsx and (admin)/layout.tsx using DashboardShell with proper nav configs, updated dashboard pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-homepage/02-CONTEXT.md
@.planning/phases/02-homepage/02-01-SUMMARY.md
@.planning/phases/02-homepage/02-02-SUMMARY.md

@src/app/(portal)/layout.tsx
@src/app/(admin)/layout.tsx
@src/app/(portal)/dashboard/page.tsx
@src/app/(admin)/admin/dashboard/page.tsx
@src/components/dashboard/DashboardShell.tsx
@src/data/dashboard-nav.ts

<interfaces>
<!-- Components and types from Plan 02 that these layouts consume -->

From src/components/dashboard/DashboardShell.tsx:
```typescript
export function DashboardShell({
  children,
  navItems,
}: {
  children: React.ReactNode;
  navItems: NavItem[];
}): JSX.Element;
```

From src/data/dashboard-nav.ts:
```typescript
export const customerNavItems: NavItem[];
export const adminNavItems: NavItem[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire DashboardShell into portal and admin layouts, update dashboard pages</name>
  <files>src/app/(portal)/layout.tsx, src/app/(admin)/layout.tsx, src/app/(portal)/dashboard/page.tsx, src/app/(admin)/admin/dashboard/page.tsx</files>
  <read_first>
    - src/app/(portal)/layout.tsx (the file being modified)
    - src/app/(admin)/layout.tsx (the file being modified)
    - src/app/(portal)/dashboard/page.tsx (the file being modified)
    - src/app/(admin)/admin/dashboard/page.tsx (the file being modified)
    - src/components/dashboard/DashboardShell.tsx (the component being integrated)
    - src/data/dashboard-nav.ts (nav configs being imported)
  </read_first>
  <acceptance_criteria>
    - (portal)/layout.tsx imports DashboardShell from @/components/dashboard/DashboardShell
    - (portal)/layout.tsx imports customerNavItems from @/data/dashboard-nav
    - (portal)/layout.tsx renders `<DashboardShell navItems={customerNavItems}>` wrapping children
    - (portal)/layout.tsx remains a server component (no "use client" directive)
    - (portal)/layout.tsx still has metadata export
    - (admin)/layout.tsx imports DashboardShell from @/components/dashboard/DashboardShell
    - (admin)/layout.tsx imports adminNavItems from @/data/dashboard-nav
    - (admin)/layout.tsx renders `<DashboardShell navItems={adminNavItems}>` wrapping children
    - (admin)/layout.tsx remains a server component (no "use client" directive)
    - (admin)/layout.tsx still has metadata export
    - Both layouts still import dashboard.css and ThemeProvider
    - Both layouts still have html/body tags with font variables and suppressHydrationWarning
    - Portal dashboard page no longer uses min-h-screen centering (shell handles layout)
    - Admin dashboard page no longer uses min-h-screen centering (shell handles layout)
    - Both dashboard pages still have auth checks and redirects
    - pnpm build succeeds
  </acceptance_criteria>
  <action>
    Per D-09, update both route group layouts to use DashboardShell with their respective nav configs.

    **src/app/(portal)/layout.tsx** -- Update the existing server component layout:
    - Keep all existing imports (fonts, ThemeProvider, dashboard.css)
    - Add import: `import { DashboardShell } from "@/components/dashboard/DashboardShell";`
    - Add import: `import { customerNavItems } from "@/data/dashboard-nav";`
    - Keep metadata export unchanged
    - In the JSX, replace `<main>{children}</main>` with:
      ```
      <DashboardShell navItems={customerNavItems}>
        {children}
      </DashboardShell>
      ```
    - The layout remains a server component (no "use client") -- it renders a client component (DashboardShell) which is fine
    - Keep html/body structure, ThemeProvider wrapping, and font variables exactly as they are

    **src/app/(admin)/layout.tsx** -- Update the existing server component layout:
    - Keep all existing imports (fonts, ThemeProvider, dashboard.css)
    - Add import: `import { DashboardShell } from "@/components/dashboard/DashboardShell";`
    - Add import: `import { adminNavItems } from "@/data/dashboard-nav";`
    - Keep metadata export unchanged
    - In the JSX, replace `<main>{children}</main>` with:
      ```
      <DashboardShell navItems={adminNavItems}>
        {children}
      </DashboardShell>
      ```
    - The layout remains a server component

    **src/app/(portal)/dashboard/page.tsx** -- Update the existing portal dashboard page:
    - Keep the auth check, session fetch, and role redirect logic unchanged
    - Remove the LogoutButton import (logout is now in the header UserDropdown)
    - Remove the `<div className="flex items-center justify-center min-h-screen">` wrapper (the shell handles full layout)
    - Replace the centered layout with a simple content area:
      ```
      <div>
        <h1 className="text-2xl font-bold font-syne mb-2">Dashboard</h1>
        <p className="text-text2">Welcome, {session.user.name || session.user.email}</p>
        <p className="text-sm text-muted mt-4">Portal features coming in Phase 3</p>
      </div>
      ```

    **src/app/(admin)/admin/dashboard/page.tsx** -- Update the existing admin dashboard page:
    - Keep the auth check, session fetch, and role redirect logic unchanged
    - Remove the LogoutButton import (logout is now in the header UserDropdown)
    - Remove the `<div className="flex items-center justify-center min-h-screen">` wrapper
    - Replace with:
      ```
      <div>
        <h1 className="text-2xl font-bold font-syne mb-2">Admin Overview</h1>
        <p className="text-text2">Welcome, {session.user.name || session.user.email}</p>
        <p className="text-sm text-muted mt-4">Admin features coming in Phase 5</p>
      </div>
      ```
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && grep "DashboardShell" src/app/\(portal\)/layout.tsx && grep "customerNavItems" src/app/\(portal\)/layout.tsx && grep "DashboardShell" src/app/\(admin\)/layout.tsx && grep "adminNavItems" src/app/\(admin\)/layout.tsx && grep "metadata" src/app/\(portal\)/layout.tsx && grep "metadata" src/app/\(admin\)/layout.tsx && head -1 src/app/\(portal\)/layout.tsx | grep -v "use client" && echo "PASS: Portal layout is server component" && head -1 src/app/\(admin\)/layout.tsx | grep -v "use client" && echo "PASS: Admin layout is server component" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Both route group layouts use DashboardShell with their respective nav configs. Both remain server components. Dashboard pages updated to render content inside the shell. LogoutButton removed from pages (now in header). pnpm build passes.</done>
</task>

</tasks>

<verification>
- pnpm build succeeds with no errors
- Customer portal layout uses DashboardShell with customerNavItems
- Admin layout uses DashboardShell with adminNavItems
- Both layouts remain server components with metadata exports
- Dashboard pages render inside the shell without min-h-screen centering
- No remaining LogoutButton imports in dashboard pages (moved to header)
</verification>

<success_criteria>
- Customer portal /dashboard shows sidebar with 6 nav items (Dashboard, Licenses, Billing, Downloads, Support, Account) per D-10
- Admin /admin/dashboard shows sidebar with 6 nav items (Overview, Sales, Users, Invoices, Licenses, Settings) per D-11
- Sidebar collapses to hamburger menu on mobile per D-01
- Theme toggle works consistently per DASH-03
- Dashboard CSS isolated from marketing site per DASH-02
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-03-SUMMARY.md`
</output>
