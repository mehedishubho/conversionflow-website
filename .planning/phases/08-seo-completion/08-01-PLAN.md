---
phase: 08-seo-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/sitemap.ts
  - src/app/robots.ts
autonomous: true
requirements: [SEO-03, SEO-04]

must_haves:
  truths:
    - "`/sitemap.xml` is generated by native Next.js metadata routes"
    - "`/sitemap.xml` includes all current public routes"
    - "`/sitemap.xml` includes blog and docs dynamic slugs from MDX content"
    - "`/robots.txt` allows normal crawling and points to the canonical sitemap"
    - "No `next-sitemap` dependency or generated static sitemap file is added"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Next.js sitemap metadata route"
      contains: "MetadataRoute.Sitemap"
    - path: "src/app/robots.ts"
      provides: "Next.js robots metadata route"
      contains: "MetadataRoute.Robots"
  key_links:
    - from: "src/app/sitemap.ts"
      to: "src/lib/mdx.ts"
      via: "getBlogPosts() and getDocPosts()"
      pattern: "getBlogPosts"
---

<objective>
Create native Next.js sitemap and robots metadata routes for WooBooster. The sitemap must cover all current public pages, including Phase 7 blog, docs, and legal routes. Robots must allow search engines to crawl public content and advertise the sitemap URL.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-seo-completion/08-CONTEXT.md
@.planning/phases/08-seo-completion/08-RESEARCH.md
@.planning/phases/08-seo-completion/08-VALIDATION.md
@src/lib/mdx.ts
@src/data/navigation.ts
@src/data/docs-nav.ts
@src/app/layout.tsx

<interfaces>
From `src/lib/mdx.ts`:

```typescript
export function getBlogPosts(): BlogPost[];
export function getDocPosts(): DocPost[];
```

Expected route files:

```typescript
import type { MetadataRoute } from "next";

export default function sitemap(): MetadataRoute.Sitemap {}
export default function robots(): MetadataRoute.Robots {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sitemap metadata route</name>
  <files>src/app/sitemap.ts</files>
  <read_first>src/lib/mdx.ts, src/data/navigation.ts, src/data/docs-nav.ts</read_first>
  <action>
Create `src/app/sitemap.ts` using the native Next.js metadata route convention.

Implementation requirements:
- Import `type { MetadataRoute }` from `"next"`.
- Import `getBlogPosts` and `getDocPosts` from `"@/lib/mdx"`.
- Use `const siteUrl = "https://woobooster.com";`.
- Include static routes:
  - `/`
  - `/features`
  - `/pricing`
  - `/changelog`
  - `/support`
  - `/blog`
  - `/docs`
  - `/privacy`
  - `/terms`
  - `/refund`
  - `/license`
- Include dynamic blog routes from `getBlogPosts().map((post) => `/blog/${post.slug}`)`.
- Include dynamic docs routes from `getDocPosts().map((doc) => `/docs/${doc.slug}`)`.
- Set `lastModified: new Date()` for all entries.
- Use sensible `changeFrequency` and `priority` values. Keep them deterministic by route type.
- Return `MetadataRoute.Sitemap`.

Do not add new dependencies.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <manual>Start `pnpm dev`, open `/sitemap.xml`, and confirm static, blog, docs, and legal routes are present with `https://woobooster.com` URLs.</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Create robots metadata route</name>
  <files>src/app/robots.ts</files>
  <read_first>src/app/layout.tsx</read_first>
  <action>
Create `src/app/robots.ts` using the native Next.js metadata route convention.

Implementation requirements:
- Import `type { MetadataRoute }` from `"next"`.
- Use `const siteUrl = "https://woobooster.com";`.
- Return:
  - `rules.userAgent = "*"`
  - `rules.allow = "/"`
  - `rules.disallow = ["/_next/", "/api/"]`
  - `sitemap = "https://woobooster.com/sitemap.xml"`
  - `host = "https://woobooster.com"`

Do not create `public/robots.txt`; the App Router metadata route should own this output.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <manual>Start `pnpm dev`, open `/robots.txt`, and confirm crawling is allowed and sitemap URL is present.</manual>
  </verify>
</task>

</tasks>

<threat_model>
No user input, secrets, or private data are exposed by these routes. Sitemap slugs come from local MDX content files only. Robots should not accidentally block public pages; the only disallowed paths should be generated/internal prefixes.
</threat_model>

<success_criteria>
- `/sitemap.xml` is accessible.
- `/sitemap.xml` lists all public pages from Phase 8 context.
- `/robots.txt` is accessible.
- `/robots.txt` allows public crawling and points to the sitemap.
- `pnpm build` passes.
</success_criteria>
