---
phase: 05-data-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/pricing.ts
  - src/data/changelog.ts
  - src/data/faq.ts
  - src/app/pricing/page.tsx
  - src/app/changelog/page.tsx
  - src/components/sections/FAQAccordion.tsx
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-05
  - CHLOG-03

must_haves:
  truths:
    - "Pricing page reads pricingTiers from @/data/pricing, not inline array"
    - "Changelog page reads changelogEntries from @/data/changelog, not inline array"
    - "FAQAccordion reads faqItems from @/data/faq, not inline array"
    - "tagLabels stays in changelog page component (presentation logic, not data per D-03)"
    - "All pages render identically -- no visual or content change"
  artifacts:
    - path: "src/data/pricing.ts"
      provides: "PricingTier interface + pricingTiers export with dual-currency structure (D-04)"
      exports: ["pricingTiers"]
    - path: "src/data/changelog.ts"
      provides: "ChangelogEntry interface + changelogEntries export"
      exports: ["changelogEntries"]
    - path: "src/data/faq.ts"
      provides: "FAQItem interface + faqItems export"
      exports: ["faqItems"]
  key_links:
    - from: "src/app/pricing/page.tsx"
      to: "src/data/pricing.ts"
      via: "import { pricingTiers } from '@/data/pricing'"
      pattern: "import.*pricingTiers.*from.*@/data/pricing"
    - from: "src/app/changelog/page.tsx"
      to: "src/data/changelog.ts"
      via: "import { changelogEntries } from '@/data/changelog'"
      pattern: "import.*changelogEntries.*from.*@/data/changelog"
    - from: "src/components/sections/FAQAccordion.tsx"
      to: "src/data/faq.ts"
      via: "import { faqItems } from '@/data/faq'"
      pattern: "import.*faqItems.*from.*@/data/faq"
---

<objective>
Extract pricing tiers, changelog entries, and FAQ items from their inline locations into dedicated data files. Update all consuming components to import from the new data files.

Purpose: These are the three highest-value data extractions for content maintainability. Pricing uses dual-currency structure (D-04) to support Phase 6 currency toggle. Changelog and FAQ are pure content that benefits from externalization for i18n (Phase 9).
Output: 3 new data files (pricing.ts, changelog.ts, faq.ts); 3 updated consumer files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-layer/05-CONTEXT.md
@.planning/phases/05-data-layer/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pricing.ts, changelog.ts, faq.ts and update consumers</name>
  <files>src/data/pricing.ts, src/data/changelog.ts, src/data/faq.ts, src/app/pricing/page.tsx, src/app/changelog/page.tsx, src/components/sections/FAQAccordion.tsx</files>
  <read_first>
    src/app/pricing/page.tsx
    src/app/changelog/page.tsx
    src/components/sections/FAQAccordion.tsx
    .planning/phases/05-data-layer/05-CONTEXT.md
  </read_first>
  <action>
    **File 1: Create `src/data/pricing.ts`** (per D-01, D-02, D-03, D-04)

    Extract the `pricingTiers` array from `src/app/pricing/page.tsx` (lines 14-75). The data file must:
    - Define `interface PricingFeature` with fields: `text: string`, `included: boolean`
    - Define `interface PricingTier` with fields: `plan: string`, `priceUSD: string`, `priceBDT: string`, `period: string`, `desc: string`, `popular: boolean`, `features: PricingFeature[]`, `buttonText: string`, `buttonStyle: "btn-primary" | "btn-outline"` (use literal union, not `as const`)
    - The current pricing page uses `price` and `bdt` fields. Rename them to `priceUSD` and `priceBDT` for the dual-currency structure (D-04). Use these exact values from the inline data:
      - Starter: `priceUSD: "$29"`, `priceBDT: "3,499 BDT"`
      - Professional: `priceUSD: "$69"`, `priceBDT: "8,299 BDT"`
      - Agency: `priceUSD: "$129"`, `priceBDT: "15,499 BDT"`
    - Export as `export const pricingTiers: PricingTier[] = [...]`
    - Remove `as const` from `buttonStyle` values -- use the literal union type in the interface instead

    **File 2: Create `src/data/changelog.ts`** (per D-01, D-02, D-03)

    Extract the `changelogEntries` array from `src/app/changelog/page.tsx` (lines 13-51). The data file must:
    - Define `type ChangeType = "new" | "imp" | "fix"`
    - Define `interface ChangelogChange` with fields: `type: ChangeType`, `text: string`
    - Define `interface ChangelogEntry` with fields: `version: string`, `date: string`, `name: string`, `isLatest: boolean`, `changes: ChangelogChange[]`
    - Export as `export const changelogEntries: ChangelogEntry[] = [...]`
    - Remove `as const` from type values -- use the ChangeType union instead
    - IMPORTANT: Do NOT include `tagLabels` in the data file. Per D-03 and RESEARCH Pitfall 2, `tagLabels` maps types to CSS classes and display labels -- this is presentation logic, not data. Keep `tagLabels` in the changelog page component.

    **File 3: Create `src/data/faq.ts`** (per D-01, D-02, D-03)

    Extract the `faqItems` array from `src/components/sections/FAQAccordion.tsx` (lines 5-31). The data file must:
    - Define `interface FAQItem` with fields: `question: string`, `answer: string`
    - Export as `export const faqItems: FAQItem[] = [...]`
    - Copy all 5 FAQ items verbatim

    **Consumer Updates:**

    **Update `src/app/pricing/page.tsx`:**
    - REMOVE the inline `pricingTiers` array (lines 14-75)
    - ADD import: `import { pricingTiers } from "@/data/pricing";`
    - UPDATE the JSX template where it references `tier.price` to `tier.priceUSD` and `tier.bdt` to `tier.priceBDT`
    - Specifically: in the price display section, change `{tier.price}` to `{tier.priceUSD}`, change `{tier.bdt}` to `{tier.priceBDT}`

    **Update `src/app/changelog/page.tsx`:**
    - REMOVE the inline `changelogEntries` array (lines 13-51)
    - ADD import: `import { changelogEntries } from "@/data/changelog";`
    - KEEP the `tagLabels` object inline (lines 53-57) -- it stays in this component as presentation logic

    **Update `src/components/sections/FAQAccordion.tsx`:**
    - REMOVE the inline `faqItems` array (lines 5-31)
    - ADD import: `import { faqItems } from "@/data/faq";`
    - Keep the `"use client"` directive and `useState` import -- this component remains a client component
  </action>
  <acceptance_criteria>
    - `src/data/pricing.ts` exists with `interface PricingFeature`, `interface PricingTier`, `export const pricingTiers: PricingTier[]` -- interface has `priceUSD` and `priceBDT` string fields (not `price` and `bdt`)
    - `src/data/changelog.ts` exists with `type ChangeType`, `interface ChangelogChange`, `interface ChangelogEntry`, `export const changelogEntries: ChangelogEntry[]`
    - `src/data/faq.ts` exists with `interface FAQItem`, `export const faqItems: FAQItem[]`
    - `src/app/pricing/page.tsx` contains `import { pricingTiers } from "@/data/pricing"` and does NOT contain `const pricingTiers =`
    - `src/app/pricing/page.tsx` references `tier.priceUSD` and `tier.priceBDT` (not `tier.price` or `tier.bdt`)
    - `src/app/changelog/page.tsx` contains `import { changelogEntries } from "@/data/changelog"` and still contains `const tagLabels` (NOT extracted)
    - `src/components/sections/FAQAccordion.tsx` contains `import { faqItems } from "@/data/faq"` and does NOT contain `const faqItems =`
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/Devsroom-Work/woobooster-website" && pnpm build</automated>
  </verify>
  <done>Three data files created (pricing.ts, changelog.ts, faq.ts). Three consumer files updated to import from @/data/. Pricing uses dual-currency fields (priceUSD/priceBDT). tagLabels remains in changelog component. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Static data extraction only. No trust boundaries crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | S | data files | accept | Static TypeScript data, no user input, no runtime mutations |
</threat_model>

<verification>
- `pnpm build` passes
- `grep "const pricingTiers" src/app/pricing/page.tsx` returns nothing
- `grep "const changelogEntries" src/app/changelog/page.tsx` returns nothing
- `grep "const faqItems" src/components/sections/FAQAccordion.tsx` returns nothing
- `grep "tagLabels" src/data/changelog.ts` returns nothing (tagLabels NOT in data file)
- All three import statements present in consumer files
</verification>

<success_criteria>
- 3 data files exist in src/data/ with co-located interfaces and named exports
- 3 consumer files updated to import from @/data/ instead of inline arrays
- Pricing uses dual-currency structure (priceUSD/priceBDT)
- tagLabels stays in changelog component (not in data file)
- pnpm build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-layer/05-02-SUMMARY.md`
</output>
