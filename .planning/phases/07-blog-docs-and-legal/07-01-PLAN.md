---
phase: 07-blog-docs-and-legal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - tsconfig.json
  - mdx-components.tsx
  - src/app/globals.css
  - src/lib/mdx.ts
  - src/data/docs-nav.ts
  - src/content/blog/woo-booster-getting-started.mdx
  - src/content/blog/bd-ecommerce-courier-guide.mdx
  - src/content/blog/meta-capi-bangladesh.mdx
  - src/content/docs/getting-started.mdx
  - src/content/docs/courier-sync.mdx
  - src/content/docs/meta-capi.mdx
  - src/content/docs/fraud-shield.mdx
  - src/content/docs/analytics.mdx
autonomous: true
requirements: [BLOG-03, BLOG-04]

must_haves:
  truths:
    - "MDX files compile at build time without errors"
    - "gray-matter extracts frontmatter (title, date, excerpt) from blog MDX files"
    - "remark-gfm enables tables, strikethrough, and task lists in MDX content"
    - "rehype-slug adds IDs to all headings for TOC anchor links"
    - "prose class applies typography styles using existing design tokens"
    - "3 blog MDX files and 5 docs MDX files exist with valid frontmatter"
  artifacts:
    - path: "next.config.ts"
      provides: "MDX compilation with remark-gfm, rehype-slug, rehype-autolink-headings"
      contains: "createMDX"
    - path: "mdx-components.tsx"
      provides: "Required global MDX component overrides"
      contains: "useMDXComponents"
    - path: "src/app/globals.css"
      provides: "Typography plugin registration and prose customization"
      contains: "@plugin"
    - path: "src/lib/mdx.ts"
      provides: "getBlogPosts(), getDocPosts(), reading time calculation"
      exports: ["getBlogPosts", "getDocPosts"]
    - path: "src/data/docs-nav.ts"
      provides: "Docs sidebar navigation structure (2 categories, 5 guides)"
      contains: "Getting Started"
    - path: "src/content/blog/woo-booster-getting-started.mdx"
      provides: "Sample blog post with frontmatter, headings, table, code block"
      contains: "title"
    - path: "src/content/docs/getting-started.mdx"
      provides: "Sample doc with frontmatter (title, category, order)"
      contains: "category"
  key_links:
    - from: "next.config.ts"
      to: "@next/mdx"
      via: "createMDX wrapper"
      pattern: "createMDX"
    - from: "src/lib/mdx.ts"
      to: "src/content/blog/*.mdx"
      via: "fs.readdirSync + gray-matter"
      pattern: "matter\\(raw\\)"
    - from: "src/app/globals.css"
      to: "@tailwindcss/typography"
      via: "@plugin directive"
      pattern: "@plugin.*typography"
---

<objective>
Install MDX dependencies, configure Next.js for MDX compilation with remark/rehype plugins, set up Tailwind Typography with design token customization, create the MDX utility library, and write all 8 content files (3 blog posts + 5 docs). This plan builds the foundation that Plans 02, 03, and 04 consume.

Purpose: All downstream plans (blog UI, docs UI, legal pages) depend on MDX compilation and content files existing. This plan installs, configures, and seeds everything.
Output: Working MDX pipeline, typography styles, content files, and utility functions for metadata extraction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-blog-docs-and-legal/07-CONTEXT.md
@.planning/phases/07-blog-docs-and-legal/07-RESEARCH.md
@.planning/phases/07-blog-docs-and-legal/07-UI-SPEC.md
@next.config.ts
@tsconfig.json
@src/app/globals.css
@src/data/navigation.ts

<interfaces>
<!-- Existing types the executor needs -->

From src/data/navigation.ts:
```typescript
interface NavLink {
  name: string;
  href: string;
}
```

From next.config.ts (current state):
```typescript
import type { NextConfig } from "next";
const nextConfig: NextConfig = { output: "standalone" };
export default nextConfig;
```

From tsconfig.json (current include array):
```json
["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts", ".next/dev/types/**/*.ts", "**/*.mts"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install MDX dependencies, configure Next.js, and set up Tailwind Typography</name>
  <files>next.config.ts, mdx-components.tsx, tsconfig.json, src/app/globals.css, package.json</files>
  <read_first>next.config.ts, tsconfig.json, src/app/globals.css</read_first>
  <action>
Per D-01, D-05: Install MDX and typography dependencies as dev dependencies:

```bash
pnpm add -D @next/mdx @mdx-js/loader @mdx-js/react @types/mdx @tailwindcss/typography gray-matter remark-gfm rehype-slug rehype-autolink-headings
```

**next.config.ts** -- Replace entire file with MDX configuration per D-01, D-05. Use `createMDX` wrapper. Configure remark-gfm (per D-21 for BLOG-03), rehype-slug, and rehype-autolink-headings (per D-15 for DOCS-02). Keep `output: "standalone"`. Add `pageExtensions: ["js", "jsx", "md", "mdx", "ts", "tsx"]`. Exact configuration:

```typescript
import type { NextConfig } from "next";
import createMDX from "@next/mdx";
import remarkGfm from "remark-gfm";
import rehypeSlug from "rehype-slug";
import rehypeAutolinkHeadings from "rehype-autolink-headings";

const nextConfig: NextConfig = {
  output: "standalone",
  pageExtensions: ["js", "jsx", "md", "mdx", "ts", "tsx"],
};

const withMDX = createMDX({
  options: {
    remarkPlugins: [remarkGfm],
    rehypePlugins: [
      rehypeSlug,
      [rehypeAutolinkHeadings, { behavior: "wrap" }],
    ],
  },
});

export default withMDX(nextConfig);
```

**mdx-components.tsx** -- Create at project root (mandatory for @next/mdx per RESEARCH.md Pitfall 2). Exact content:

```typescript
import type { MDXComponents } from "mdx/types";

export function useMDXComponents(components: MDXComponents): MDXComponents {
  return {
    ...components,
  };
}
```

**tsconfig.json** -- Add `"**/*.mdx"` to the `include` array so TypeScript recognizes MDX files. The include array becomes:

```json
["next-env.d.ts", "**/*.ts", "**/*.tsx", "**/*.mdx", ".next/types/**/*.ts", ".next/dev/types/**/*.ts", "**/*.mts"]
```

**src/app/globals.css** -- Per D-21, D-22, D-24. Add `@plugin "@tailwindcss/typography";` on the line immediately after `@import "tailwindcss";` (line 1). Then add prose customization at the end of the file (after the responsive media queries). Exact CSS to append:

```css
/* ── PROSE / MDX TYPOGRAPHY ── */
.prose {
  --tw-prose-body: var(--text2);
  --tw-prose-headings: var(--foreground);
  --tw-prose-links: var(--accent);
  --tw-prose-code: var(--foreground);
  --tw-prose-pre-bg: var(--bg2);
  --tw-prose-pre-border: var(--border);
  --tw-prose-th-borders: var(--border);
  --tw-prose-td-borders: var(--border);
  font-family: var(--font-dm-sans);
}
.prose h2, .prose h3, .prose h4 {
  font-family: var(--font-syne);
}
.prose :where(code):not(:where([class~="not-prose"], [class~="not-prose"] *)) {
  font-family: var(--font-mono);
  font-weight: 600;
}
```
  </action>
  <acceptance_criteria>
    - `next.config.ts` contains `createMDX`
    - `next.config.ts` contains `remarkGfm`
    - `next.config.ts` contains `rehypeSlug`
    - `next.config.ts` contains `pageExtensions`
    - `next.config.ts` contains `output: "standalone"`
    - `mdx-components.tsx` exists at project root and contains `useMDXComponents`
    - `tsconfig.json` contains `**/*.mdx` in include array
    - `src/app/globals.css` contains `@plugin "@tailwindcss/typography"`
    - `src/app/globals.css` contains `--tw-prose-body: var(--text2)`
    - `src/app/globals.css` contains `.prose h2, .prose h3, .prose h4`
    - `pnpm build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "D:\Devsroom-Work\woobooster-website" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>MDX pipeline configured and build passes with all dependencies installed. Typography plugin registered with prose customization matching design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Create MDX utility library, docs navigation data, and all 8 content files</name>
  <files>src/lib/mdx.ts, src/data/docs-nav.ts, src/content/blog/woo-booster-getting-started.mdx, src/content/blog/bd-ecommerce-courier-guide.mdx, src/content/blog/meta-capi-bangladesh.mdx, src/content/docs/getting-started.mdx, src/content/docs/courier-sync.mdx, src/content/docs/meta-capi.mdx, src/content/docs/fraud-shield.mdx, src/content/docs/analytics.mdx</files>
  <read_first>src/lib/utils.ts, src/data/navigation.ts</read_first>
  <action>
Per D-02, D-03, D-08, D-12, D-14: Create all content infrastructure and content files.

**1. src/lib/mdx.ts** -- MDX utility module with metadata extraction. Uses `gray-matter` for frontmatter parsing (per RESEARCH.md Pattern 3) and manual reading time calculation (`Math.ceil(words / 200)`). Export `getBlogPosts()` and `getDocPosts()` functions. Blog posts sorted by date descending.

```typescript
import fs from "fs";
import path from "path";
import matter from "gray-matter";

const BLOG_DIR = path.join(process.cwd(), "src/content/blog");
const DOCS_DIR = path.join(process.cwd(), "src/content/docs");

export interface BlogPostMeta {
  slug: string;
  title: string;
  date: string;
  excerpt: string;
  readingTime: number;
}

export interface DocPostMeta {
  slug: string;
  title: string;
  category: string;
  order: number;
}

export function getBlogPosts(): BlogPostMeta[] {
  const files = fs.readdirSync(BLOG_DIR).filter((f) => f.endsWith(".mdx"));
  return files
    .map((file) => {
      const slug = file.replace(/\.mdx$/, "");
      const raw = fs.readFileSync(path.join(BLOG_DIR, file), "utf-8");
      const { data, content } = matter(raw);
      const words = content.split(/\s+/).filter(Boolean).length;
      return {
        slug,
        title: data.title as string,
        date: data.date as string,
        excerpt: data.excerpt as string,
        readingTime: Math.ceil(words / 200),
      };
    })
    .sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime());
}

export function getDocPosts(): DocPostMeta[] {
  const files = fs.readdirSync(DOCS_DIR).filter((f) => f.endsWith(".mdx"));
  return files
    .map((file) => {
      const slug = file.replace(/\.mdx$/, "");
      const raw = fs.readFileSync(path.join(DOCS_DIR, file), "utf-8");
      const { data } = matter(raw);
      return {
        slug,
        title: data.title as string,
        category: data.category as string,
        order: data.order as number,
      };
    })
    .sort((a, b) => a.order - b.order);
}
```

**2. src/data/docs-nav.ts** -- Docs sidebar navigation structure per D-14. Two categories (Getting Started, Modules) with 5 guides. Hardcoded (not auto-generated per RESEARCH.md recommendation).

```typescript
export interface DocNavItem {
  slug: string;
  title: string;
}

export interface DocNavCategory {
  category: string;
  items: DocNavItem[];
}

export const docsNav: DocNavCategory[] = [
  {
    category: "Getting Started",
    items: [
      { slug: "getting-started", title: "Getting Started" },
    ],
  },
  {
    category: "Modules",
    items: [
      { slug: "courier-sync", title: "Courier Sync" },
      { slug: "meta-capi", title: "Meta CAPI" },
      { slug: "fraud-shield", title: "Fraud Shield" },
      { slug: "analytics", title: "Analytics" },
    ],
  },
];
```

**3. Blog MDX content files** per D-02, D-08. Each file has frontmatter with `title`, `date`, `excerpt`. Content includes varied MDX formatting to demonstrate GFM support (tables, code blocks, headings, lists). Three posts:

**src/content/blog/woo-booster-getting-started.mdx** -- Getting started guide (~400 words). Include an ordered list for installation steps, a feature comparison table, and a short code block showing a WordPress filter example. Date: 2025-06-01.

**src/content/blog/bd-ecommerce-courier-guide.mdx** -- Courier comparison guide for BD stores (~350 words). Include a comparison table of Steadfast, Pathao, RedX with columns for Coverage, Speed, Best For. Include headings with ## and ###. Date: 2025-07-15.

**src/content/blog/meta-capi-bangladesh.mdx** -- Meta CAPI setup guide (~350 words). Include a code block showing a Meta Pixel configuration snippet. Include a table of tracking events. Include bold, italic, and inline code formatting. Date: 2025-08-20.

**4. Docs MDX content files** per D-03, D-12. Each file has frontmatter with `title`, `category`, `order`. Content is structured documentation with h2/h3 headings for TOC generation (per D-15, DOCS-02). Five docs:

**src/content/docs/getting-started.mdx** -- Installation and activation guide. Category: "Getting Started", order: 1. Content covers: prerequisites (WooCommerce 5.0+, WordPress 6.0+, PHP 7.4+), download and upload steps, license activation, initial configuration. Include a requirements table and an ordered list.

**src/content/docs/courier-sync.mdx** -- Courier Sync module docs. Category: "Modules", order: 2. Content covers: supported couriers (Steadfast, Pathao, RedX), API key setup, automatic tracking sync, status mapping. Include a status flow description and a configuration table.

**src/content/docs/meta-capi.mdx** -- Meta CAPI configuration docs. Category: "Modules", order: 3. Content covers: Meta Pixel setup, Conversions API configuration, event mapping, test events. Include a code block and a table of supported events.

**src/content/docs/fraud-shield.mdx** -- Fraud Shield settings docs. Category: "Modules", order: 4. Content covers: fraud detection rules, order scoring, automatic blocking, whitelist/blacklist. Include an ordered list for configuration steps.

**src/content/docs/analytics.mdx** -- Analytics dashboard docs. Category: "Modules", order: 5. Content covers: dashboard overview, revenue tracking, order metrics, export functionality. Include headings and a table of available metrics.

Each MDX file must start with frontmatter in this exact format:
```
---
title: "Title Here"
date: "YYYY-MM-DD"   (blog only)
excerpt: "1-2 sentence summary"   (blog only)
category: "Category Name"   (docs only)
order: N   (docs only)
---
```

IMPORTANT: Create the `src/content/blog/` and `src/content/docs/` directories before writing files. Content must be realistic and professional, tailored to Bangladeshi WooCommerce store owners (the target audience). Use WooBooster product name throughout.
  </action>
  <acceptance_criteria>
    - `src/lib/mdx.ts` contains `export function getBlogPosts()`
    - `src/lib/mdx.ts` contains `export function getDocPosts()`
    - `src/lib/mdx.ts` contains `gray-matter` import
    - `src/lib/mdx.ts` contains `Math.ceil(words / 200)`
    - `src/data/docs-nav.ts` contains `Getting Started` category
    - `src/data/docs-nav.ts` contains `Modules` category
    - `src/data/docs-nav.ts` exports `docsNav` array
    - `src/content/blog/woo-booster-getting-started.mdx` exists with `title`, `date`, `excerpt` frontmatter
    - `src/content/blog/bd-ecommerce-courier-guide.mdx` exists with `title`, `date`, `excerpt` frontmatter
    - `src/content/blog/meta-capi-bangladesh.mdx` exists with `title`, `date`, `excerpt` frontmatter
    - `src/content/docs/getting-started.mdx` exists with `title`, `category`, `order` frontmatter
    - `src/content/docs/courier-sync.mdx` exists with frontmatter
    - `src/content/docs/meta-capi.mdx` exists with frontmatter
    - `src/content/docs/fraud-shield.mdx` exists with frontmatter
    - `src/content/docs/analytics.mdx` exists with frontmatter
    - At least one blog MDX file contains a markdown table (pipes `|`)
    - At least one blog MDX file contains a code block (triple backticks)
    - `pnpm build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "D:\Devsroom-Work\woobooster-website" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>MDX utility library exports getBlogPosts() and getDocPosts(). Docs navigation data file defines 2 categories with 5 guides. All 3 blog MDX posts and 5 docs MDX guides exist with valid frontmatter, varied formatting (tables, code blocks, headings). Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local MDX files -> build pipeline | MDX content is committed to git, compiled at build time. No runtime loading. |
| Dynamic slug -> file import | `generateStaticParams` with `dynamicParams: false` restricts to known slugs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | MDX compilation | mitigate | Only local MDX files; @next/mdx compiles at build time, not runtime |
| T-07-02 | Tampering | Dynamic slug imports | mitigate | dynamicParams: false restricts to known slugs; no user-controlled file paths |
| T-07-03 | Information Disclosure | Build output | accept | No sensitive data in MDX content; all content is public-facing |
</threat_model>

<verification>
- `pnpm build` passes with all new content files compiled
- `src/lib/mdx.ts` exports both functions and both interfaces
- All 8 MDX content files parse correctly with gray-matter
- Typography plugin is registered and prose customization is in globals.css
</verification>

<success_criteria>
- `pnpm build` exits 0 with MDX pipeline configured
- `next.config.ts` wraps config with createMDX including remark-gfm and rehype-slug
- `mdx-components.tsx` exists at project root
- `globals.css` has `@plugin "@tailwindcss/typography"` and prose customization with design tokens
- `src/lib/mdx.ts` exports `getBlogPosts()` and `getDocPosts()` that correctly parse MDX frontmatter
- `src/data/docs-nav.ts` exports sidebar navigation with 2 categories and 5 guides
- 3 blog MDX files exist with varied formatting (tables, code blocks)
- 5 docs MDX files exist with category and order frontmatter
- All 8 MDX files have correct frontmatter fields
</success_criteria>

<output>
After completion, create `.planning/phases/07-blog-docs-and-legal/07-01-SUMMARY.md`
</output>
