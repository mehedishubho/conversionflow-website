---
phase: 10-core-seo
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/components/admin/seo/GeneralSeoForm.tsx
  - src/components/admin/seo/SerpPreview.tsx
  - src/components/admin/seo/SeoScore.tsx
  - src/app/(admin)/admin/settings/seo/general/page.tsx
autonomous: true
requirements:
  - GSEO-01
  - GSEO-02
  - GSEO-03
  - GSEO-04
  - GSEO-05
  - GSEO-06
  - GSEO-07

must_haves:
  truths:
    - "Admin can configure website title, meta title, meta description, keywords, canonical URL, separator, robots directive, and OG image in the General SEO form"
    - "Admin sees a real-time Google SERP preview snippet showing how the page will appear in search results"
    - "Admin sees character count indicators for meta title (50-60 recommended) and meta description (150-160 recommended) with color coding"
    - "Admin can toggle URL formatting options (lowercase URLs, trailing slash) and auto meta generation (per GSEO-03)"
    - "Admin sees an SEO score progress bar based on configured settings completeness across all sub-sections"
  artifacts:
    - path: "src/components/admin/seo/GeneralSeoForm.tsx"
      provides: "Full General SEO configuration form including URL formatting toggles"
      contains: "seo_lowercase_urls"
    - path: "src/components/admin/seo/SerpPreview.tsx"
      provides: "Google SERP preview component"
      contains: "SERP"
    - path: "src/components/admin/seo/SeoScore.tsx"
      provides: "SEO completeness score indicator"
      contains: "percentage"
    - path: "src/app/(admin)/admin/settings/seo/general/page.tsx"
      provides: "General SEO settings page"
      contains: "GeneralSeoForm"
  key_links:
    - from: "src/components/admin/seo/GeneralSeoForm.tsx"
      to: "src/app/(admin)/actions/admin-seo.ts"
      via: "server action import"
      pattern: "saveSeoSettings|getSeoSettings"
    - from: "src/app/(admin)/admin/settings/seo/general/page.tsx"
      to: "src/components/admin/seo/GeneralSeoForm.tsx"
      via: "component import"
      pattern: "GeneralSeoForm"
---

<objective>
Build the General SEO settings page with full form, Google SERP preview, character counters, toggle switches (including URL formatting options per GSEO-03), and SEO score indicator.

Purpose: This is the primary SEO configuration page where admins set site-wide meta defaults. The SERP preview and character counters provide immediate visual feedback (per D-02, D-06). The SEO score shows overall configuration completeness (per D-06). URL formatting toggles (lowercase URLs, trailing slash) satisfy GSEO-03.

Output: 3 new components (GeneralSeoForm, SerpPreview, SeoScore) and the General SEO page replacing the Phase 9 placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-core-seo/10-CONTEXT.md
@.planning/phases/10-core-seo/10-01-SUMMARY.md

@src/components/admin/TrackingSettingsForm.tsx
@src/components/common/ComponentCard.tsx
@src/components/form/input/InputField.tsx
@src/components/form/switch/Switch.tsx
@src/components/ui/button/Button.tsx
@src/app/(admin)/admin/settings/seo/general/page.tsx
@src/app/(admin)/actions/admin-seo.ts

<interfaces>
<!-- Components and patterns the executor must use -->

From src/components/common/ComponentCard.tsx:
```typescript
interface ComponentCardProps {
  title: string;
  children: React.ReactNode;
  className?: string;
  desc?: string;
}
```

From src/components/form/switch/Switch.tsx:
```typescript
interface SwitchProps {
  label: string;
  defaultChecked?: boolean;
  disabled?: boolean;
  onChange?: (checked: boolean) => void;
  color?: "blue" | "gray";
}
```

From src/components/form/input/InputField.tsx:
```typescript
interface InputProps {
  type?: "text" | "number" | "email" | "password" | "date" | "time" | string;
  placeholder?: string;
  defaultValue?: string | number;
  value?: string;
  onChange?: (e: React.ChangeEvent<HTMLInputElement>) => void;
  disabled?: boolean;
  hint?: string;
}
```

From src/components/admin/TrackingSettingsForm.tsx (pattern to replicate):
```typescript
// Pattern: client component with initialData prop, useTransition, isPending
// Pattern: useState for form data, updateField helper, handleSave with startTransition
// Pattern: success/error message display, Button with disabled={isPending}
```

From admin-seo.ts (created in Plan 01):
```typescript
// GENERAL_SEO_KEYS: seo_title, seo_description, seo_keywords, seo_canonical_url,
//   seo_separator, seo_robots_default, seo_og_image, seo_auto_meta,
//   seo_lowercase_urls, seo_trailing_slash
export async function getSeoSettings(keys?: string[]): Promise<SeoSettingsData>;
export async function saveSeoSettings(data: SeoSettingsData): Promise<{success: boolean}>;
export async function getSeoScore(): Promise<{filled: number, total: number, percentage: number}>;
```

From CONTEXT.md D-01, settings keys for General SEO:
- seo_title (string, default "ConversionFlow")
- seo_description (string, default from hardcoded pageSeo)
- seo_keywords (string, default "")
- seo_canonical_url (string, default "")
- seo_separator (string, default "|")
- seo_robots_default (string, default "index, follow")
- seo_og_image (string, default "")
- seo_auto_meta (boolean as string, default "false")
- seo_lowercase_urls (boolean as string, default "false") -- per GSEO-03
- seo_trailing_slash (boolean as string, default "false") -- per GSEO-03

From CONTEXT.md D-02: Google SERP preview only, blue title, green URL, gray description
From CONTEXT.md D-06: SEO score = filled count / total, progress bar red (<50%), yellow (50-79%), green (>=80%)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SerpPreview, SeoScore, and GeneralSeoForm components</name>
  <files>
    src/components/admin/seo/SerpPreview.tsx
    src/components/admin/seo/SeoScore.tsx
    src/components/admin/seo/GeneralSeoForm.tsx
  </files>
  <read_first>
    - src/components/admin/TrackingSettingsForm.tsx (exact form pattern to replicate)
    - src/components/common/ComponentCard.tsx (card wrapper)
    - src/components/form/input/InputField.tsx (input component)
    - src/components/form/switch/Switch.tsx (toggle component)
    - src/components/ui/button/Button.tsx (button component)
    - src/app/(admin)/actions/admin-seo.ts (server actions, GENERAL_SEO_KEYS)
    - .planning/phases/10-core-seo/10-CONTEXT.md (D-02 SERP preview, D-06 SEO score specs)
  </read_first>
  <action>
Create 3 components under `src/components/admin/seo/`:

**1. SerpPreview.tsx** (`"use client"`)
- Props: `title: string`, `description: string`, `url: string`
- Renders a Google-style SERP preview card:
  - Title: blue link text, font-size 18px, truncated to ~60 chars with ellipsis
  - URL: green text below title, show canonical URL or "https://salesconversionflow.com"
  - Description: gray text, truncated to ~160 chars with ellipsis, font-size 14px
  - Favicon placeholder (small gray circle) before URL
- Styled with Tailwind: rounded border, white bg, padding, shadow-sm
- Updates in real-time as parent form fields change (controlled via props)

**2. SeoScore.tsx** (`"use client"`)
- Props: `filled: number`, `total: number`, `percentage: number`
- Displays a progress bar with percentage label
- Bar color logic (per D-06):
  - Red background when percentage < 50% (`bg-red-500`)
  - Yellow/amber when 50-79% (`bg-amber-500`)
  - Green when >= 80% (`bg-green-500`)
- Track: `bg-gray-200 dark:bg-gray-700`, rounded-full, h-2.5
- Text: "{filled} of {total} fields configured ({percentage}%)"
- Compact display: label above bar, no card wrapper (parent wraps)

**3. GeneralSeoForm.tsx** (`"use client"`)
- Props: `initialData: Record<string, string>` (from getSeoSettings)
- Follows TrackingSettingsForm.tsx pattern exactly:
  - `useState` for form data (copy of initialData)
  - `useTransition` + `isPending` for save state
  - `message` state for success/error feedback
  - `handleSave` with `startTransition` calling `saveSeoSettings(data)`
- Layout: ComponentCard titled "General SEO Settings" containing:

  a) **SEO Score section** (top): Call `getSeoScore` from admin-seo.ts on mount via useEffect, display SeoScore component with the result. Store score in local state.

  b) **SERP Preview section**: SerpPreview component reading live form values for `seo_title` and `seo_description`, with site URL from seo_canonical_url or hardcoded fallback.

  c) **Website Title field**: InputField for `seo_title`, placeholder "ConversionFlow", hint "Default website title used across all pages"

  d) **Meta Title field**: InputField for `seo_title` (same value, SERP uses it), with character count badge next to label. Badge color: green (50-60 chars), yellow (40-49 or 61-70), red (<40 or >70). Count shows current length.

  e) **Meta Description field**: textarea (not InputField -- needs multi-line) for `seo_description`, with character count badge. Badge color: green (150-160), yellow (120-149 or 161-180), red (<120 or >180). Use a plain HTML textarea styled with Tailwind matching InputField appearance.

  f) **Keywords field**: InputField for `seo_keywords`, placeholder "e.g. eCommerce, WooCommerce, tracking", hint "Comma-separated keywords"

  g) **Canonical URL field**: InputField for `seo_canonical_url`, placeholder "https://salesconversionflow.com", hint "Override default site URL for canonical tags"

  h) **SEO Separator field**: InputField for `seo_separator`, placeholder "|", hint "Character used to separate page title from site name"

  i) **Robots Default Directive field**: InputField for `seo_robots_default`, placeholder "index, follow", hint "Default robots meta directive for all pages"

  j) **OG Image URL field**: InputField for `seo_og_image`, placeholder "https://salesconversionflow.com/og-image.png", hint "Default Open Graph image for social sharing"

  k) **Toggle switches** (in a separate ComponentCard titled "URL & Auto Settings"):
     - Switch for `seo_auto_meta` (label "Auto Meta Generation", defaultChecked from initialData, hint "Automatically generate meta tags from page content")
     - Switch for `seo_lowercase_urls` (label "Lowercase URLs", defaultChecked from initialData, hint "Convert all page URLs to lowercase for consistency") -- per GSEO-03
     - Switch for `seo_trailing_slash` (label "Trailing Slash", defaultChecked from initialData, hint "Add trailing slash to URLs (e.g. /about/ instead of /about)") -- per GSEO-03
     - On each toggle change: update state with "true"/"false" string value for the corresponding key

- Save button at bottom: Button component with `disabled={isPending}`, text "Save SEO Settings" / "Saving..."
- The SEO score call uses `getSeoScore()` imported from admin-seo.ts as a server action, called in useEffect on mount.
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && npx tsc --noEmit 2>&1 | grep -E "seo/(GeneralSeoForm|SerpPreview|SeoScore)" | head -10 || echo "No errors in SEO components"</automated>
  </verify>
  <acceptance_criteria>
    - SerpPreview.tsx exists with `"use client"` directive
    - SerpPreview renders title (blue), URL (green), description (gray) in Google SERP style
    - SeoScore.tsx exists with progress bar using red/yellow/green color thresholds
    - GeneralSeoForm.tsx exists with all 10 settings fields (seo_title, seo_description, seo_keywords, seo_canonical_url, seo_separator, seo_robots_default, seo_og_image, seo_auto_meta, seo_lowercase_urls, seo_trailing_slash)
    - GeneralSeoForm imports and calls `saveSeoSettings` from admin-seo.ts
    - GeneralSeoForm imports and calls `getSeoScore` from admin-seo.ts
    - Character count badges present for meta title and meta description with color coding
    - Switch components used for all 3 toggles: seo_auto_meta, seo_lowercase_urls, seo_trailing_slash
    - ComponentCard wrappers present for form sections including "URL & Auto Settings"
  </acceptance_criteria>
  <done>GeneralSeoForm component with all 10 General SEO fields (8 text + 2 URL formatting toggles per GSEO-03 + auto meta toggle), SERP preview, character counters, SEO score, and toggle switches. Form saves via server action and displays success/error messages.</done>
</task>

<task type="auto">
  <name>Task 2: Replace General SEO placeholder page</name>
  <files>src/app/(admin)/admin/settings/seo/general/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/settings/seo/general/page.tsx (current placeholder)
    - src/app/(admin)/admin/settings/seo/page.tsx (pattern: server component loading data)
    - src/app/(admin)/actions/admin-seo.ts (getSeoSettings, GENERAL_SEO_KEYS)
    - src/components/admin/seo/GeneralSeoForm.tsx (just created)
  </read_first>
  <action>
Replace the placeholder General SEO page with a real server component that:

1. Sets `export const dynamic = "force-dynamic"` to ensure fresh data on each request.

2. Calls `getSeoSettings()` (all keys, or use GENERAL_SEO_KEYS from admin-seo.ts if exported) to load current settings.

3. Renders `GeneralSeoForm` component passing `initialData` prop with the loaded settings.

4. Wraps in a simple div container (SettingsShell from the layout already provides the sidebar).

5. Add PageBreadcrumb component at the top: `pageTitle="General SEO"` with `basePath="/admin/settings"`.

The page should be a server component (no "use client") that fetches data and passes it down to the client form component, matching the exact pattern used by `src/app/(admin)/admin/settings/seo/page.tsx` with TrackingSettingsForm.
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && grep -c "GeneralSeoForm" src/app/\\(admin\\)/admin/settings/seo/general/page.tsx && grep -c "getSeoSettings" src/app/\\(admin\\)/admin/settings/seo/general/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx no longer contains "Coming in Phase 10" text
    - page.tsx imports GeneralSeoForm from `@/components/admin/seo/GeneralSeoForm`
    - page.tsx imports getSeoSettings from `@/app/(admin)/actions/admin-seo`
    - page.tsx has `export const dynamic = "force-dynamic"`
    - page.tsx passes `initialData` prop to GeneralSeoForm
    - PageBreadcrumb rendered with pageTitle="General SEO"
    - No "use client" directive (server component)
  </acceptance_criteria>
  <done>General SEO page at /admin/settings/seo/general loads settings from DB via server action and renders the full GeneralSeoForm with SERP preview, character counters, URL formatting toggles, and SEO score indicator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GeneralSeoForm -> saveSeoSettings | Client-side form data sent to server action |
| SERP preview | Read-only display, no data boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Spoofing | SerpPreview | accept | Read-only display component, no security implications |
| T-10-05 | Information Disclosure | OG image URL | accept | OG image URLs are public by design |
</threat_model>

<verification>
1. TypeScript compiles without errors for all 4 files
2. General SEO page loads without "Coming in Phase 10" text
3. SERP preview renders with title, URL, description
4. Character counters show with correct color thresholds
5. SEO score progress bar displays
6. Save button calls server action
7. URL formatting toggles (lowercase URLs, trailing slash) are present and functional (per GSEO-03)
</verification>

<success_criteria>
- General SEO page fully functional with form, SERP preview, character counters, toggle switches, and SEO score
- All 10 General SEO settings (including seo_lowercase_urls and seo_trailing_slash) persistable to DB via server action
- URL formatting toggles visible in "URL & Auto Settings" card (per GSEO-03)
- Placeholder text replaced with real form
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-seo/10-02-SUMMARY.md`
</output>
