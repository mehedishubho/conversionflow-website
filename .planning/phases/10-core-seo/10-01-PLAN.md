---
phase: 10-core-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/actions/admin-seo.ts
  - src/lib/seo.ts
autonomous: true
requirements:
  - GSEO-01
  - GSEO-02
  - GSEO-03
  - GSEO-04
  - GSEO-07
  - VERF-01
  - VERF-02
  - VERF-03
  - VERF-04
  - VERF-05
  - SITM-01
  - SITM-02
  - SITM-03
  - SITM-05
  - ROBT-01
  - ROBT-02
  - ROBT-03
  - ROBT-04
  - ROBT-05

must_haves:
  truths:
    - "Admin can save and load all 22 SEO settings keys via typed server actions"
    - "seo.ts reads settings from DB first and falls back to hardcoded values when no DB row exists"
    - "All SEO server actions enforce admin auth via requireAdmin() and create audit logs"
  artifacts:
    - path: "src/app/(admin)/actions/admin-seo.ts"
      provides: "Server actions for all SEO settings CRUD"
      exports: ["getSeoSettings", "saveSeoSettings", "getSeoScore"]
    - path: "src/lib/seo.ts"
      provides: "SEO utilities with DB fallback"
      contains: "getSettings"
  key_links:
    - from: "src/app/(admin)/actions/admin-seo.ts"
      to: "src/lib/db/schema.ts"
      via: "settings table import"
      pattern: "from.*@/lib/db/schema.*settings"
    - from: "src/lib/seo.ts"
      to: "src/app/(admin)/actions/admin-seo.ts"
      via: "DB settings read"
      pattern: "getSettings|db\\.select"
---

<objective>
Create the shared server action foundation for all 4 SEO sub-sections and integrate DB-based settings into seo.ts with hardcoded fallback.

Purpose: All SEO sub-section pages (Plans 02-04) depend on these server actions for reading/writing settings. seo.ts DB integration ensures admin SEO overrides take effect across the marketing site without breaking existing hardcoded behavior.

Output: admin-seo.ts with typed get/save/score functions; seo.ts enhanced with DB fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-core-seo/10-CONTEXT.md

@src/app/(admin)/actions/admin-tracking.ts
@src/lib/seo.ts
@src/lib/db/schema.ts
@src/lib/audit.ts
@src/lib/site.ts

<interfaces>
<!-- Existing patterns the executor must replicate exactly -->

From src/app/(admin)/actions/admin-tracking.ts:
```typescript
// Pattern: requireAdmin() for auth guard
// Pattern: const KEYS array + get/save functions
// Pattern: createAuditLog() after save
async function requireAdmin() {
  const session = await auth.api.getSession({ headers: await headers() });
  if (!session) redirect("/login");
  const role = (session.user as Record<string, unknown>).role as string;
  if (role !== "admin" && role !== "super_admin") redirect("/admin/dashboard");
  return { session, userId: session.user.id, role };
}
```

From src/lib/db/schema.ts:
```typescript
export const settings = pgTable("settings", {
  key: text("key").primaryKey(),
  value: text("value").notNull(),
  updatedAt: timestamp("updated_at").notNull().defaultNow().$onUpdate(() => new Date()),
});
```

From src/lib/seo.ts:
```typescript
// Current hardcoded pageSeo record and createPageMetadata function
// This file must be enhanced to read DB first, fallback to hardcoded
export const pageSeo: Record<string, PageSeo> = { ... };
export function createPageMetadata(key: keyof typeof pageSeo, locale: string): Metadata { ... }
```

Settings keys for Phase 10 (from CONTEXT.md D-01):
- seo_title, seo_description, seo_keywords, seo_canonical_url, seo_separator, seo_robots_default, seo_og_image, seo_auto_meta
- seo_verify_google, seo_verify_bing, seo_verify_yandex, seo_verify_baidu, seo_verify_pinterest
- seo_sitemap_enabled, seo_sitemap_pages, seo_sitemap_blog, seo_sitemap_docs, seo_sitemap_landing, seo_sitemap_excludes, seo_sitemap_frequency
- seo_robots_txt, seo_ai_bots
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin-seo.ts server actions</name>
  <files>src/app/(admin)/actions/admin-seo.ts</files>
  <read_first>
    - src/app/(admin)/actions/admin-tracking.ts (pattern to replicate exactly)
    - src/lib/db/schema.ts (settings table shape)
    - src/lib/audit.ts (createAuditLog signature)
    - .planning/phases/10-core-seo/10-CONTEXT.md (all 22 settings keys from D-01 table)
  </read_first>
  <action>
Create `src/app/(admin)/actions/admin-seo.ts` as a "use server" module following the exact pattern from `admin-tracking.ts`:

1. Import `auth` from `@/lib/auth`, `headers` from `next/headers`, `redirect` from `next/navigation`, `db` from `@/lib/db`, `settings` from `@/lib/db/schema`, `eq`/`inArray` from `drizzle-orm`, `createAuditLog` from `@/lib/audit`.

2. Define `SEO_KEYS` const array with all 22 settings keys as strings:
```
seo_title, seo_description, seo_keywords, seo_canonical_url, seo_separator, seo_robots_default, seo_og_image, seo_auto_meta,
seo_verify_google, seo_verify_bing, seo_verify_yandex, seo_verify_baidu, seo_verify_pinterest,
seo_sitemap_enabled, seo_sitemap_pages, seo_sitemap_blog, seo_sitemap_docs, seo_sitemap_landing, seo_sitemap_excludes, seo_sitemap_frequency,
seo_robots_txt, seo_ai_bots
```

3. Define grouped key arrays for each sub-section:
   - `GENERAL_SEO_KEYS`: first 8 keys (seo_title through seo_auto_meta)
   - `VERIFICATION_SEO_KEYS`: next 5 keys (seo_verify_google through seo_verify_pinterest)
   - `SITEMAP_SEO_KEYS`: next 7 keys (seo_sitemap_enabled through seo_sitemap_frequency)
   - `ROBOTS_SEO_KEYS`: last 2 keys (seo_robots_txt, seo_ai_bots)

4. Define `SeoSettingsData` interface as `Record<string, string>` (all values are strings in the DB; boolean settings stored as "true"/"false").

5. Copy `requireAdmin()` function exactly from admin-tracking.ts (same auth check, same redirect logic).

6. Create `getSeoSettings(keys?: string[]): Promise<SeoSettingsData>`:
   - Call `requireAdmin()`
   - If `keys` provided, filter to those; otherwise use all `SEO_KEYS`
   - Query DB: `db.select().from(settings).where(inArray(settings.key, keys))`
   - Return map of key -> value (empty string if no DB row)

7. Create `saveSeoSettings(data: SeoSettingsData): Promise<{success: boolean}>`:
   - Call `requireAdmin()`
   - For each key in `data`, upsert: check if row exists, update or insert
   - Call `createAuditLog` with action `"admin.seo_settings_updated"`, targetType `"settings"`, targetId `"seo"`, details including which keys were updated
   - Return `{ success: true }`

8. Create `getSeoScore(): Promise<{filled: number, total: number, percentage: number}>`:
   - Call `requireAdmin()`
   - Load all SEO settings via `getSeoSettings()`
   - Count non-empty values (not "" and not undefined)
   - Return `{ filled, total: SEO_KEYS.length, percentage: Math.round((filled/total)*100) }`
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && npx tsc --noEmit src/app/\\(admin\\)/actions/admin-seo.ts 2>&1 | head -20 || echo "TypeScript check done"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `"use server"` directive at line 1
    - File exports `getSeoSettings`, `saveSeoSettings`, `getSeoScore`
    - File defines `SEO_KEYS` array with exactly 22 entries
    - File defines `GENERAL_SEO_KEYS`, `VERIFICATION_SEO_KEYS`, `SITEMAP_SEO_KEYS`, `ROBOTS_SEO_KEYS` grouped arrays
    - `requireAdmin()` function present with auth/role check
    - `saveSeoSettings` calls `createAuditLog` with action `"admin.seo_settings_updated"`
    - All DB queries use `settings` table from `@/lib/db/schema`
  </acceptance_criteria>
  <done>admin-seo.ts exports getSeoSettings, saveSeoSettings, getSeoScore with full typed CRUD for all 22 SEO settings keys, admin auth guard, and audit logging.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance seo.ts with DB fallback</name>
  <files>src/lib/seo.ts</files>
  <read_first>
    - src/lib/seo.ts (current hardcoded SEO utilities)
    - src/lib/site.ts (siteConfig with url, legalName)
    - src/app/(admin)/actions/admin-seo.ts (just created, for DB read pattern)
    - src/lib/db/schema.ts (settings table)
  </read_first>
  <action>
Modify `src/lib/seo.ts` to read SEO settings from the DB first, falling back to current hardcoded values when no DB row exists (per D-01).

1. Add imports at the top: `db` from `@/lib/db`, `settings` from `@/lib/db/schema`, `inArray` from `drizzle-orm`.

2. Create a `getCachedSeoOverrides()` async function:
   - Define the override keys: `seo_title`, `seo_description`, `seo_keywords`, `seo_canonical_url`, `seo_separator`, `seo_robots_default`, `seo_og_image`
   - Query DB for these keys using `inArray`
   - Return a `Record<string, string>` map (empty string for missing keys)
   - Wrap in try/catch: if DB fails (e.g., no connection), return empty map so hardcoded defaults take over

3. Create `getSeoSetting(key: string, fallback: string): Promise<string>` helper:
   - Calls `getCachedSeoOverrides()` internally
   - Returns `overrides[key]` if non-empty, otherwise returns `fallback`

4. Modify `createPageMetadata()` to be async (if not already) and use DB overrides:
   - `seo_title` override: if DB has a value, use it as a prefix/base for the title. Pattern: `${dbTitle} ${separator} ${pageSpecificTitle}` or just the page title if no DB override
   - `seo_description` override: use page-specific description as primary, no override needed (per-page descriptions are better than global)
   - `seo_canonical_url` override: use DB canonical URL as siteUrl base instead of hardcoded `siteConfig.url` if set
   - `seo_og_image` override: if DB has a value, set `openGraph.images` to that URL
   - Keep ALL existing hardcoded behavior as fallback -- this is the critical constraint. Every existing page must render identically if no DB rows exist.

5. Keep `pageSeo` record and all schema functions (`organizationSchema`, `websiteSchema`, `productSchema`, `breadcrumbSchema`) completely unchanged -- they don't need DB overrides.

6. Do NOT change any function signatures in a breaking way. The `createPageMetadata` function may become async but must still accept the same `(key, locale)` parameters.
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && npx tsc --noEmit src/lib/seo.ts 2>&1 | head -20 || echo "TypeScript check done"</automated>
  </verify>
  <acceptance_criteria>
    - `seo.ts` still exports `pageSeo`, `createPageMetadata`, `organizationSchema`, `websiteSchema`, `productSchema`, `breadcrumbSchema`
    - File imports `db` from `@/lib/db` and `settings` from `@/lib/db/schema`
    - `createPageMetadata` uses DB-read with hardcoded fallback (try/catch wrapping DB call)
    - All existing function signatures preserved (async addition is acceptable)
    - No existing hardcoded pageSeo values are removed or changed
  </acceptance_criteria>
  <done>seo.ts reads SEO overrides from DB (seo_title, seo_description, seo_canonical_url, seo_og_image, seo_separator) with full fallback to existing hardcoded values when DB has no rows or is unreachable. All existing page metadata generation continues working identically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin -> SEO server actions | Admin-only mutations crossing into shared settings table |
| seo.ts -> DB | Server-side DB read during page rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege | admin-seo.ts | mitigate | requireAdmin() enforces admin/super_admin role before any mutation |
| T-10-02 | Tampering | saveSeoSettings | mitigate | Input values are strings from controlled form; audit log records all changes with actor identity |
| T-10-03 | Denial of Service | getCachedSeoOverrides | accept | DB read on every page render is acceptable for admin-only SEO pages; try/catch fallback prevents outages |
</threat_model>

<verification>
1. TypeScript compilation passes for both new/modified files
2. All 22 SEO settings keys are defined in admin-seo.ts
3. seo.ts still exports all original functions unchanged
4. DB fallback pattern has try/catch so hardcoded defaults survive DB failures
</verification>

<success_criteria>
- admin-seo.ts created with getSeoSettings, saveSeoSettings, getSeoScore functions
- seo.ts enhanced with DB read and hardcoded fallback
- No existing page metadata behavior changes when no DB rows exist
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-seo/10-01-SUMMARY.md`
</output>
