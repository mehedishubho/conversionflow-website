---
phase: 05-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/navigation.ts
  - src/components/layout/Navbar.tsx
  - src/components/layout/Footer.tsx
autonomous: true
requirements:
  - DATA-07

must_haves:
  truths:
    - "Navbar reads navLinks from @/data/navigation, not inline array"
    - "Footer reads productLinks, companyLinks, legalLinks from @/data/navigation, not inline arrays"
    - "All pages render identically -- no visual or behavioral change"
  artifacts:
    - path: "src/data/navigation.ts"
      provides: "NavLink interface + navLinks, footerProductLinks, footerCompanyLinks, footerLegalLinks exports"
      exports: ["navLinks", "footerProductLinks", "footerCompanyLinks", "footerLegalLinks"]
  key_links:
    - from: "src/components/layout/Navbar.tsx"
      to: "src/data/navigation.ts"
      via: "import { navLinks } from '@/data/navigation'"
      pattern: "import.*navLinks.*from.*@/data/navigation"
    - from: "src/components/layout/Footer.tsx"
      to: "src/data/navigation.ts"
      via: "import { footerProductLinks, footerCompanyLinks, footerLegalLinks } from '@/data/navigation'"
      pattern: "import.*footerProductLinks.*from.*@/data/navigation"
---

<objective>
Extract all navigation link data from Navbar and Footer into a single src/data/navigation.ts data file, then update both components to import from it.

Purpose: Navigation data must be externalized for i18n readiness (Phase 9). Navbar navLinks and Footer link arrays are the highest-impact extraction since they appear on every page.
Output: src/data/navigation.ts created; Navbar.tsx and Footer.tsx updated to import from it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-layer/05-CONTEXT.md
@.planning/phases/05-data-layer/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create navigation.ts data file and update Navbar + Footer</name>
  <files>src/data/navigation.ts, src/components/layout/Navbar.tsx, src/components/layout/Footer.tsx</files>
  <read_first>
    src/components/layout/Navbar.tsx
    src/components/layout/Footer.tsx
    .planning/phases/05-data-layer/05-CONTEXT.md
  </read_first>
  <action>
    1. Create directory `src/data/` if it does not exist.

    2. Create `src/data/navigation.ts` with the following exact content (per D-01: co-located interface, D-02: named export, D-03: pure data only):

    ```typescript
    // src/data/navigation.ts

    interface NavLink {
      name: string;
      href: string;
    }

    export const navLinks: NavLink[] = [
      { name: "Home", href: "/" },
      { name: "Features", href: "/features" },
      { name: "Pricing", href: "/pricing" },
      { name: "Changelog", href: "/changelog" },
      { name: "Support", href: "/support" },
    ];

    export const footerProductLinks: NavLink[] = [
      { name: "Features", href: "/features" },
      { name: "Pricing", href: "/pricing" },
      { name: "Changelog", href: "/changelog" },
      { name: "Documentation", href: "#" },
    ];

    export const footerCompanyLinks: NavLink[] = [
      { name: "Devsroom", href: "https://devsroom.com" },
      { name: "WPMHS", href: "https://wpmhs.com" },
      { name: "Blog", href: "#" },
      { name: "Support", href: "/support" },
    ];

    export const footerLegalLinks: NavLink[] = [
      { name: "Privacy Policy", href: "#" },
      { name: "Terms of Service", href: "#" },
      { name: "Refund Policy", href: "#" },
      { name: "License Agreement", href: "#" },
    ];
    ```

    3. Update `src/components/layout/Navbar.tsx`:
       - REMOVE the inline `navLinks` array (lines 11-17)
       - ADD import at the top (after other imports): `import { navLinks } from "@/data/navigation";`

    4. Update `src/components/layout/Footer.tsx`:
       - REMOVE the three inline arrays: `productLinks` (lines 3-8), `companyLinks` (lines 10-15), `legalLinks` (lines 17-22)
       - ADD import at the top (after the Link import): `import { footerProductLinks, footerCompanyLinks, footerLegalLinks } from "@/data/navigation";`
       - In the Product column, replace `productLinks.map` with `footerProductLinks.map`
       - In the Company column, replace `companyLinks.map` with `footerCompanyLinks.map`
       - In the Legal column, replace `legalLinks.map` with `footerLegalLinks.map`
  </action>
  <acceptance_criteria>
    - `src/data/navigation.ts` exists and contains `interface NavLink`, `export const navLinks`, `export const footerProductLinks`, `export const footerCompanyLinks`, `export const footerLegalLinks`
    - `src/components/layout/Navbar.tsx` contains `import { navLinks } from "@/data/navigation"` and does NOT contain inline `const navLinks = [`
    - `src/components/layout/Footer.tsx` contains `import { footerProductLinks, footerCompanyLinks, footerLegalLinks } from "@/data/navigation"` and does NOT contain inline `const productLinks = [`, `const companyLinks = [`, or `const legalLinks = [`
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/Devsroom-Work/woobooster-website" && pnpm build</automated>
  </verify>
  <done>Navigation data fully extracted to src/data/navigation.ts. Navbar and Footer both import from @/data/navigation. Build passes. No inline link arrays remain in either component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only moves static data between files. No trust boundaries crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | S | navigation.ts | accept | Static data file, no user input, no authentication surface |
</threat_model>

<verification>
- `pnpm build` passes
- `grep -r "const navLinks" src/components/layout/Navbar.tsx` returns nothing (inline array removed)
- `grep -r "const productLinks" src/components/layout/Footer.tsx` returns nothing (inline array removed)
- `grep -r "from.*@/data/navigation" src/components/layout/` finds both files
</verification>

<success_criteria>
- src/data/navigation.ts exists with 4 named exports
- Navbar.tsx imports navLinks from @/data/navigation
- Footer.tsx imports all 3 footer link arrays from @/data/navigation
- pnpm build passes
- Dev server renders all pages identically
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-layer/05-01-SUMMARY.md`
</output>
