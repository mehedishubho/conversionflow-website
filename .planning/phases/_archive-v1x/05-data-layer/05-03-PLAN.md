---
phase: 05-data-layer
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/features.ts
  - src/data/testimonials.ts
  - src/data/support.ts
  - src/components/sections/FeaturesBento.tsx
  - src/components/sections/Testimonials.tsx
  - src/app/features/page.tsx
  - src/app/support/page.tsx
autonomous: true
requirements:
  - DATA-03
  - DATA-04
  - DATA-06

must_haves:
  truths:
    - "FeaturesBento reads feature modules from @/data/features, not inline JSX"
    - "Features page reads module detail data from @/data/features, not inline JSX"
    - "Testimonials reads testimonial data from @/data/testimonials, not inline JSX"
    - "Support page reads support channel data from @/data/support, not inline JSX"
    - "All pages render identically -- no visual or content change"
  artifacts:
    - path: "src/data/features.ts"
      provides: "FeatureModule interface + featureModules export (supports both bento summary and detail row fields)"
      exports: ["featureModules"]
    - path: "src/data/testimonials.ts"
      provides: "Testimonial interface + testimonials export"
      exports: ["testimonials"]
    - path: "src/data/support.ts"
      provides: "SupportChannel interface + supportChannels export"
      exports: ["supportChannels"]
  key_links:
    - from: "src/components/sections/FeaturesBento.tsx"
      to: "src/data/features.ts"
      via: "import { featureModules } from '@/data/features'"
      pattern: "import.*featureModules.*from.*@/data/features"
    - from: "src/components/sections/Testimonials.tsx"
      to: "src/data/testimonials.ts"
      via: "import { testimonials } from '@/data/testimonials'"
      pattern: "import.*testimonials.*from.*@/data/testimonials"
    - from: "src/app/features/page.tsx"
      to: "src/data/features.ts"
      via: "import { featureModules } from '@/data/features'"
      pattern: "import.*featureModules.*from.*@/data/features"
    - from: "src/app/support/page.tsx"
      to: "src/data/support.ts"
      via: "import { supportChannels } from '@/data/support'"
      pattern: "import.*supportChannels.*from.*@/data/support"
---

<objective>
Extract feature/module data, testimonial data, and support channel data from their inline locations into dedicated data files. Update all consuming components to import from the new data files.

Purpose: Features data is the most complex extraction -- it feeds both the homepage bento (summary cards) and the features page (detail rows with tracking panels and fraud boxes). A single source of truth in features.ts prevents data drift. Testimonials and support are straightforward content extractions for i18n readiness.
Output: 3 new data files (features.ts, testimonials.ts, support.ts); 4 updated consumer files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-layer/05-CONTEXT.md
@.planning/phases/05-data-layer/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create features.ts data file with unified module structure</name>
  <files>src/data/features.ts, src/components/sections/FeaturesBento.tsx, src/app/features/page.tsx</files>
  <read_first>
    src/components/sections/FeaturesBento.tsx
    src/app/features/page.tsx
    .planning/phases/05-data-layer/05-CONTEXT.md
    .planning/phases/05-data-layer/05-RESEARCH.md
  </read_first>
  <action>
    **Create `src/data/features.ts`** (per D-01, D-02, D-03)

    This is the most complex data file. It must accommodate both the homepage bento (icon, title, description, tags) and the features page detail rows (eyebrow, extended description, checks, visual components data). Per RESEARCH Pitfall 3, use a single file so components pick the fields they need.

    Define these interfaces:
    ```typescript
    interface FeatureTag {
      label: string;
    }

    interface TrackingPlatform {
      name: string;
      status: string;
    }

    interface FraudOrder {
      id: string;
      phone: string;
      status: string;
      statusClass: string;
      action: string;
    }

    interface FeatureModule {
      icon: string;
      title: string;
      description: string;
      tags: FeatureTag[];
      // Detail fields used by features page (optional since only 3 of 6 modules have detail rows)
      eyebrow?: string;
      detailDescription?: string;
      checks?: string[];
      trackingPlatforms?: TrackingPlatform[];
      fraudOrders?: FraudOrder[];
      fraudStats?: { blocked: number; protected: string };
    }
    ```

    Export `featureModules: FeatureModule[]` with all 6 modules. Extract data verbatim from:
    - `FeaturesBento.tsx` (lines 13-91): 6 module summaries (icon, title, description, tags)
    - `features/page.tsx` (lines 43-258): 3 detail rows (Module 01 Courier Sync, Module 02 Meta CAPI, Module 03 Fraud Shield) with eyebrow, detailDescription, checks, and visual component data

    For Module 01 (Courier Sync), the visual component shows courier cards and order flow -- this is UI chrome data, not business content. Keep the courier cards and flow data INLINE in the features page JSX (per RESEARCH Pitfall 5 -- decorative/illustrative data stays inline).

    For Module 02 (Meta CAPI), the tracking panel shows platform statuses -- include `trackingPlatforms` array with these exact values:
    - `{ name: "Meta Pixel", status: "Active + CAPI" }`
    - `{ name: "Google GA4", status: "Active" }`
    - `{ name: "TikTok Pixel", status: "Active" }`
    - `{ name: "Pinterest Tag", status: "Active" }`
    - `{ name: "Google Tag Manager", status: "Active" }`

    For Module 03 (Fraud Shield), include `fraudOrders` and `fraudStats` data from the fraud box.

    **Update `src/components/sections/FeaturesBento.tsx`:**
    - ADD import: `import { featureModules } from "@/data/features";`
    - Replace the 6 hardcoded `<div className="bc ...">` blocks with a `featureModules.map(...)` loop
    - Each iteration renders: `bc-icon` div with `module.icon`, `bc-title` with `module.title`, `bc-desc` with `module.description`, and tags from `module.tags.map(t => <span className="tag">{t.label}</span>)`
    - Preserve the first card's `w2` className by checking index: `className={`bc${index === 0 ? " w2" : ""}`}`

    **Update `src/app/features/page.tsx`:**
    - ADD import: `import { featureModules } from "@/data/features";`
    - Replace the 3 inline feat-row blocks with a loop over `featureModules.filter(m => m.eyebrow)` (only modules with detail data)
    - For each module, render the feat-row with: eyebrow from `module.eyebrow`, sec-title from `module.title`, description from `module.detailDescription`, checks from `module.checks.map(...)`, tags from `module.tags`
    - The visual component columns (courier cards, tracking panel, fraud box) are complex JSX with CSS class names -- keep them inline in the features page but wire them to the data where possible:
      - Module 02 tracking panel: loop over `module.trackingPlatforms` instead of hardcoded rows
      - Module 03 fraud box: loop over `module.fraudOrders` for the table rows, use `module.fraudStats` for the stats card
      - Module 01 courier cards and flow diagram: keep inline (decorative UI chrome)
    - Preserve the `rev` className on alternating rows: `className={`feat-row${index % 2 === 1 ? " rev" : ""}`}`
    - Keep the `feat-tabs` section inline -- the tabs are decorative (no filtering JS), just CSS active state
  </action>
  <acceptance_criteria>
    - `src/data/features.ts` exists with `interface FeatureTag`, `interface TrackingPlatform`, `interface FraudOrder`, `interface FeatureModule`, `export const featureModules: FeatureModule[]`
    - `src/components/sections/FeaturesBento.tsx` contains `import { featureModules } from "@/data/features"` and uses `.map()` to render bento cards
    - `src/app/features/page.tsx` contains `import { featureModules } from "@/data/features"` and does NOT contain hardcoded `Module 01`, `Module 02`, `Module 03` eyebrow divs (uses loop instead)
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/Devsroom-Work/woobooster-website" && pnpm build</automated>
  </verify>
  <done>Feature module data extracted to src/data/features.ts with unified interface supporting both bento and detail views. FeaturesBento and features page both import from @/data/features. Visual components in features page use data-driven loops where applicable. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create testimonials.ts and support.ts, update consumers</name>
  <files>src/data/testimonials.ts, src/data/support.ts, src/components/sections/Testimonials.tsx, src/app/support/page.tsx</files>
  <read_first>
    src/components/sections/Testimonials.tsx
    src/app/support/page.tsx
    .planning/phases/05-data-layer/05-CONTEXT.md
  </read_first>
  <action>
    **Create `src/data/testimonials.ts`** (per D-01, D-02, D-03)

    Extract testimonial data from `src/components/sections/Testimonials.tsx` (lines 10-54). The data file must:
    - Define `interface Testimonial` with fields: `stars: string`, `quote: string`, `initials: string`, `name: string`, `store: string`, `avatarColor: "bg-accent" | "bg-green" | "bg-orange"` (literal union)
    - Export as `export const testimonials: Testimonial[] = [...]`
    - Include all 3 testimonials with exact data:
      1. `{ stars: "★★★★★", quote: "আমাদের প্রতিদিন ৩০০+ অর্ডার আসে...", initials: "RA", name: "Rahim Ahmed", store: "StyleBD.com · Dhaka", avatarColor: "bg-accent" }`
      2. `{ stars: "★★★★★", quote: "The Fraud Shield alone saved us ৳25,000 last month...", initials: "TH", name: "Tanvir Hossain", store: "TechGear.com.bd · Chittagong", avatarColor: "bg-green" }`
      3. `{ stars: "★★★★★", quote: "Meta CAPI changed our ad results completely...", initials: "SK", name: "Sumaiya Khan", store: "HomeDecorBD · Sylhet", avatarColor: "bg-orange" }`

    **Create `src/data/support.ts`** (per D-01, D-02, D-03)

    Extract support channel data from `src/app/support/page.tsx` (lines 36-69). The data file must:
    - Define `interface SupportChannel` with fields: `icon: string`, `title: string`, `description: string`, `action: string`, `href: string`
    - Export as `export const supportChannels: SupportChannel[] = [...]`
    - Include all 3 channels with exact data:
      1. `{ icon: "📧", title: "Email Support", description: "Send us a detailed message and we'll respond within 24 hours, usually much faster.", action: "mhs@wpmhs.com", href: "mailto:mhs@wpmhs.com" }`
      2. `{ icon: "💬", title: "WhatsApp (BD)", description: "Bangladesh-based direct support via WhatsApp. Reply within business hours (9AM–6PM BST).", action: "+880 1721-328992", href: "#" }`
      3. `{ icon: "📖", title: "Documentation", description: "Step-by-step guides for every module — from first install to advanced CAPI configuration.", action: "Browse Docs →", href: "#" }`

    **Update `src/components/sections/Testimonials.tsx`:**
    - ADD import: `import { testimonials } from "@/data/testimonials";`
    - Replace the 3 hardcoded `<div className="tcard">` blocks with a `testimonials.map(...)` loop
    - Each iteration renders: stars from `t.stars`, quote from `t.quote`, initials from `t.initials`, name from `t.name`, store from `t.store`, avatarColor via `className={`tav ${t.avatarColor}`}`

    **Update `src/app/support/page.tsx`:**
    - ADD import: `import { supportChannels } from "@/data/support";`
    - Replace the 3 hardcoded `<div className="support-card">` blocks with a `supportChannels.map(...)` loop
    - Each iteration renders: sc-icon from `ch.icon`, sc-title from `ch.title`, sc-desc from `ch.description`, button text from `ch.action`, href from `ch.href`
    - Keep the contact form JSX inline -- the form is structural UI, not content data
    - Use `<a href={ch.href}>` for email channel (mailto link) and `<Link href={ch.href}>` for other channels. Determine which to use by checking if `ch.href` starts with `"mailto:"`
  </action>
  <acceptance_criteria>
    - `src/data/testimonials.ts` exists with `interface Testimonial`, `export const testimonials: Testimonial[]`
    - `src/data/support.ts` exists with `interface SupportChannel`, `export const supportChannels: SupportChannel[]`
    - `src/components/sections/Testimonials.tsx` contains `import { testimonials } from "@/data/testimonials"` and uses `.map()` to render testimonial cards
    - `src/app/support/page.tsx` contains `import { supportChannels } from "@/data/support"` and uses `.map()` to render support cards
    - Contact form remains inline in support page (not extracted)
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/Devsroom-Work/woobooster-website" && pnpm build</automated>
  </verify>
  <done>Testimonial and support data extracted to dedicated data files. Testimonials.tsx and support page both use data-driven loops. Contact form remains inline. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Static data extraction only. No trust boundaries crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | S | data files | accept | Static TypeScript data, no user input, no runtime mutations |
</threat_model>

<verification>
- `pnpm build` passes
- `grep "from.*@/data/features" src/components/sections/FeaturesBento.tsx` returns match
- `grep "from.*@/data/features" src/app/features/page.tsx` returns match
- `grep "from.*@/data/testimonials" src/components/sections/Testimonials.tsx` returns match
- `grep "from.*@/data/support" src/app/support/page.tsx` returns match
- All 7 data files exist in src/data/
</verification>

<success_criteria>
- 3 data files created (features.ts, testimonials.ts, support.ts)
- FeaturesBento uses featureModules.map() instead of hardcoded cards
- Features page uses featureModules.filter() for detail rows with data-driven tracking panel and fraud box
- Testimonials uses testimonials.map() instead of hardcoded cards
- Support page uses supportChannels.map() instead of hardcoded cards
- pnpm build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-layer/05-03-SUMMARY.md`
</output>
