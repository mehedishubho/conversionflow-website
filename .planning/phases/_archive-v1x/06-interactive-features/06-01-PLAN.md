---
phase: 06-interactive-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useCountUp.ts
  - src/components/sections/TrustBar.tsx
  - src/components/sections/HeroSection.tsx
autonomous: true
requirements:
  - HOME-10

must_haves:
  truths:
    - "All 5 TrustBar stats animate from 0 to their target number when scrolled into view"
    - "All 3 hero dashboard stat values (Revenue 42, Orders 834, Blocked 12) count up on scroll"
    - "Hero chart bars animate from 0 height to target heights with staggered delays"
    - "Animations trigger only once per page load and never re-trigger on re-scroll"
    - "Animation completes in 1.5 seconds with cubic ease-out deceleration"
  artifacts:
    - path: "src/hooks/useCountUp.ts"
      provides: "Reusable count-up animation hook with IntersectionObserver"
      exports: ["useCountUp"]
    - path: "src/components/sections/TrustBar.tsx"
      provides: "TrustBar using useCountUp for all 5 stats"
      contains: "useCountUp"
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero dashboard with count-up stats and animated chart bars"
      contains: "useCountUp"
  key_links:
    - from: "src/components/sections/TrustBar.tsx"
      to: "src/hooks/useCountUp.ts"
      via: "import { useCountUp }"
      pattern: "import.*useCountUp.*from.*@/hooks/useCountUp"
    - from: "src/components/sections/HeroSection.tsx"
      to: "src/hooks/useCountUp.ts"
      via: "import { useCountUp }"
      pattern: "import.*useCountUp.*from.*@/hooks/useCountUp"
    - from: "src/components/sections/HeroSection.tsx"
      to: "framer-motion"
      via: "motion.div scaleY animation on chart bars"
      pattern: "motion\\.div.*scaleY"
---

<objective>
Extract a reusable `useCountUp` hook from TrustBar's inline animation logic, then apply it to all 5 TrustBar stats and all 3 hero dashboard stats. Add framer-motion chart bar stagger animation to HeroSection.

Purpose: Stats and numbers animate from 0 to their final value on scroll, creating a polished data-driven feel per D-14 through D-18. The existing TrustBar only animates the first stat (500+ hardcoded). Generalizing into a reusable hook covers all stats efficiently.

Output: `src/hooks/useCountUp.ts` (new), updated `TrustBar.tsx`, updated `HeroSection.tsx`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-interactive-features/06-CONTEXT.md
@.planning/phases/06-interactive-features/06-RESEARCH.md
@src/lib/utils.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useCountUp hook and refactor TrustBar</name>
  <files>src/hooks/useCountUp.ts, src/components/sections/TrustBar.tsx</files>
  <read_first>
    - src/components/sections/TrustBar.tsx
    - src/hooks/ (directory check -- may not exist yet)
  </read_first>
  <action>
Create `src/hooks/useCountUp.ts` -- a generalized version of TrustBar's existing count-up animation logic.

The hook interface:
```typescript
"use client";
import { useState, useEffect, useRef } from "react";

interface UseCountUpOptions {
  target: number;
  duration?: number;   // default 1500 per D-16
  threshold?: number;  // default 0.3
}

export function useCountUp({ target, duration = 1500, threshold = 0.3 }: UseCountUpOptions) {
  // Returns { count, ref }
  // Uses IntersectionObserver on ref to trigger once
  // Uses requestAnimationFrame with cubic ease-out (1 - Math.pow(1 - progress, 3))
  // Guards against re-triggering with hasAnimated state
  // Disconnects observer after first trigger
}
```

Key behaviors (from TrustBar's existing working code at lines 29-43):
- IntersectionObserver watches the ref element
- On intersect, start rAF loop with cubic ease-out: `eased = 1 - Math.pow(1 - progress, 3)`
- `setCount(Math.floor(eased * target))` during animation, `setCount(target)` on completion
- hasAnimated guard prevents re-trigger (D-17, D-18)
- Return `{ count, ref }` -- formatting (suffix, prefix, display override) stays in the component

Then refactor `src/components/sections/TrustBar.tsx`:
- Remove the inline IntersectionObserver + rAF + hasAnimated + count state (lines 15-54)
- Import `useCountUp` from `@/hooks/useCountUp`
- Change the stats array to include numeric targets for all animated stats:
  ```
  { number: 500, label: "Active Stores", suffix: "+" },
  { number: 3, label: "BD Couriers" },
  { number: 6, label: "Tracking Platforms" },
  { number: 100, label: "CAPI Accuracy", suffix: "%" },
  { number: 0, label: "BDT Pricing", display: "৳৳৳" },
  ```
- Use a single container ref from one `useCountUp` call (target = 500 for the first stat) and individual `useState` counters per stat, OR observe a parent wrapper div and animate all stats together. The recommended approach per research: use a single IntersectionObserver on a parent wrapper div, then each stat with `animated: true` uses its own `useCountUp` hook call. BUT since `useCountUp` creates its own observer, a simpler approach: give the parent div a ref, manually observe it, and trigger all stat animations together. This avoids 5 separate observers.

Simpler approach: Create a `useCountUpGroup` or just handle it in the component:
- Use ONE `useCountUp` call for the primary stat (500+). For the other stats that need animation, they can each use their own `useCountUp` since they share the same viewport threshold and the observer disconnects after first trigger. With 5 stats this is fine -- the research pitfall about multiple observers is about pathological cases.

Actually, keep it simple: each stat that needs animation gets its own `useCountUp` hook call. The 4 non-500 stats (3, 6, 100) and the BDT display stat are small enough that multiple observers are fine.

For the `display: "৳৳৳"` stat -- it does not animate (display override), keep it as static display.

Update the rendering: Replace the current `{stat.animated ? \`${count}+\` : ...}` logic with individual hook results. Each stat item needs to be its own small component or use the hook at the TrustBar level for all stats.

Cleanest approach: Extract a `StatItem` sub-component that uses `useCountUp` for its own number. This avoids the rules-of-hooks issue of calling hooks conditionally.
  </action>
  <acceptance_criteria>
    - `src/hooks/useCountUp.ts` exists and exports `useCountUp` function
    - `useCountUp` accepts `{ target: number, duration?: number, threshold?: number }` options object
    - `useCountUp` returns `{ count: number, ref: React.RefObject<HTMLDivElement> }`
    - TrustBar.tsx imports `useCountUp` from `@/hooks/useCountUp`
    - TrustBar.tsx no longer has inline `IntersectionObserver` or `requestAnimationFrame` code
    - TrustBar.tsx no longer has hardcoded `500` in three places
    - All 5 TrustBar stats render correctly (500+, 3, 6, 100%, ৳৳৳)
    - `pnpm build` passes with exit code 0
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/Devsroom-Work/woobooster-website" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>useCountUp hook extracted and TrustBar refactored to use it. All 5 stats display correctly. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add count-up animations and chart bar stagger to HeroSection</name>
  <files>src/components/sections/HeroSection.tsx</files>
  <read_first>
    - src/components/sections/HeroSection.tsx
    - src/hooks/useCountUp.ts
  </read_first>
  <action>
Modify `src/components/sections/HeroSection.tsx` to add count-up animations to the 3 dashboard stats and staggered scaleY animation to the 7 chart bars.

**Dashboard stat animations (D-14):**
The 3 stats that need count-up:
- Revenue: target 42 (display "৳42L")
- Orders: target 834 (display "834")
- Blocked: target 12 (display "12")

Approach: Create a `DashStat` sub-component inside HeroSection.tsx that takes `{ target: number, label: string, className: string, formatter?: (count: number) => string }` and uses `useCountUp` internally. This avoids conditional hook calls.

The formatter functions:
- Revenue: `(count) => \`৳\${count}L\``
- Orders: `(count) => String(count)`
- Blocked: `(count) => String(count)`

Replace the 3 hardcoded `.ms-v` divs with `DashStat` components.

**Chart bar animation (D-15):**
Convert the 7 `<div className="cb ...">` elements to `<motion.div>` with:
- `initial={{ scaleY: 0 }}`
- `animate` triggered by framer-motion's `useInView` hook on the chart container
- `transition={{ duration: 0.6, delay: i * 0.08, ease: [0.22, 1, 0.36, 1] }}`
- `style={{ height: bar.height, transformOrigin: "bottom" }}`

The chart bars keep their existing height values (38%, 52%, 46%, 68%, 60%, 82%, 100%) -- scaleY animates from 0 to 1 while the height stays at target.

Import `useInView` from `framer-motion` for the chart container ref.
  </action>
  <acceptance_criteria>
    - HeroSection.tsx imports `useCountUp` from `@/hooks/useCountUp`
    - HeroSection.tsx imports `useInView` from `framer-motion`
    - Dashboard stats (Revenue 42, Orders 834, Blocked 12) use count-up animation
    - Revenue stat displays as "৳42L" at completion (formatter applied)
    - Chart bars use `<motion.div>` with `scaleY` animation
    - Chart bars have `transformOrigin: "bottom"` style
    - Chart bars have staggered delay (`i * 0.08`)
    - No layout shift during chart bar animation (scaleY, not height change)
    - `pnpm build` passes with exit code 0
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/Devsroom-Work/woobooster-website" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Hero dashboard stats animate from 0 to target on scroll. Chart bars animate with staggered scaleY from bottom. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Component | Scroll position triggers animation; no untrusted data input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | useCountUp | accept | Animation is purely cosmetic; worst case is no animation. No security impact. |
| T-06-02 | Tampering | useCountUp target | accept | Target values are hardcoded in source, not user-supplied. No runtime input. |
</threat_model>

<verification>
1. `pnpm build` passes with no errors
2. TrustBar stats render with correct final values: "500+", "3", "6", "100%", "৳৳৳"
3. Hero dashboard stats render: "৳42L", "834", "12"
4. Chart bars render at their target heights without layout shift
5. Animations only trigger once per page load
</verification>

<success_criteria>
- useCountUp hook extracted to src/hooks/useCountUp.ts and used by both TrustBar and HeroSection
- All 5 TrustBar stats display correctly (4 animated + 1 static display)
- All 3 hero dashboard stats animate from 0 to target with correct formatting
- Chart bars animate with staggered scaleY, transform-origin bottom, no layout shift
- Build passes cleanly
- HOME-10 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-interactive-features/06-01-SUMMARY.md`
</output>
