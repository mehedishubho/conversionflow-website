# Plan 03-01: Features Page

**Wave:** 1
**Objective:** Create the Features page at `/features` with page hero, feature tabs, and 3 feature rows (Courier Sync, Meta CAPI, Fraud Shield) porting from `woobooster-v2.html` lines 686-815.
**Requirements:** FEAT-01, FEAT-02, FEAT-03, FEAT-04
**Files Modified:**
- `src/app/features/page.tsx` (new)
- `src/app/globals.css` (add page-specific CSS classes)

## Tasks

### Task 1: Add Features page CSS classes to globals.css

**Action:** Add the following CSS classes to the `@layer utilities` block in `src/app/globals.css`, porting from `woobooster-v2.html` lines 337-345 (page hero), 376-380 (feature tabs), 213-263 (feature rows, tracking panel, fraud box):

- `.page-hero-sm` — small page hero with gradient background and radial glow
- `.feat-tabs` — feature category tabs container
- `.ftab` — individual tab pill
- `.feat-rows` — feature rows container
- `.feat-row` — individual feature row (2-column grid)
- `.fc` — feature content column
- `.fv` — feature visual column
- `.tp`, `.tp-head`, `.tp-row`, `.tp-name` — tracking panel styles
- `.ts-on`, `.ts-off` — tracking status badges
- `.fraud-box`, `.fb-head`, `.fb-row`, `.fid` — fraud box table styles
- `.blk-btn`, `.blkd-badge` — block button and blocked badge
- Responsive rules for `.feat-row` at 960px and 640px breakpoints

**read_first:**
- `src/app/globals.css` — see current utility classes and patterns
- `woobooster-v2.html` lines 337-345, 376-380, 213-263 — source CSS to port

**acceptance_criteria:**
- `src/app/globals.css` contains `.page-hero-sm` class
- `src/app/globals.css` contains `.feat-tabs` and `.ftab` classes
- `src/app/globals.css` contains `.feat-row` and `.feat-rows` classes
- `src/app/globals.css` contains `.tp`, `.tp-head`, `.tp-row` classes
- `src/app/globals.css` contains `.fraud-box`, `.fb-head`, `.fb-row` classes
- `src/app/globals.css` contains `.blk-btn` class
- `pnpm build` passes after changes

### Task 2: Create Features page at `src/app/features/page.tsx`

**Action:** Create `src/app/features/page.tsx` as a server component (default export) matching `woobooster-v2.html` lines 686-815. The page contains:

1. **Page Hero** (lines 687-693): Small hero with eyebrow "All Features", title "Everything WooBooster Does", subtitle about six modules
2. **Feature Tabs** (lines 730-737): Decorative tab bar showing "All Modules", "Courier Sync", "Tracking", "Fraud Shield", "Analytics", "Lead Recovery" — "All Modules" has active state
3. **Feature Row 1** (lines 739-763): "Module 01 — Automated Courier Sync" with courier cards visual panel and order flow visualization
4. **Feature Row 2** (lines 764-786): "Module 02 — Meta Pixel + CAPI Tracking" with tracking hub panel (reversed layout)
5. **Feature Row 3** (lines 787-812): "Module 03 — Fraud Shield & Security" with fraud box table and blocked stats

Each feature row has: eyebrow label, title, description paragraph, checkmark list, tags, and a visual panel.

**read_first:**
- `woobooster-v2.html` lines 686-815 — complete features page template
- `src/app/globals.css` — available CSS classes
- `src/components/sections/FeaturesBento.tsx` — pattern reference
- `src/components/sections/BDSection.tsx` — courier card pattern reference

**acceptance_criteria:**
- `src/app/features/page.tsx` exists with `export default function Features()`
- File contains page hero with eyebrow "All Features"
- File contains 3 feature rows with titles "Automated Courier Sync", "Meta Pixel + CAPI Tracking", "Fraud Shield & Security"
- File contains feature tabs bar
- File contains courier card visual elements
- File contains tracking hub panel with platform rows
- File contains fraud box table with order rows
- Uses `@/` imports if any
- Uses design tokens (no hardcoded hex colors)
