# Plan 03-03: Changelog and Support Pages

**Wave:** 1
**Depends on:** None (parallel with 03-01 and 03-02)
**Objective:** Create the Changelog page at `/changelog` and Support page at `/support`, porting from `woobooster-v2.html` lines 905-986.
**Requirements:** CHNG-01, CHNG-02, CHNG-03, SUPP-01, SUPP-02, SUPP-03
**Files Modified:**
- `src/app/changelog/page.tsx` (new)
- `src/app/support/page.tsx` (new)
- `src/app/globals.css` (add changelog and support CSS classes)

## Tasks

### Task 1: Add Changelog and Support CSS classes to globals.css

**Action:** Add the following CSS classes to the `@layer utilities` block in `src/app/globals.css`, porting from `woobooster-v2.html` lines 347-374 (changelog and support):

**Changelog classes (from lines 347-359):**
- `.clog-list` — changelog list container (flex column, gap)
- `.clog-item` — individual changelog entry card
- `.clog-v` — version badge (accent background, white text)
- `.clog-date` — release date text
- `.clog-name` — version name heading
- `.clog-changes` — changes list container
- `.clog-tag` — change type tag (inline-flex)
- `.ct-new` — "New" tag (green)
- `.ct-fix` — "Fix" tag (orange)
- `.ct-imp` — "Improved" tag (accent)
- `.clog-entry` — individual change entry

**Support classes (from lines 362-374):**
- `.support-grid` — 3-column support cards grid
- `.support-card` — individual support card
- `.sc-icon` — support card icon
- `.sc-title` — support card title
- `.sc-desc` — support card description
- `.contact-form` — contact form container
- `.form-grid` — 2-column form grid
- `.form-group` — form field group
- `.form-input` — form input/textarea styling
- Responsive rules for `.support-grid` at 960px and `.form-grid` at 640px

**read_first:**
- `src/app/globals.css` — current utility classes
- `woobooster-v2.html` lines 347-374 — changelog and support CSS

**acceptance_criteria:**
- `src/app/globals.css` contains `.clog-list`, `.clog-item`, `.clog-v` classes
- `src/app/globals.css` contains `.clog-tag`, `.ct-new`, `.ct-fix`, `.ct-imp` classes
- `src/app/globals.css` contains `.support-grid`, `.support-card` classes
- `src/app/globals.css` contains `.contact-form`, `.form-grid`, `.form-group`, `.form-input` classes
- `pnpm build` passes

### Task 2: Create Changelog page at `src/app/changelog/page.tsx`

**Action:** Create `src/app/changelog/page.tsx` as a server component (default export) matching `woobooster-v2.html` lines 905-953. The page contains:

1. **Page Hero** (lines 906-912): Small hero with eyebrow "Changelog", title "What's New in WooBooster", subtitle
2. **Changelog List** (lines 915-950): 3 version entries, newest first:
   - v0.0.14 "Analytics Suite Release" (May 2025): 3 New, 1 Improved, 1 Fixed
   - v0.0.13 "Fraud Shield & CAPI Events" (April 2025): 3 New, 1 Improved (version badge uses surface2 styling)
   - v0.0.12 "RedX Integration & Lead Capture" (March 2025): 3 New, 1 Fixed (version badge uses surface2 styling)

**read_first:**
- `woobooster-v2.html` lines 905-953 — complete changelog page template
- `src/app/globals.css` — available CSS classes (after Task 1)

**acceptance_criteria:**
- `src/app/changelog/page.tsx` exists with `export default function Changelog()`
- Contains page hero with "Changelog" eyebrow
- Contains 3 version entries (v0.0.14, v0.0.13, v0.0.12)
- Each entry has version badge, date, name, and categorized changes
- Change types use `.ct-new`, `.ct-imp`, `.ct-fix` tags
- Uses design tokens (no hardcoded hex colors)

### Task 3: Create Support page at `src/app/support/page.tsx`

**Action:** Create `src/app/support/page.tsx` as a server component (default export) matching `woobooster-v2.html` lines 955-986. The page contains:

1. **Page Hero** (lines 956-962): Small hero with eyebrow "Support", title "We've Got Your Back", subtitle
2. **Support Cards** (lines 965-969): 3 support method cards:
   - Email Support: mhs@wpmhs.com
   - WhatsApp (BD): +880 1721-328992
   - Documentation: Browse Docs →
3. **Contact Form** (lines 970-983): Section header "Send Us a Message" + form with fields: Name, Email, License Key, Subject, Message textarea, Submit button

**read_first:**
- `woobooster-v2.html` lines 955-986 — complete support page template
- `src/app/globals.css` — available CSS classes (after Task 1)

**acceptance_criteria:**
- `src/app/support/page.tsx` exists with `export default function Support()`
- Contains page hero with "Support" eyebrow
- Contains 3 support cards (Email, WhatsApp, Documentation)
- Email card links to `mailto:mhs@wpmhs.com`
- Contains contact form with 5 fields (Name, Email, License Key, Subject, Message)
- Uses `.btn-primary` for submit button
- Uses design tokens (no hardcoded hex colors)
