---
phase: 10-polish-and-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/ScrollReveal.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - FOUND-09
must_haves:
  truths:
    - "Only one ScrollReveal component exists (layout/ version)"
    - "No legacy CSS .sr classes remain in globals.css"
    - "All ScrollReveal imports resolve to layout/ScrollReveal"
  artifacts:
    - path: "src/components/layout/ScrollReveal.tsx"
      provides: "whileInView fade-up wrapper"
      contains: "whileInView"
    - path: "src/app/globals.css"
      provides: "Clean CSS without legacy .sr classes"
      not_contains: ".sr { opacity: 0"
  key_links:
    - from: "src/components/layout/ScrollReveal.tsx"
      to: "framer-motion"
      via: "motion.div with whileInView"
      pattern: "whileInView"
---

<objective>
Delete the duplicate ScrollReveal component in sections/ and remove legacy CSS scroll-reveal classes.

Purpose: Consolidate to a single ScrollReveal implementation (layout/ version using whileInView) and remove dead CSS code that is fully replaced by Framer Motion.
Output: Clean codebase with one ScrollReveal component and no legacy CSS animation classes.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-polish-and-enhancements/10-CONTEXT.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/layout/ScrollReveal.tsx (KEEP this):
```tsx
"use client";
import { motion } from "framer-motion";
import { cn } from "@/lib/utils";

export function ScrollReveal({
  children,
  className,
  delay = 0,
  yOffset = 32,
}: {
  children: React.ReactNode;
  className?: string;
  delay?: number;
  yOffset?: number;
}) {
  return (
    <motion.div
      initial={{ opacity: 0, y: yOffset }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: "-60px" }}
      transition={{ duration: 0.65, delay, ease: [0.22, 1, 0.36, 1] }}
      className={cn(className)}
    >
      {children}
    </motion.div>
  );
}
```

From src/components/sections/ScrollReveal.tsx (DELETE this):
```tsx
"use client";
import { useRef } from "react";
import { motion, useInView } from "framer-motion";

interface ScrollRevealProps {
  children: React.ReactNode;
  className?: string;
  delay?: number;
}

export function ScrollReveal({ children, className, delay = 0 }: ScrollRevealProps) {
  const ref = useRef<HTMLDivElement>(null);
  const isInView = useInView(ref, { once: true, margin: "-80px" });
  // ...
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete duplicate ScrollReveal and verify no imports break</name>
  <files>src/components/sections/ScrollReveal.tsx</files>
  <action>
    1. Verify no file imports from `@/components/sections/ScrollReveal` (grep confirms zero matches already)
    2. Delete `src/components/sections/ScrollReveal.tsx`
    3. Run `pnpm build` to verify no import resolution errors
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>sections/ScrollReveal.tsx is deleted, build passes with no import errors</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy CSS scroll-reveal classes from globals.css</name>
  <files>src/app/globals.css</files>
  <action>
    Remove the following CSS block from globals.css (lines ~466-469):
    ```css
    .sr { opacity: 0; transform: translateY(32px); transition: opacity 0.65s ease, transform 0.65s cubic-bezier(0.22, 1, 0.36, 1); }
    .sr.vis { opacity: 1; transform: none; }
    .sr-d1 { transition-delay: 0.08s; } .sr-d2 { transition-delay: 0.16s; } .sr-d3 { transition-delay: 0.24s; } .sr-d4 { transition-delay: 0.32s; }
    ```
    Also remove the section comment `/* — SCROLL REVEAL — */` above it.
    
    No component uses these classes (grep confirms zero matches for `className.*\bsr\b`).
    Framer Motion ScrollReveal component fully replaces this functionality.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Legacy .sr CSS classes removed from globals.css, build passes</done>
</task>

</tasks>

<verification>
- `pnpm build` completes without errors
- Only `src/components/layout/ScrollReveal.tsx` exists (no sections/ duplicate)
- No `.sr` or `.sr.vis` or `.sr-d*` classes in globals.css
- Homepage still renders ScrollReveal animations correctly
</verification>

<success_criteria>
- Single ScrollReveal component at layout/ScrollReveal.tsx
- Zero legacy CSS scroll-reveal classes in globals.css
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-and-enhancements/10-01-SUMMARY.md`
</output>
