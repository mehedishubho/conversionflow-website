---
phase: 10-polish-and-enhancements
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/app/[locale]/features/page.tsx
  - src/app/[locale]/pricing/page.tsx
  - src/app/[locale]/changelog/page.tsx
  - src/app/[locale]/support/page.tsx
  - src/app/[locale]/blog/page.tsx
  - src/app/[locale]/docs/page.tsx
  - src/app/[locale]/privacy/page.tsx
  - src/app/[locale]/terms/page.tsx
  - src/app/[locale]/refund/page.tsx
  - src/app/[locale]/license/page.tsx
autonomous: true
requirements:
  - FOUND-09
must_haves:
  truths:
    - "All content pages have ScrollReveal wrapping on below-the-fold sections"
    - "PageTransition works correctly with /en/* and /bn/* locale routes"
    - "Scroll animations are consistent: fade-up with yOffset=32, duration=0.65"
  artifacts:
    - path: "src/app/[locale]/features/page.tsx"
      provides: "Features page with ScrollReveal on below-fold content"
      contains: "ScrollReveal"
    - path: "src/app/[locale]/pricing/page.tsx"
      provides: "Pricing page with ScrollReveal on below-fold content"
      contains: "ScrollReveal"
    - path: "src/app/[locale]/changelog/page.tsx"
      provides: "Changelog page with ScrollReveal on below-fold content"
      contains: "ScrollReveal"
    - path: "src/app/[locale]/support/page.tsx"
      provides: "Support page with ScrollReveal on below-fold content"
      contains: "ScrollReveal"
    - path: "src/app/[locale]/blog/page.tsx"
      provides: "Blog page with ScrollReveal on below-fold content"
      contains: "ScrollReveal"
    - path: "src/app/[locale]/docs/page.tsx"
      provides: "Docs page with ScrollReveal on below-fold content"
      contains: "ScrollReveal"
  key_links:
    - from: "src/app/[locale]/features/page.tsx"
      to: "src/components/layout/ScrollReveal.tsx"
      via: "import and wrap below-fold section"
      pattern: "import.*ScrollReveal.*from.*layout/ScrollReveal"
---

<objective>
Add ScrollReveal fade-up animations to all below-the-fold sections across every content page.

Purpose: The homepage already wraps all sections with ScrollReveal for consistent fade-up on scroll. Content pages (Features, Pricing, Changelog, Support, Blog, Docs, Legal) currently have no ScrollReveal wrapping at the page level, creating an inconsistent experience. This plan adds the same treatment to all pages.
Output: Every page has consistent scroll-reveal animations on below-the-fold content.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-polish-and-enhancements/10-CONTEXT.md
@src/app/[locale]/page.tsx
@src/components/layout/ScrollReveal.tsx
</context>

<interfaces>
<!-- ScrollReveal component API (from layout/ScrollReveal.tsx): -->

```tsx
export function ScrollReveal({
  children,
  className,
  delay = 0,
  yOffset = 32,
}: {
  children: React.ReactNode;
  className?: string;
  delay?: number;
  yOffset?: number;
})
```

<!-- Homepage usage pattern (src/app/[locale]/page.tsx): -->

```tsx
import { ScrollReveal } from "@/components/layout/ScrollReveal";

// HeroSection is above the fold — no wrapper
<HeroSection />

// All below-fold sections wrapped
<ScrollReveal>
  <TrustBar />
</ScrollReveal>
<ScrollReveal>
  <FeaturesBento />
</ScrollReveal>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add ScrollReveal to Features, Pricing, Changelog, and Support pages</name>
  <files>
    src/app/[locale]/features/page.tsx,
    src/app/[locale]/pricing/page.tsx,
    src/app/[locale]/changelog/page.tsx,
    src/app/[locale]/support/page.tsx
  </files>
  <action>
    For each of these 4 page files, wrap the client component import with ScrollReveal at the page level.
    
    Pattern to apply (same as homepage):
    ```tsx
    import { ScrollReveal } from "@/components/layout/ScrollReveal";
    // ... existing imports
    
    export default async function XxxPage({ params }: { ... }) {
      return (
        <ScrollReveal>
          <XxxClient params={params} />
        </ScrollReveal>
      );
    }
    ```
    
    IMPORTANT: The page-hero section inside each client component is above the fold and should NOT get a separate ScrollReveal. By wrapping the entire client component at the page level, the internal whileInView animations on individual elements still work (they fire independently). The ScrollReveal wrapper adds a consistent container-level fade-up for the below-fold content area.
    
    Keep `generateStaticParams` exports unchanged.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Features, Pricing, Changelog, and Support pages each wrap their client component with ScrollReveal</done>
</task>

<task type="auto">
  <name>Task 2: Add ScrollReveal to Blog, Docs, and Legal pages</name>
  <files>
    src/app/[locale]/blog/page.tsx,
    src/app/[locale]/docs/page.tsx,
    src/app/[locale]/privacy/page.tsx,
    src/app/[locale]/terms/page.tsx,
    src/app/[locale]/refund/page.tsx,
    src/app/[locale]/license/page.tsx
  </files>
  <action>
    For Blog and Docs pages: wrap the below-fold content section with ScrollReveal at the page level.
    
    For Legal pages (privacy, terms, refund, license): wrap the `<LegalLayout>` component with ScrollReveal. LegalLayout is a server component so this works directly.
    
    Pattern for Blog/Docs:
    ```tsx
    import { ScrollReveal } from "@/components/layout/ScrollReveal";
    // ... existing imports
    
    export default async function XxxPage({ params }: { ... }) {
      // ... existing data fetching
      return (
        <ScrollReveal>
          <XxxClient ... />
        </ScrollReveal>
      );
    }
    ```
    
    Pattern for Legal pages:
    ```tsx
    import { ScrollReveal } from "@/components/layout/ScrollReveal";
    
    export default async function XxxPage({ params }: { ... }) {
      await params;
      return (
        <ScrollReveal>
          <LegalLayout title="..." lastUpdated="...">
            {/* content */}
          </LegalLayout>
        </ScrollReveal>
      );
    }
    ```
    
    Keep `generateStaticParams` exports unchanged.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Blog, Docs, and all 4 Legal pages wrap their content with ScrollReveal</done>
</task>

</tasks>

<verification>
- `pnpm build` completes without errors
- All 10 content page files import ScrollReveal from `@/components/layout/ScrollReveal`
- Navigate to each page and verify fade-up animation triggers on scroll
- Locale routes (/en/features, /bn/features etc.) work correctly with PageTransition
</verification>

<success_criteria>
- All content pages (Features, Pricing, Changelog, Support, Blog, Docs, Privacy, Terms, Refund, License) have ScrollReveal wrapping
- PageTransition animates correctly between locale routes
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-and-enhancements/10-02-SUMMARY.md`
</output>
