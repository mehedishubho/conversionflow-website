---
phase: 10-polish-and-enhancements
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/app/globals.css
  - src/components/layout/Navbar.tsx
  - src/components/layout/Footer.tsx
  - src/components/sections/HeroSection.tsx
  - src/components/sections/BDSection.tsx
  - src/components/sections/FeaturesBento.tsx
  - src/components/sections/Testimonials.tsx
  - src/components/sections/CTASection.tsx
  - src/components/sections/HowItWorks.tsx
  - src/components/sections/VideoSection.tsx
  - src/components/sections/TrustBar.tsx
  - src/components/features/FeaturesClient.tsx
  - src/components/pricing/PricingClient.tsx
  - src/components/changelog/ChangelogClient.tsx
  - src/components/support/SupportClient.tsx
  - src/components/blog/BlogPageClient.tsx
  - src/components/docs/DocsClient.tsx
  - src/app/[locale]/not-found.tsx
  - src/app/[locale]/docs/[slug]/layout.tsx
autonomous: true
requirements:
  - FOUND-05
must_haves:
  truths:
    - "Container max-width is 1280px across the entire site"
    - "All pages verified responsive at 960px (tablet) and 640px (mobile) breakpoints"
    - "No layout overflow or horizontal scroll at any breakpoint"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Updated container class with 1280px max-width"
      contains: "max-width: 1280px"
  key_links:
    - from: "src/app/globals.css"
      to: "all component files"
      via: "consistent max-width token"
      pattern: "max-w-\\[1280px\\]"
---

<objective>
Widen the site container from 1160px to 1280px and verify responsive behavior across all breakpoints.

Purpose: More breathing room on larger screens. The current 1160px container feels tight on 1440px+ monitors. Widening to 1280px gives content more space while maintaining readability.
Output: All 27 container references updated to 1280px, responsive layout verified at 960px and 640px.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-polish-and-enhancements/10-CONTEXT.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update container max-width from 1160px to 1280px everywhere</name>
  <files>
    src/app/globals.css,
    src/components/layout/Navbar.tsx,
    src/components/layout/Footer.tsx,
    src/components/sections/HeroSection.tsx,
    src/components/sections/BDSection.tsx,
    src/components/sections/FeaturesBento.tsx,
    src/components/sections/Testimonials.tsx,
    src/components/sections/CTASection.tsx,
    src/components/sections/HowItWorks.tsx,
    src/components/sections/VideoSection.tsx,
    src/components/sections/TrustBar.tsx,
    src/components/features/FeaturesClient.tsx,
    src/components/pricing/PricingClient.tsx,
    src/components/changelog/ChangelogClient.tsx,
    src/components/support/SupportClient.tsx,
    src/components/blog/BlogPageClient.tsx,
    src/components/docs/DocsClient.tsx,
    src/app/[locale]/not-found.tsx,
    src/app/[locale]/docs/[slug]/layout.tsx
  </files>
  <action>
    1. In `src/app/globals.css`, update the `.container` class:
       - Change `max-width: 1160px` to `max-width: 1280px` (line ~235)
    
    2. In ALL component files listed above, do a find-and-replace:
       - Replace `max-w-[1160px]` with `max-w-[1280px]`
       - This is a direct string replacement, no logic changes
    
    There are 27 instances across 19 files. Every instance follows the same pattern:
    `className="max-w-[1160px] mx-auto px-7 ..."` or similar.
    
    The Navbar has 2 instances (placeholder div + nav element).
    The Footer has 3 instances (top, achievements, bottom).
    All other components have 1-2 instances each.
    
    IMPORTANT: Do NOT change any other Tailwind classes or component logic. This is a pure find-and-replace of the max-width value.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>All 27 instances of max-w-[1160px] replaced with max-w-[1280px], container CSS class updated, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Responsive audit at 960px and 640px breakpoints</name>
  <files>
    src/app/globals.css
  </files>
  <action>
    After widening the container, audit the responsive CSS in globals.css for potential issues:
    
    1. Check the `@media (max-width: 960px)` block (line ~527-545):
       - Verify `.bento` grid (2 columns) still works at 960px with wider container
       - Verify `.feat-row` gap reduction is sufficient
       - Verify `.price-grid` single column still centers properly
       - Verify `.footer-grid` 2-column layout doesn't overflow
    
    2. Check the `@media (max-width: 640px)` block (line ~546-566):
       - Verify single-column layouts don't have horizontal overflow
       - Verify `.cta-wrap` padding is sufficient
       - Verify `.contact-form` padding works
    
    3. Check the Footer responsive block (line ~617-630):
       - Verify `.ft-grid` 2-column layout works at 960px
       - Verify `.ft-ach-grid` 3-column layout works at 960px
    
    4. Check the Navbar at narrow widths:
       - The Navbar uses `max-w-[1280px]` for the nav bar itself
       - At 960px and below, verify the desktop nav links are hidden (md:flex = 768px+)
       - At 640px and below, verify mobile menu button is visible
    
    If any layout issues are found, adjust padding, gap, or grid values in the responsive media queries.
    Do NOT add new breakpoints (per D-13).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Responsive layouts verified at 960px and 640px, no horizontal overflow, build passes</done>
</task>

</tasks>

<verification>
- `grep -r "1160px" src/` returns zero matches
- `pnpm build` completes without errors
- Container is visibly wider on desktop (1280px vs 1160px)
- No horizontal scrollbar at 960px or 640px viewport widths
</verification>

<success_criteria>
- All container references use 1280px
- No layout breakage at tablet (960px) or mobile (640px)
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-and-enhancements/10-03-SUMMARY.md`
</output>
