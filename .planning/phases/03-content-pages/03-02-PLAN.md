# Plan 03-02: Pricing Page

**Wave:** 1
**Depends on:** None (parallel with 03-01)
**Objective:** Create the Pricing page at `/pricing` with pricing tiers, comparison features, trust strip, and FAQ accordion porting from `woobooster-v2.html` lines 817-903.
**Requirements:** PRIC-01, PRIC-02, PRIC-03, PRIC-04, PRIC-05
**Files Modified:**
- `src/app/pricing/page.tsx` (new)
- `src/app/globals.css` (add pricing and FAQ CSS classes)

## Tasks

### Task 1: Add Pricing and FAQ CSS classes to globals.css

**Action:** Add the following CSS classes to the `@layer utilities` block in `src/app/globals.css`, porting from `woobooster-v2.html` lines 286-313 (pricing grid) and 303-313 (FAQ):

- `.price-grid` — 3-column pricing grid
- `.pc` — pricing card with border, padding, hover effects
- `.pop` — popular card variant with accent border and glow
- `.pop-tag` — "MOST POPULAR" positioned tag
- `.p-plan` — plan name label
- `.p-price` — price display (large Syne font)
- `.p-bdt` — BDT equivalent text
- `.p-desc` — plan description
- `.p-features` — features list
- `.p-ck` — checkmark icon (green)
- `.p-no` — missing feature (muted)
- `.trust-strip` — trust signals strip below pricing
- `.ts-it` — individual trust item
- `.faq-list` — FAQ container
- `.fi` — FAQ item
- `.fi-q` — FAQ question
- `.fi-ic` — FAQ icon (+)
- `.fi-a` — FAQ answer container (max-height animation)
- `.fi-a-in` — FAQ answer inner content
- `.fi.open` — open state for FAQ item
- Responsive rules for `.price-grid` at 960px breakpoint

**read_first:**
- `src/app/globals.css` — current utility classes
- `woobooster-v2.html` lines 286-313 — pricing and FAQ CSS

**acceptance_criteria:**
- `src/app/globals.css` contains `.price-grid` class
- `src/app/globals.css` contains `.pc` and `.pop` classes
- `src/app/globals.css` contains `.p-plan`, `.p-price`, `.p-features` classes
- `src/app/globals.css` contains `.trust-strip` and `.ts-it` classes
- `src/app/globals.css` contains `.faq-list`, `.fi`, `.fi-q`, `.fi-a`, `.fi-a-in` classes
- `pnpm build` passes

### Task 2: Create FAQ Accordion client component

**Action:** Create `src/components/sections/FAQAccordion.tsx` as a client component (`"use client"`) that renders the FAQ section from `woobooster-v2.html` lines 893-900. The component has:

- 5 FAQ items matching the HTML reference exactly:
  1. "Is this a one-time payment or subscription?"
  2. "Which couriers are supported?"
  3. "Do I need a developer?"
  4. "Can I pay in BDT using bKash or Nagad?"
  5. "What is your refund policy?"
- Click-to-toggle accordion behavior (click question to expand/collapse)
- Only one FAQ open at a time (accordion pattern)
- Smooth max-height CSS transition matching the HTML reference

**read_first:**
- `woobooster-v2.html` lines 893-900 — FAQ content and structure
- `woobooster-v2.html` lines 1186-1200 — FAQ accordion JavaScript behavior
- `src/components/layout/Navbar.tsx` — client component pattern reference

**acceptance_criteria:**
- `src/components/sections/FAQAccordion.tsx` exists with `"use client"` directive
- Component exports `FAQAccordion` named function
- Contains 5 FAQ questions and answers matching HTML reference
- Uses `useState` for open item tracking
- First FAQ item is open by default
- Uses existing `.faq-list`, `.fi`, `.fi-q`, `.fi-a` CSS classes

### Task 3: Create Pricing page at `src/app/pricing/page.tsx`

**Action:** Create `src/app/pricing/page.tsx` as a server component (default export) matching `woobooster-v2.html` lines 817-903. The page contains:

1. **Page Hero** (lines 818-824): Small hero with eyebrow "Pricing", title "Simple, One-Time Pricing", subtitle about no subscriptions
2. **Pricing Grid** (lines 827-880): 3 pricing cards:
   - Starter: $29 / ৳3,499, 1 site, 1 year updates, email support, outline button
   - Professional: $69 / ৳8,299, 3 sites, lifetime updates, priority + WhatsApp support, primary button, "MOST POPULAR" tag
   - Agency: $129 / ৳15,499, unlimited sites, lifetime updates, priority WhatsApp, white-label, outline button
3. **Trust Strip** (lines 881-887): 5 trust signals
4. **FAQ Section** (lines 888-900): Uses `FAQAccordion` client component

**read_first:**
- `woobooster-v2.html` lines 817-903 — complete pricing page template
- `src/components/sections/FAQAccordion.tsx` — FAQ component (created in Task 2)
- `src/app/globals.css` — available pricing CSS classes (after Task 1)

**acceptance_criteria:**
- `src/app/pricing/page.tsx` exists with `export default function Pricing()`
- Contains page hero with "Pricing" eyebrow
- Contains 3 pricing cards with correct prices ($29, $69, $129)
- Professional card has "MOST POPULAR" tag and accent styling
- Each card has feature list with checkmarks and one missing feature (Starter: no Priority Support)
- Trust strip shows 5 items
- Imports and renders `FAQAccordion` component
- Uses `@/` imports
- Uses design tokens (no hardcoded hex colors)
