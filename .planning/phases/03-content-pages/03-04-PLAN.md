# Plan 03-04: Navigation Verification and Theme Testing

**Wave:** 2
**Depends on:** 03-01, 03-02, 03-03
**Objective:** Verify all navigation links work end-to-end, test dark/light mode on all pages, test mobile menu functionality. Run build and lint checks.
**Requirements:** NAV-01, NAV-03, NAV-04, THEME-01, THEME-02, THEME-03
**Files Modified:**
- None (verification only, unless issues found)

## Tasks

### Task 1: Verify navigation and run build/lint

**Action:** Run verification checks:

1. Run `pnpm build` — must succeed with no errors
2. Run `pnpm lint` — must pass with no errors
3. Verify all 5 routes exist:
   - `src/app/page.tsx` (Home)
   - `src/app/features/page.tsx` (Features)
   - `src/app/pricing/page.tsx` (Pricing)
   - `src/app/changelog/page.tsx` (Changelog)
   - `src/app/support/page.tsx` (Support)
4. Verify Navbar links match route directories
5. Verify Footer links resolve to actual pages
6. Verify all pages use design tokens (no hardcoded hex colors)
7. Verify all pages have proper TypeScript types (no `any`)

**read_first:**
- `src/components/layout/Navbar.tsx` — verify navLinks href values
- `src/components/layout/Footer.tsx` — verify footer link href values
- All page.tsx files — verify they exist and export default functions

**acceptance_criteria:**
- `pnpm build` exits with code 0
- `pnpm lint` exits with code 0
- All 5 page files exist at expected paths
- All nav links in Navbar.tsx match existing routes
- No hardcoded hex color values in page components (use CSS variables/tokens)
