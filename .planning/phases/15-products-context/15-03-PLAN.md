---
phase: 15-products-context
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - src/app/(admin)/admin/products/page.tsx
  - src/app/(admin)/admin/products/new/page.tsx
  - src/app/(admin)/admin/products/[id]/layout.tsx
  - src/app/(admin)/admin/products/[id]/page.tsx
  - src/app/(admin)/admin/products/[id]/edit/page.tsx
  - src/components/admin/ProductsTable.tsx
  - src/components/admin/ProductForm.tsx
  - src/components/admin/ProductDetailShell.tsx
  - src/data/dashboard-nav.ts
autonomous: true
requirements:
  - PROD-01
  - PROD-07

must_haves:
  truths:
    - "Admin can see product list at /admin/products with name, slug, version, and created date"
    - "Admin can click Add Product to create a new product with name and description"
    - "Admin can click a product row to navigate to /admin/products/[id] detail page"
    - "Admin can edit product details at /admin/products/[id]/edit"
    - "Products entry appears in admin sidebar navigation"
  artifacts:
    - path: "src/app/(admin)/admin/products/page.tsx"
      provides: "Product list page (server component)"
      contains: "export default async function"
    - path: "src/app/(admin)/admin/products/new/page.tsx"
      provides: "Create product page"
      contains: "export default async function"
    - path: "src/components/admin/ProductsTable.tsx"
      provides: "Client component: product list table with actions"
      contains: '"use client"'
    - path: "src/components/admin/ProductForm.tsx"
      provides: "Client component: product create/edit form"
      contains: '"use client"'
    - path: "src/components/admin/ProductDetailShell.tsx"
      provides: "Client component: tab navigation for product detail"
      contains: '"use client"'
    - path: "src/data/dashboard-nav.ts"
      provides: "Updated admin navigation with Products entry"
      contains: "Products"
  key_links:
    - from: "src/app/(admin)/admin/products/page.tsx"
      to: "src/app/(admin)/actions/admin-products.ts"
      via: "import server actions for table callbacks"
      pattern: "import.*admin-products"
    - from: "src/components/admin/ProductsTable.tsx"
      to: "/admin/products/[id]"
      via: "Link to product detail on row click"
      pattern: "/admin/products/"
    - from: "src/data/dashboard-nav.ts"
      to: "/admin/products"
      via: "Nav item with Package icon"
      pattern: "/admin/products"
---

<objective>
Build the admin UI for product listing, creation, and detail viewing. This includes the product list page, create product page, product detail shell with tab navigation, edit product page, and the Products entry in admin sidebar navigation.

Purpose: Provides the primary admin interface for managing products. The detail shell establishes the tabbed layout that Plan 04 will extend with versions and plans tabs.

Output: Admin product CRUD pages, table and form components, detail shell, updated navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-products-context/15-CONTEXT.md
@.planning/phases/15-products-context/15-RESEARCH.md
@.planning/phases/15-products-context/15-01-SUMMARY.md
@.planning/phases/15-products-context/15-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/app/(admin)/admin/orders/page.tsx (admin page pattern):
```typescript
export const dynamic = "force-dynamic";
export default async function AdminOrdersPage() {
  const session = await auth.api.getSession({ headers: await headers() });
  if (!session) redirect("/login");
  const userRole = (session.user as Record<string, unknown>).role as string;
  if (userRole !== "admin" && userRole !== "super_admin") redirect("/admin/dashboard");
  // ... data fetch, render with PageBreadcrumb + ComponentCard + Table component
}
```

From src/components/admin/SettingsShell.tsx (tab navigation pattern):
```typescript
"use client";
// Uses usePathname() for active tab detection
// Renders aside with nav links + main content area with children
// Uses cn() for conditional active classes
```

From src/components/admin/OrdersTable.tsx (admin table pattern):
```typescript
"use client";
// Client component receiving data as props and server action callbacks
// Uses useTransition for server action calls
// Renders table with columns, badges for status, action buttons
```

From src/data/dashboard-nav.ts:
```typescript
import { Package } from "lucide-react"; // already available, not imported yet
export type NavItem = { name: string; icon: LucideIcon; path: string; subItems?: ... };
export const adminNavItems: NavItem[] = [ ... ];
// Need to add: { name: "Products", icon: Package, path: "/admin/products" }
```

From src/lib/db/schema.ts:
```typescript
export const products = pgTable("products", { id, name, slug, description, currentVersion, createdAt, updatedAt });
```

From src/app/(admin)/actions/admin-products.ts (Plan 02 output):
```typescript
export async function createProduct(formData: FormData): Promise<{ success?: true; productId?: string; error?: string }>
export async function updateProduct(productId: string, formData: FormData): Promise<{ success?: true; error?: string }>
export async function deleteProduct(productId: string): Promise<{ success?: true; error?: string }>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin product list page, create page, and navigation entry</name>
  <files>
    - src/app/(admin)/admin/products/page.tsx
    - src/app/(admin)/admin/products/new/page.tsx
    - src/components/admin/ProductsTable.tsx
    - src/components/admin/ProductForm.tsx
    - src/data/dashboard-nav.ts
  </files>
  <read_first>
    - src/app/(admin)/admin/orders/page.tsx (exact server component pattern to follow)
    - src/components/admin/OrdersTable.tsx (exact client table component pattern to follow)
    - src/data/dashboard-nav.ts (adminNavItems array to add Products entry to)
    - src/lib/db/schema.ts (products table columns for select query)
    - src/app/(admin)/actions/admin-products.ts (server action imports — createProduct, deleteProduct)
  </read_first>
  <action>
Create the following files:

1. **`src/data/dashboard-nav.ts`** — ADD a Products entry to `adminNavItems` array. Insert it between "Orders" and "Licenses" entries. Import `Package` from `lucide-react` (add to existing import). Add entry: `{ name: "Products", icon: Package, path: "/admin/products" }`. Do NOT modify any existing entries.

2. **`src/components/admin/ProductsTable.tsx`** — Client component (`"use client"`) following the OrdersTable.tsx pattern:
   - Props: `products: Array<{ id: string; name: string; slug: string; description: string | null; currentVersion: string | null; createdAt: Date }>`, `onDelete: (id: string) => Promise<{ success?: boolean; error?: string }>`
   - Import `useTransition` from react, `useRouter` from next/navigation, `Link` from next/link
   - Import `Trash2, Edit, Eye` from lucide-react
   - Render a table with columns: Name, Slug, Current Version, Created, Actions
   - Each row has: product name, slug (monospace), currentVersion or dash, createdAt formatted with date-fns `format(date, "MMM d, yyyy")`, action buttons (Eye link to `/admin/products/${id}`, Edit link to `/admin/products/${id}/edit`, Delete button with confirmation)
   - Delete button calls `onDelete(id)` wrapped in `startTransition`, shows loading state
   - Use the same table styling classes as OrdersTable.tsx (reference its HTML structure)
   - Handle empty state: "No products yet. Create your first product." with link to /admin/products/new

3. **`src/components/admin/ProductForm.tsx`** — Client component (`"use client"`):
   - Props: `action: (formData: FormData) => Promise<{ success?: boolean; error?: string; productId?: string }>`, `product?: { id: string; name: string; description: string | null; currentVersion: string | null }` (optional for edit mode), `submitButtonLabel?: string`
   - Import `useTransition, useState` from react, `useRouter` from next/navigation
   - Form fields: Name (text input, required), Description (textarea, optional)
   - If `product` prop provided, pre-fill fields (edit mode). Otherwise empty (create mode).
   - On submit: call `action(formData)` via startTransition, on success redirect to `/admin/products` (create) or show success (edit)
   - Show error message if action returns error
   - Style form fields using existing admin form input patterns (check other admin forms for class names)

4. **`src/app/(admin)/admin/products/page.tsx`** — Server component following orders/page.tsx pattern:
   - `export const dynamic = "force-dynamic"`
   - Auth check: session + admin role check (copy from orders page)
   - Data fetch: `db.select().from(products).orderBy(desc(products.createdAt))`
   - Render: `PageBreadcrumb` with pageTitle "Products", `ComponentCard` with title "Product Management" and action button linking to `/admin/products/new`
   - Pass data and `deleteProduct` action to `ProductsTable`
   - Import `deleteProduct` from `@/app/(admin)/actions/admin-products`

5. **`src/app/(admin)/admin/products/new/page.tsx`** — Server component:
   - Auth check (same pattern)
   - Render: `PageBreadcrumb` with pageTitle "New Product", `ComponentCard` with title "Create Product"
   - Render `ProductForm` with `action={createProduct}` and no product prop (create mode)
   - Import `createProduct` from admin-products actions

Create directory structure: `src/app/(admin)/admin/products/` and `src/app/(admin)/admin/products/new/`
  </action>
  <acceptance_criteria>
    - `grep "Products" src/data/dashboard-nav.ts` returns a match
    - `grep "Package" src/data/dashboard-nav.ts` returns a match
    - `grep "/admin/products" src/data/dashboard-nav.ts` returns a match
    - `grep '"use client"' src/components/admin/ProductsTable.tsx` returns a match
    - `grep "onDelete" src/components/admin/ProductsTable.tsx` returns a match
    - `grep "/admin/products/" src/components/admin/ProductsTable.tsx` returns a match (Link to detail)
    - `grep '"use client"' src/components/admin/ProductForm.tsx` returns a match
    - `grep "formData" src/components/admin/ProductForm.tsx` returns a match
    - `grep "export const dynamic" src/app/\(admin\)/admin/products/page.tsx` returns a match
    - `grep "PageBreadcrumb" src/app/\(admin\)/admin/products/page.tsx` returns a match
    - `grep "ProductsTable" src/app/\(admin\)/admin/products/page.tsx` returns a match
    - `grep "export default async function" src/app/\(admin\)/admin/products/new/page.tsx` returns a match
    - `grep "ProductForm" src/app/\(admin\)/admin/products/new/page.tsx` returns a match
  </acceptance_criteria>
  <verify>
    <automated>cd "D:\Devsroom-Work\conversionflow-website" && npx tsc --noEmit 2>&1 | grep -i "products/page\|ProductsTable\|ProductForm\|dashboard-nav" | head -20</automated>
  </verify>
  <done>Admin product list page at /admin/products showing all products in a table. Create product page at /admin/products/new with form. ProductsTable component with view/edit/delete actions. ProductForm component for create/edit. "Products" entry added to admin sidebar navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Create product detail shell, detail page, and edit page</name>
  <files>
    - src/app/(admin)/admin/products/[id]/layout.tsx
    - src/app/(admin)/admin/products/[id]/page.tsx
    - src/app/(admin)/admin/products/[id]/edit/page.tsx
    - src/components/admin/ProductDetailShell.tsx
  </files>
  <read_first>
    - src/app/(admin)/admin/settings/layout.tsx (exact nested layout pattern — auth guard + shell wrapper)
    - src/components/admin/SettingsShell.tsx (exact sidebar shell pattern to adapt for product detail)
    - src/app/(admin)/admin/orders/page.tsx (admin page auth pattern)
    - src/lib/db/schema.ts (products table columns)
    - src/app/(admin)/actions/admin-products.ts (updateProduct action)
  </read_first>
  <action>
Create the following files:

1. **`src/components/admin/ProductDetailShell.tsx`** — Client component (`"use client"`) adapted from SettingsShell.tsx pattern:
   - Props: `product: { id: string; name: string; slug: string }, children: React.ReactNode`
   - Define DETAIL_TABS array with navigation items:
     - `{ label: "Overview", href: "/admin/products/[id]", icon: Eye }` (from lucide-react)
     - `{ label: "Edit", href: "/admin/products/[id]/edit", icon: Edit }`
     - `{ label: "Versions", href: "/admin/products/[id]/versions", icon: GitBranch }`
     - `{ label: "Plans", href: "/admin/products/[id]/plans", icon: CreditCard }`
   - Replace `[id]` in hrefs with actual `product.id` value
   - Render layout: header with product name + slug, then horizontal tab navigation (not sidebar like Settings — use horizontal tabs since there are only 4 items), then children content area
   - Use `usePathname()` for active tab detection
   - Use `cn()` for conditional active tab styling
   - Import `Eye, Edit, GitBranch, CreditCard` from lucide-react

2. **`src/app/(admin)/admin/products/[id]/layout.tsx`** — Server component following settings/layout.tsx pattern:
   - `export const dynamic = "force-dynamic"`
   - Auth check (session + admin role)
   - Fetch product: `db.select().from(products).where(eq(products.id, params.id)).limit(1)` (use `await params` for Next.js 16 params — they are now a Promise)
   - If product not found, `redirect("/admin/products")`
   - Render `<ProductDetailShell product={product}>{children}</ProductDetailShell>`

3. **`src/app/(admin)/admin/products/[id]/page.tsx`** — Server component (overview tab):
   - `export const dynamic = "force-dynamic"`
   - Auth check (or rely on layout — still add for safety)
   - Fetch product details (same query as layout, or receive from layout — re-fetch is fine)
   - Render: `PageBreadcrumb` with pageTitle product name, `ComponentCard` showing product details in a description list: Name, Slug (monospace), Description, Current Version, Created Date, Updated Date
   - Also show quick stats: number of versions and plans (count queries)

4. **`src/app/(admin)/admin/products/[id]/edit/page.tsx`** — Server component:
   - `export const dynamic = "force-dynamic"`
   - Auth check
   - Fetch product by id
   - Render: `PageBreadcrumb` with pageTitle "Edit Product", `ComponentCard` with title "Edit Product"
   - Render `ProductForm` with `action={updateProduct.bind(null, productId)}` and `product={product}` (edit mode)
   - Import `updateProduct` from admin-products actions

Create directory structure: `src/app/(admin)/admin/products/[id]/` and `src/app/(admin)/admin/products/[id]/edit/`

**IMPORTANT for Next.js 16:** The `params` prop in page/layout components is now a Promise. Access it via `const { id } = await params;` (not `params.id` directly).
  </action>
  <acceptance_criteria>
    - `grep '"use client"' src/components/admin/ProductDetailShell.tsx` returns a match
    - `grep "DETAIL_TABS\|tabs\|Tab" src/components/admin/ProductDetailShell.tsx` returns a match
    - `grep "usePathname" src/components/admin/ProductDetailShell.tsx` returns a match
    - `grep "Versions" src/components/admin/ProductDetailShell.tsx` returns a match
    - `grep "Plans" src/components/admin/ProductDetailShell.tsx` returns a match
    - `grep "ProductDetailShell" src/app/\(admin\)/admin/products/\[id\]/layout.tsx` returns a match
    - `grep "export default async function" src/app/\(admin\)/admin/products/\[id\]/layout.tsx` returns a match
    - `grep "export default async function" src/app/\(admin\)/admin/products/\[id\]/page.tsx` returns a match
    - `grep "PageBreadcrumb" src/app/\(admin\)/admin/products/\[id\]/page.tsx` returns a match
    - `grep "ProductForm" src/app/\(admin\)/admin/products/\[id\]/edit/page.tsx` returns a match
    - `grep "updateProduct" src/app/\(admin\)/admin/products/\[id\]/edit/page.tsx` returns a match
  </acceptance_criteria>
  <verify>
    <automated>cd "D:\Devsroom-Work\conversionflow-website" && npx tsc --noEmit 2>&1 | grep -i "products/\[id\]\|ProductDetail" | head -20</automated>
  </verify>
  <done>Product detail shell component with horizontal tab navigation (Overview, Edit, Versions, Plans). Product detail layout with auth guard and product data fetch. Product overview page showing product details with counts. Product edit page with pre-filled form. All pages render correctly within the tabbed layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> Product pages | Authenticated admin session required for all pages |
| Product pages -> Server actions | Form data flows through validated server actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Spoofing | Product pages | mitigate | Auth check in every server component (session + role); layout adds shared guard |
| T-15-10 | Tampering | Product form | mitigate | FormData validated in server actions (Plan 02); no direct DB writes from UI |
| T-15-11 | Information disclosure | Product detail | accept | Product data is admin-visible only; no PII or secrets |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. `/admin/products` renders product list page
3. `/admin/products/new` renders create product form
4. `/admin/products/[id]` renders product detail with tab navigation
5. `/admin/products/[id]/edit` renders edit form pre-filled with product data
6. Admin sidebar shows "Products" nav item
7. `pnpm build` succeeds
</verification>

<success_criteria>
- Product list page at /admin/products with table showing all products
- Create product page at /admin/products/new with name/description form
- Product detail page with tabbed navigation (Overview, Edit, Versions, Plans tabs)
- Edit product page pre-filled with existing data
- "Products" entry in admin sidebar navigation between Orders and Licenses
- TypeScript and build pass without errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-products-context/15-03-SUMMARY.md`
</output>
