---
phase: 15-products-context
plan: 04
type: execute
wave: 4
depends_on:
  - 15-03
files_modified:
  - src/app/(admin)/admin/products/[id]/versions/page.tsx
  - src/app/(admin)/admin/products/[id]/versions/new/page.tsx
  - src/app/(admin)/admin/products/[id]/plans/page.tsx
  - src/app/(admin)/admin/products/[id]/plans/new/page.tsx
  - src/components/admin/ProductVersionsTable.tsx
  - src/components/admin/ProductPlansTable.tsx
  - src/components/admin/PlanForm.tsx
autonomous: true
requirements:
  - PROD-02
  - PROD-03
  - PROD-04
  - PROD-05
  - PROD-06
  - PROD-07

must_haves:
  truths:
    - "Admin can view product versions at /admin/products/[id]/versions with version number, status badge, download URL, and release date"
    - "Admin can create new versions at /admin/products/[id]/versions/new with semver, download URL, and changelog"
    - "Admin can release a version (draft -> stable) which sets releasedAt timestamp"
    - "Admin can view product plans at /admin/products/[id]/plans with name, dual pricing (BDT + USD), license type, activations, and feature flags"
    - "Admin can create new plans at /admin/products/[id]/plans/new with all pricing and licensing fields"
    - "Plan form enforces lifetime/subscription invariants (lifetime hides billing fields, subscription requires billing cycle)"
  artifacts:
    - path: "src/app/(admin)/admin/products/[id]/versions/page.tsx"
      provides: "Version list page for a product"
      contains: "export default async function"
    - path: "src/app/(admin)/admin/products/[id]/versions/new/page.tsx"
      provides: "Create version page"
      contains: "export default async function"
    - path: "src/app/(admin)/admin/products/[id]/plans/page.tsx"
      provides: "Plan list page for a product"
      contains: "export default async function"
    - path: "src/app/(admin)/admin/products/[id]/plans/new/page.tsx"
      provides: "Create plan page"
      contains: "export default async function"
    - path: "src/components/admin/ProductVersionsTable.tsx"
      provides: "Client component: version list with status badges and release action"
      contains: '"use client"'
    - path: "src/components/admin/ProductPlansTable.tsx"
      provides: "Client component: plan list with pricing display"
      contains: '"use client"'
    - path: "src/components/admin/PlanForm.tsx"
      provides: "Client component: plan create/edit form with conditional billing fields"
      contains: '"use client"'
  key_links:
    - from: "src/components/admin/ProductVersionsTable.tsx"
      to: "src/app/(admin)/actions/admin-products.ts"
      via: "releaseVersion server action callback"
      pattern: "releaseVersion"
    - from: "src/components/admin/PlanForm.tsx"
      to: "src/app/(admin)/actions/admin-products.ts"
      via: "createPlan/updatePlan server action"
      pattern: "createPlan\|updatePlan"
    - from: "src/app/(admin)/admin/products/[id]/versions/page.tsx"
      to: "src/lib/db/schema.ts"
      via: "productVersions table query"
      pattern: "productVersions"
    - from: "src/app/(admin)/admin/products/[id]/plans/page.tsx"
      to: "src/lib/db/schema.ts"
      via: "productPlans table query"
      pattern: "productPlans"
---

<objective>
Build the versions and plans management UI within the product detail tabs. This includes version listing with status badges and release action, version creation form, plan listing with dual-currency pricing display, and plan creation form with conditional billing fields based on license type.

Purpose: Completes the product detail pages with version and plan management. This is where D-04 (lifetime/subscription invariants) and D-06 (full version tracking) become visible to the admin.

Output: Version list and create pages, plan list and create pages, version table, plan table, and plan form components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-products-context/15-CONTEXT.md
@.planning/phases/15-products-context/15-RESEARCH.md
@.planning/phases/15-products-context/15-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/db/schema.ts:
```typescript
export const productVersions = pgTable("product_versions", {
  id: uuid, productId: uuid, version: text, downloadUrl: text,
  changelog: text, status: versionStatusEnum ("stable"|"beta"|"draft"),
  releasedAt: timestamp, createdAt: timestamp, updatedAt: timestamp
});
export const productPlans = pgTable("product_plans", {
  id: uuid, productId: uuid, name: text, slug: text, description: text,
  priceBDT: integer, priceUSD: integer, licenseType: licenseTypeEnum ("lifetime"|"subscription"),
  billingCycle: billingCycleEnum ("monthly"|"yearly"|"custom") nullable,
  billingDurationMonths: integer nullable, maxActivations: integer,
  features: jsonb $type<Record<string, boolean>>, sortOrder: integer,
  active: boolean, createdAt: timestamp, updatedAt: timestamp
});
```

From src/app/(admin)/actions/admin-products.ts (Plan 02 output):
```typescript
export async function createVersion(productId: string, formData: FormData)
export async function updateVersion(versionId: string, formData: FormData)
export async function releaseVersion(versionId: string)
export async function createPlan(productId: string, formData: FormData)
export async function updatePlan(planId: string, formData: FormData)
export async function deletePlan(planId: string)
```

From src/components/admin/ProductDetailShell.tsx (Plan 03 output):
```typescript
// Tab navigation with "Versions" tab at /admin/products/[id]/versions
// and "Plans" tab at /admin/products/[id]/plans
```

From src/components/admin/ProductsTable.tsx (Plan 03 output — pattern to follow):
```typescript
"use client";
// Client component with table, action buttons, useTransition for server action calls
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create version management UI (table component, list page, create page)</name>
  <files>
    - src/components/admin/ProductVersionsTable.tsx
    - src/app/(admin)/admin/products/[id]/versions/page.tsx
    - src/app/(admin)/admin/products/[id]/versions/new/page.tsx
  </files>
  <read_first>
    - src/components/admin/ProductsTable.tsx (client table component pattern to follow)
    - src/components/admin/ProductForm.tsx (client form component pattern to follow)
    - src/app/(admin)/admin/products/page.tsx (server page pattern with auth + data fetch)
    - src/lib/db/schema.ts (productVersions table columns)
    - src/app/(admin)/actions/admin-products.ts (createVersion, releaseVersion actions)
    - src/components/admin/ProductDetailShell.tsx (for understanding tab navigation context)
  </read_first>
  <action>
Create the following files:

1. **`src/components/admin/ProductVersionsTable.tsx`** — Client component (`"use client"`):
   - Props: `versions: Array<{ id: string; version: string; downloadUrl: string | null; changelog: string | null; status: "stable" | "beta" | "draft"; releasedAt: Date | null; createdAt: Date }>`, `onRelease: (id: string) => Promise<{ success?: boolean; error?: string }>`
   - Import `useTransition` from react, `Badge` or inline badge styling
   - Table columns: Version (semver string, monospace), Status (colored badge: stable=green, beta=yellow, draft=gray), Download URL (link or dash), Released (date or "Unreleased"), Actions
   - Status badge rendering: use `cn()` with conditional classes for each status color
   - Actions column: "Release" button (only shown when status is "draft" or "beta") — calls `onRelease(id)` wrapped in `startTransition`. Show loading spinner during transition.
   - Empty state: "No versions yet. Create a version."

2. **`src/app/(admin)/admin/products/[id]/versions/page.tsx`** — Server component:
   - `export const dynamic = "force-dynamic"`
   - Access params via `const { id } = await params;` (Next.js 16 async params)
   - Auth check (session + admin role)
   - Fetch versions: `db.select().from(productVersions).where(eq(productVersions.productId, id)).orderBy(desc(productVersions.createdAt))`
   - Render: `ComponentCard` with title "Version Management" and action link to `/admin/products/${id}/versions/new` labeled "Add Version"
   - Render `ProductVersionsTable` with fetched data and `releaseVersion` action callback
   - Import `releaseVersion` from admin-products actions

3. **`src/app/(admin)/admin/products/[id]/versions/new/page.tsx`** — Server component:
   - Access params via `const { id } = await params;`
   - Auth check
   - Render: `PageBreadcrumb` with pageTitle "New Version", `ComponentCard` with title "Create Version"
   - Simple form (can be inline, not a separate component since it's small):
     - Version field: text input with placeholder "e.g., 1.2.0", required, pattern validation for semver
     - Download URL: text input, optional, placeholder "https://..."
     - Changelog: textarea, optional
     - Submit button: "Create Version"
   - Form action calls `createVersion.bind(null, productId)` — use `.bind()` to curry the productId
   - Import `createVersion` from admin-products actions

Create directory structure: `src/app/(admin)/admin/products/[id]/versions/` and `src/app/(admin)/admin/products/[id]/versions/new/`
  </action>
  <acceptance_criteria>
    - `grep '"use client"' src/components/admin/ProductVersionsTable.tsx` returns a match
    - `grep "onRelease" src/components/admin/ProductVersionsTable.tsx` returns a match
    - `grep "stable\|beta\|draft" src/components/admin/ProductVersionsTable.tsx` returns a match (status badges)
    - `grep "export default async function" src/app/\(admin\)/admin/products/\[id\]/versions/page.tsx` returns a match
    - `grep "productVersions" src/app/\(admin\)/admin/products/\[id\]/versions/page.tsx` returns a match
    - `grep "ProductVersionsTable" src/app/\(admin\)/admin/products/\[id\]/versions/page.tsx` returns a match
    - `grep "releaseVersion" src/app/\(admin\)/admin/products/\[id\]/versions/page.tsx` returns a match
    - `grep "export default async function" src/app/\(admin\)/admin/products/\[id\]/versions/new/page.tsx` returns a match
    - `grep "createVersion" src/app/\(admin\)/admin/products/\[id\]/versions/new/page.tsx` returns a match
    - `grep "semver\|pattern\|1\\.2\\.0" src/app/\(admin\)/admin/products/\[id\]/versions/new/page.tsx` returns a match (semver validation)
  </acceptance_criteria>
  <verify>
    <automated>cd "D:\Devsroom-Work\conversionflow-website" && npx tsc --noEmit 2>&1 | grep -i "versions\|ProductVersions" | head -20</automated>
  </verify>
  <done>Version management UI complete: version list page at /admin/products/[id]/versions with status badges and release action, version create page at /admin/products/[id]/versions/new with semver input. Release action changes draft/beta to stable with releasedAt timestamp.</done>
</task>

<task type="auto">
  <name>Task 2: Create plan management UI (table component, plan form, list page, create page)</name>
  <files>
    - src/components/admin/ProductPlansTable.tsx
    - src/components/admin/PlanForm.tsx
    - src/app/(admin)/admin/products/[id]/plans/page.tsx
    - src/app/(admin)/admin/products/[id]/plans/new/page.tsx
  </files>
  <read_first>
    - src/components/admin/ProductsTable.tsx (client table pattern)
    - src/components/admin/ProductForm.tsx (client form pattern)
    - src/lib/db/schema.ts (productPlans table columns — priceBDT, priceUSD, licenseType, billingCycle, maxActivations, features)
    - src/app/(admin)/actions/admin-products.ts (createPlan, updatePlan, deletePlan actions — understand field names and validation)
    - src/data/pricing.ts (reference for how plans are currently displayed — pricing format, feature lists)
    - src/components/admin/ProductDetailShell.tsx (tab navigation context)
  </read_first>
  <action>
Create the following files:

1. **`src/components/admin/ProductPlansTable.tsx`** — Client component (`"use client"`):
   - Props: `plans: Array<{ id: string; name: string; slug: string; priceBDT: number; priceUSD: number; licenseType: string; billingCycle: string | null; billingDurationMonths: number | null; maxActivations: number; features: Record<string, boolean>; active: boolean; sortOrder: number }>`, `onDelete: (id: string) => Promise<{ success?: boolean; error?: string }>`
   - Table columns: Plan (name), Pricing (format as "Tk {priceBDT} / ${priceUSD}"), Type (licenseType badge: lifetime=blue, subscription=green), Billing (billingCycle + durationMonths or "N/A" for lifetime), Sites (maxActivations === 0 ? "Unlimited" : maxActivations), Features (comma-separated list of true flags, truncated to 3 with "+N more"), Active (green/red dot), Actions (Edit link, Delete button)
   - Pricing formatting: `Tk ${priceBDT.toLocaleString()} / $${priceUSD}`
   - Delete button with confirmation dialog, wrapped in `startTransition`
   - Empty state: "No plans yet. Create a plan."

2. **`src/components/admin/PlanForm.tsx`** — Client component (`"use client"`):
   - Props: `productId: string`, `action: (productId: string, formData: FormData) => Promise<{ success?: boolean; error?: string; planId?: string }>`, `plan?: { id: string; name: string; description: string | null; priceBDT: number; priceUSD: number; licenseType: string; billingCycle: string | null; billingDurationMonths: number | null; maxActivations: number; features: Record<string, boolean>; sortOrder: number; active: boolean }` (optional for edit mode)
   - Import `useState` from react, `useRouter` from next/navigation
   - State: `licenseType` (controlled, default from plan or "subscription"), `billingCycle` (controlled), `featureFlags` (controlled Record<string, boolean>, default from plan.features or empty object), `newFlagKey` (string for adding new flags), `error` (string | null)
   - Form fields:
     - Plan Name (text, required) — auto-generate slug from name on submit
     - Description (textarea, optional)
     - Price BDT (number input, required, min 0) — label "Price (BDT)"
     - Price USD (number input, required, min 0) — label "Price (USD)"
     - License Type (select: "subscription" / "lifetime") — changing to "lifetime" clears billingCycle and billingDurationMonths
     - **Conditionally shown when licenseType === "subscription":**
       - Billing Cycle (select: "monthly" / "yearly" / "custom")
       - Duration (number input, months) — label "Duration (months)"
     - Max Activations (number input, required, min 0) — label "Max Sites (0 = unlimited)"
     - Sort Order (number input, default 0)
     - Active (checkbox, default true)
     - **Feature Flags section:**
       - Display existing flags as rows with key label and boolean toggle (checkbox)
       - Each flag row has a delete button (X) to remove the flag
       - "Add Feature" row: text input for new flag key + "Add" button — adds to featureFlags state as `true`
       - On submit, serialize features as `JSON.stringify(featureFlags)` into a hidden input named "features"
   - Submit: call `action(productId, formData)` via form action binding
   - Client-side validation before submit: if subscription and no billingCycle selected, show error
   - Redirect to `/admin/products/${productId}/plans` on success

3. **`src/app/(admin)/admin/products/[id]/plans/page.tsx`** — Server component:
   - `export const dynamic = "force-dynamic"`
   - Access params via `const { id } = await params;`
   - Auth check
   - Fetch plans: `db.select().from(productPlans).where(eq(productPlans.productId, id)).orderBy(asc(productPlans.sortOrder))`
   - Render: `ComponentCard` with title "Plan Management" and action link to `/admin/products/${id}/plans/new` labeled "Add Plan"
   - Render `ProductPlansTable` with fetched data and `deletePlan` action callback

4. **`src/app/(admin)/admin/products/[id]/plans/new/page.tsx`** — Server component:
   - Access params via `const { id } = await params;`
   - Auth check
   - Render: `PageBreadcrumb` with pageTitle "New Plan", `ComponentCard` with title "Create Plan"
   - Render `PlanForm` with `productId={id}`, `action={createPlan}` (no plan prop — create mode)
   - Import `createPlan` from admin-products actions

Create directory structure: `src/app/(admin)/admin/products/[id]/plans/` and `src/app/(admin)/admin/products/[id]/plans/new/`
  </action>
  <acceptance_criteria>
    - `grep '"use client"' src/components/admin/ProductPlansTable.tsx` returns a match
    - `grep "onDelete" src/components/admin/ProductPlansTable.tsx` returns a match
    - `grep "priceBDT" src/components/admin/ProductPlansTable.tsx` returns a match
    - `grep "licenseType" src/components/admin/ProductPlansTable.tsx` returns a match
    - `grep "Unlimited" src/components/admin/ProductPlansTable.tsx` returns a match
    - `grep '"use client"' src/components/admin/PlanForm.tsx` returns a match
    - `grep "licenseType" src/components/admin/PlanForm.tsx` returns a match
    - `grep "billingCycle" src/components/admin/PlanForm.tsx` returns a match
    - `grep "featureFlags\|features" src/components/admin/PlanForm.tsx` returns a match
    - `grep "export default async function" src/app/\(admin\)/admin/products/\[id\]/plans/page.tsx` returns a match
    - `grep "productPlans" src/app/\(admin\)/admin/products/\[id\]/plans/page.tsx` returns a match
    - `grep "ProductPlansTable" src/app/\(admin\)/admin/products/\[id\]/plans/page.tsx` returns a match
    - `grep "PlanForm" src/app/\(admin\)/admin/products/\[id\]/plans/new/page.tsx` returns a match
    - `grep "createPlan" src/app/\(admin\)/admin/products/\[id\]/plans/new/page.tsx` returns a match
  </acceptance_criteria>
  <verify>
    <automated>cd "D:\Devsroom-Work\conversionflow-website" && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>Plan management UI complete: plan list page at /admin/products/[id]/plans with dual-currency pricing, license type badges, activation display, and feature flag summary. Plan create page at /admin/products/[id]/plans/new with conditional billing fields (hidden for lifetime), feature flag management UI. Full build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> Version/Plan pages | Authenticated admin session required |
| Plan form -> Server action | Features JSON string must be validated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Tampering | PlanForm features field | mitigate | Client validates boolean-only flags; server action (Plan 02) also validates before DB insert |
| T-15-13 | Tampering | Version semver field | mitigate | HTML pattern attribute + server-side regex validation in createVersion action |
| T-15-14 | Information disclosure | Plan pricing | accept | Admin-only view; pricing is not secret |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. `pnpm build` passes
3. Version list page shows versions with colored status badges
4. Version create page accepts semver string and creates draft version
5. Release action changes version to stable with releasedAt date
6. Plan list page shows plans with dual pricing (BDT + USD), license type, activations
7. Plan create form hides billing fields when "lifetime" is selected
8. Plan create form allows adding/removing feature flags
9. All CRUD operations create audit log entries
</verification>

<success_criteria>
- Version management: list, create (draft), release (draft->stable) all work
- Plan management: list with dual-currency display, create with all fields including conditional billing and feature flags
- Plan form enforces lifetime/subscription UI logic (hides billing for lifetime)
- `pnpm build` passes without errors
- All pages render within product detail tab navigation
</success_criteria>

<output>
After completion, create `.planning/phases/15-products-context/15-04-SUMMARY.md`
</output>
