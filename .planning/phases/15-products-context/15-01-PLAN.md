---
phase: 15-products-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/modules/products/domain/entities/Product.ts
  - src/modules/products/domain/entities/ProductVersion.ts
  - src/modules/products/domain/entities/ProductPlan.ts
  - src/modules/products/domain/events/ProductEvents.ts
  - src/modules/products/domain/index.ts
  - src/modules/products/infrastructure/repositories/ProductRepository.ts
  - src/modules/products/infrastructure/repositories/ProductVersionRepository.ts
  - src/modules/products/infrastructure/repositories/ProductPlanRepository.ts
  - src/modules/products/infrastructure/repositories/mappers/ProductMapper.ts
  - src/modules/products/infrastructure/repositories/mappers/ProductVersionMapper.ts
  - src/modules/products/infrastructure/repositories/mappers/ProductPlanMapper.ts
  - src/modules/products/infrastructure/index.ts
  - src/modules/products/index.ts
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-04
  - PROD-05
  - PROD-06
  - PROD-07

must_haves:
  truths:
    - "Products table exists with name, slug, description, currentVersion columns and unique slug constraint"
    - "Product versions table exists with semver, downloadUrl, changelog, status enum (stable/beta/draft), and compound unique on (productId, version)"
    - "Product plans table exists with dual currency (priceBDT, priceUSD), licenseType enum, nullable billingCycle, maxActivations, and JSONB features"
    - "Product, ProductVersion, ProductPlan domain entities enforce business invariants (lifetime no billing, subscription requires billing, non-negative activations, boolean-only features)"
    - "Three repositories extend BaseRepository with custom queries (findBySlug, findByProductId)"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "products, product_versions, product_plans tables + enums + relations"
      contains: "export const products"
    - path: "src/modules/products/domain/entities/Product.ts"
      provides: "Product domain entity class"
      exports: ["Product"]
    - path: "src/modules/products/domain/entities/ProductVersion.ts"
      provides: "ProductVersion domain entity with status lifecycle validation"
      exports: ["ProductVersion"]
    - path: "src/modules/products/domain/entities/ProductPlan.ts"
      provides: "ProductPlan domain entity with pricing/limits/features validation"
      exports: ["ProductPlan"]
    - path: "src/modules/products/infrastructure/repositories/ProductRepository.ts"
      provides: "Product repository extending BaseRepository"
      exports: ["ProductRepository"]
    - path: "src/modules/products/infrastructure/repositories/ProductVersionRepository.ts"
      provides: "ProductVersion repository extending BaseRepository"
      exports: ["ProductVersionRepository"]
    - path: "src/modules/products/infrastructure/repositories/ProductPlanRepository.ts"
      provides: "ProductPlan repository extending BaseRepository"
      exports: ["ProductPlanRepository"]
  key_links:
    - from: "src/modules/products/infrastructure/repositories/ProductRepository.ts"
      to: "src/shared/infrastructure/repositories/BaseRepository.ts"
      via: "extends BaseRepository"
      pattern: "extends BaseRepository"
    - from: "src/modules/products/domain/entities/ProductPlan.ts"
      to: "src/shared/domain/valueObjects/Money.ts"
      via: "Money.create() for price getters"
      pattern: "Money\\.create"
    - from: "src/modules/products/domain/events/ProductEvents.ts"
      to: "src/shared/infrastructure/eventBus/types.ts"
      via: "BaseEvent interface"
      pattern: "BaseEvent"
---

<objective>
Build the foundational data and domain layer for the Products bounded context: database schema (products, product_versions, product_plans tables with enums and relations), domain entities with business invariant validation, and repository implementations extending the Phase 14 BaseRepository.

Purpose: This plan creates the data foundation that all subsequent plans (server actions, admin UI) depend on. It implements all 7 locked decisions (D-01 through D-07) at the schema and domain level.

Output: Three Drizzle tables with enums/relations, three domain entities with validation, three repositories with mappers, domain events, and updated barrel exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-products-context/15-CONTEXT.md
@.planning/phases/15-products-context/15-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/shared/infrastructure/repositories/BaseRepository.ts:
```typescript
export class BaseRepository<T, Data> implements IRepository<T> {
  protected readonly db = db;
  protected readonly table: any;
  protected readonly mapper: IMapper<T, Data>;
  constructor(table: any, mapper: IMapper<T, Data>) { ... }
  async findById(id: string): Promise<T | null>
  async findAll(filters?: QueryBuilder): Promise<T[]>
  async create(data: Omit<T, 'id'>): Promise<T>
  async update(id: string, data: Partial<T>): Promise<T>
  async delete(id: string): Promise<void>
  async exists(id: string): Promise<boolean>
  async transaction<R>(callback: (trx: typeof db) => Promise<R>): Promise<R>
}
```

From src/shared/infrastructure/repositories/types.ts:
```typescript
export interface IMapper<Domain, Data> {
  toDomain(data: Data): Domain;
  toData(domain: Domain): Data;
}
export interface QueryBuilder {
  where?: SQL | SQL[];
  orderBy?: { column: string; direction: 'asc' | 'desc' };
  limit?: number;
  offset?: number;
}
```

From src/shared/domain/valueObjects/Money.ts:
```typescript
export class Money {
  static create(amount: number, currency: string = "BDT"): Money
  get amount(): number
  get currency(): string
  format(): string
  equalsValue(other: Money): boolean
}
```

From src/shared/infrastructure/eventBus/types.ts:
```typescript
export interface BaseEvent {
  id: string;
  type: string;
  aggregateId: string;
  payload: unknown;
  timestamp: Date;
  correlationId?: string;
  metadata?: { sync?: boolean; source?: string; version?: number };
}
```

From src/lib/db/schema.ts (existing patterns):
```typescript
// pgEnum pattern
export const orderStatusEnum = pgEnum("order_status", ["pending", "completed", "failed", "refunded"]);
// pgTable with uuid PK, timestamps, $onUpdate pattern
export const orders = pgTable("orders", { id: uuid("id").defaultRandom().primaryKey(), ... });
// relations pattern
export const ordersRelations = relations(orders, ({ one }) => ({ user: one(user, { ... }) }));
// compound unique pattern
(table) => [unique("name").on(table.col1, table.col2)]
// jsonb with type
features: jsonb("features").$type<Record<string, boolean>>().default({})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add products schema tables, enums, and relations to schema.ts</name>
  <files>src/lib/db/schema.ts</files>
  <read_first>
    - src/lib/db/schema.ts (existing full schema — must see all enums, tables, and relations to append correctly)
  </read_first>
  <action>
APPEND the following to `src/lib/db/schema.ts` (do NOT modify any existing code):

1. Three new enums after the existing `redirectStatusEnum` block (before Auth Tables section):
```typescript
export const versionStatusEnum = pgEnum("version_status", ["stable", "beta", "draft"]);
export const licenseTypeEnum = pgEnum("license_type", ["lifetime", "subscription"]);
export const billingCycleEnum = pgEnum("billing_cycle", ["monthly", "yearly", "custom"]);
```

2. Three new tables after the `webhookDeliveries` table (before the Blog Tables section). Use the exact column names and constraints from the RESEARCH.md Pattern 1 example:
   - `products` table: `id` (uuid defaultRandom PK), `name` (text notNull), `slug` (text notNull unique), `description` (text nullable), `currentVersion` (text nullable), `createdAt` (timestamp notNull defaultNow), `updatedAt` (timestamp notNull defaultNow $onUpdate). Per D-01 (multi-product from day one) and D-02 (standard hierarchy).
   - `productVersions` table: `id` (uuid defaultRandom PK), `productId` (uuid notNull references products.id onDelete cascade), `version` (text notNull — semver string), `downloadUrl` (text nullable), `changelog` (text nullable), `status` (versionStatusEnum notNull default "draft"), `releasedAt` (timestamp nullable), `createdAt` (timestamp notNull defaultNow), `updatedAt` (timestamp notNull defaultNow $onUpdate). Add compound unique constraint on `(productId, version)`. Per D-06.
   - `productPlans` table: `id` (uuid defaultRandom PK), `productId` (uuid notNull references products.id onDelete cascade), `name` (text notNull), `slug` (text notNull), `description` (text nullable), `priceBDT` (integer notNull default 0), `priceUSD` (integer notNull default 0), `licenseType` (licenseTypeEnum notNull default "subscription"), `billingCycle` (billingCycleEnum nullable — null for lifetime), `billingDurationMonths` (integer nullable), `maxActivations` (integer default 1 — 0/NULL means unlimited), `features` (jsonb with `$type<Record<string, boolean>>()` default `{}`), `sortOrder` (integer default 0), `active` (boolean default true), `createdAt` (timestamp notNull defaultNow), `updatedAt` (timestamp notNull defaultNow $onUpdate). Add compound unique on `(productId, slug)`. Per D-02, D-03, D-04, D-05, D-07.

3. Three new relations definitions after the existing `webhookDeliveriesRelations`:
   - `productsRelations`: has many productVersions, has many productPlans
   - `productVersionsRelations`: belongs to products (one)
   - `productPlansRelations`: belongs to products (one)

Use the exact same import style, naming conventions, and formatting as the existing schema code. Follow the `orders`/`ordersRelations` pattern for table and relation definitions.
  </action>
  <acceptance_criteria>
    - `grep "export const products = pgTable" src/lib/db/schema.ts` returns a match
    - `grep "export const productVersions = pgTable" src/lib/db/schema.ts` returns a match
    - `grep "export const productPlans = pgTable" src/lib/db/schema.ts` returns a match
    - `grep "versionStatusEnum" src/lib/db/schema.ts` returns a match
    - `grep "licenseTypeEnum" src/lib/db/schema.ts` returns a match
    - `grep "billingCycleEnum" src/lib/db/schema.ts` returns a match
    - `grep "productsRelations" src/lib/db/schema.ts` returns a match
    - `grep "productVersionsRelations" src/lib/db/schema.ts` returns a match
    - `grep "productPlansRelations" src/lib/db/schema.ts` returns a match
    - `grep "unique.*productId.*slug" src/lib/db/schema.ts` returns a match (compound unique on plans)
    - `grep "unique.*productId.*version" src/lib/db/schema.ts` returns a match (compound unique on versions)
    - `grep "onDelete.*cascade" src/lib/db/schema.ts` returns at least 2 matches (both FKs)
    - No existing tables, enums, or relations are modified or removed
  </acceptance_criteria>
  <verify>
    <automated>cd "D:\Devsroom-Work\conversionflow-website" && npx tsc --noEmit src/lib/db/schema.ts 2>&1 | head -20</automated>
  </verify>
  <done>Three new Drizzle tables (products, product_versions, product_plans), three enums (version_status, license_type, billing_cycle), and three relations added to schema.ts without modifying existing code. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create domain entities, events, and update barrel exports</name>
  <files>
    - src/modules/products/domain/entities/Product.ts
    - src/modules/products/domain/entities/ProductVersion.ts
    - src/modules/products/domain/entities/ProductPlan.ts
    - src/modules/products/domain/events/ProductEvents.ts
    - src/modules/products/domain/index.ts
  </files>
  <read_first>
    - src/shared/domain/valueObjects/Money.ts (Money.create API for price getters)
    - src/shared/infrastructure/eventBus/types.ts (BaseEvent interface for events)
    - src/modules/products/domain/index.ts (current stub to replace)
    - src/lib/db/schema.ts (column names and types must match entity fields)
  </read_first>
  <action>
Create the following files:

1. **`src/modules/products/domain/entities/Product.ts`** — Product entity class:
   - Constructor takes: `id`, `name`, `slug`, `description` (nullable), `currentVersion` (nullable), `createdAt`, `updatedAt`
   - All fields are `public readonly`
   - Static factory method `create(name: string, description?: string): Product` that auto-generates slug from name using `name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '')`
   - `updateName(name: string): Product` returns new Product with regenerated slug
   - `updateDescription(description: string | null): Product` returns new Product
   - `updateCurrentVersion(version: string | null): Product` returns new Product
   - Validate: name must be non-empty string (throw Error if blank)

2. **`src/modules/products/domain/entities/ProductVersion.ts`** — ProductVersion entity class:
   - Constructor takes: `id`, `productId`, `version` (semver string), `downloadUrl` (nullable), `changelog` (nullable), `status` (union type `"stable" | "beta" | "draft"`), `releasedAt` (nullable Date), `createdAt`, `updatedAt`
   - `release(): ProductVersion` — returns new ProductVersion with status="stable" and releasedAt=new Date(). Validates that current status is not already "stable".
   - `isStable(): boolean`, `isBeta(): boolean`, `isDraft(): boolean` getters
   - Validate: version must match semver pattern `/^\d+\.\d+\.\d+(-[a-zA-Z0-9.]+)?$/` (throw Error if invalid)

3. **`src/modules/products/domain/entities/ProductPlan.ts`** — ProductPlan entity with full invariant validation per RESEARCH.md code example:
   - Constructor takes: `id`, `productId`, `name`, `slug`, `description` (nullable), `priceBDT` (number), `priceUSD` (number), `licenseType` ("lifetime" | "subscription"), `billingCycle` ("monthly" | "yearly" | "custom" | null), `billingDurationMonths` (number | null), `maxActivations` (number), `features` (Record<string, boolean>), `sortOrder` (number), `active` (boolean), `createdAt`, `updatedAt`
   - Private `validateInvariants()` called from constructor:
     - D-04: if licenseType === "lifetime", billingCycle MUST be null AND billingDurationMonths MUST be null
     - D-04: if licenseType === "subscription", billingCycle MUST be non-null
     - D-05: maxActivations must be >= 0
     - D-07: features must be flat Record<string, boolean> — every value must be typeof "boolean"
   - Price getters using Money value object: `get priceBDT(): Money` returns `Money.create(this._priceBDT, "BDT")`, `get priceUSD(): Money` returns `Money.create(this._priceUSD, "USD")`
   - `get isUnlimitedActivations(): boolean` returns `this.maxActivations === 0`
   - Import Money from `@/shared/domain/valueObjects`

4. **`src/modules/products/domain/events/ProductEvents.ts`** — Domain event types per RESEARCH.md example:
   - `PRODUCT_EVENTS` const object with event type strings: `product.created`, `product.updated`, `product.deleted`, `product.version.created`, `product.version.released`, `product.plan.created`, `product.plan.updated`, `product.plan.deleted`
   - `createProductEvent(type: string, aggregateId: string, payload: unknown): BaseEvent` factory function using nanoid for ID, metadata with source "products-context" version 1
   - Import BaseEvent from `@/shared/infrastructure/eventBus`, nanoid from `nanoid`

5. **`src/modules/products/domain/index.ts`** — Replace stub with barrel export:
   - Export all entities: `export { Product } from "./entities/Product"`
   - Export all entities: `export { ProductVersion } from "./entities/ProductVersion"`
   - Export all entities: `export { ProductPlan } from "./entities/ProductPlan"`
   - Export events: `export { PRODUCT_EVENTS, createProductEvent } from "./events/ProductEvents"`
  </action>
  <acceptance_criteria>
    - `grep "export class Product " src/modules/products/domain/entities/Product.ts` returns a match
    - `grep "static create" src/modules/products/domain/entities/Product.ts` returns a match
    - `grep "export class ProductVersion " src/modules/products/domain/entities/ProductVersion.ts` returns a match
    - `grep "release\(\)" src/modules/products/domain/entities/ProductVersion.ts` returns a match
    - `grep "export class ProductPlan " src/modules/products/domain/entities/ProductPlan.ts` returns a match
    - `grep "validateInvariants" src/modules/products/domain/entities/ProductPlan.ts` returns a match
    - `grep "Money.create" src/modules/products/domain/entities/ProductPlan.ts` returns a match
    - `grep "PRODUCT_EVENTS" src/modules/products/domain/events/ProductEvents.ts` returns a match
    - `grep "BaseEvent" src/modules/products/domain/events/ProductEvents.ts` returns a match
    - `grep "export { Product }" src/modules/products/domain/index.ts` returns a match
    - `grep "export { ProductPlan }" src/modules/products/domain/index.ts` returns a match
    - `grep "export { PRODUCT_EVENTS" src/modules/products/domain/index.ts` returns a match
  </acceptance_criteria>
  <verify>
    <automated>cd "D:\Devsroom-Work\conversionflow-website" && npx tsc --noEmit 2>&1 | grep -i "products/domain" | head -20</automated>
  </verify>
  <done>Three domain entities (Product, ProductVersion, ProductPlan) with full invariant validation, domain events with event type constants, and barrel export updated. TypeScript compiles without errors in domain layer.</done>
</task>

<task type="auto">
  <name>Task 3: Create repository implementations with mappers and update infrastructure barrel export</name>
  <files>
    - src/modules/products/infrastructure/repositories/ProductRepository.ts
    - src/modules/products/infrastructure/repositories/ProductVersionRepository.ts
    - src/modules/products/infrastructure/repositories/ProductPlanRepository.ts
    - src/modules/products/infrastructure/repositories/mappers/ProductMapper.ts
    - src/modules/products/infrastructure/repositories/mappers/ProductVersionMapper.ts
    - src/modules/products/infrastructure/repositories/mappers/ProductPlanMapper.ts
    - src/modules/products/infrastructure/index.ts
    - src/modules/products/index.ts
  </files>
  <read_first>
    - src/shared/infrastructure/repositories/BaseRepository.ts (base class to extend, constructor signature, protected db/table/mapper)
    - src/shared/infrastructure/repositories/types.ts (IMapper interface to implement)
    - src/modules/products/domain/entities/Product.ts (entity fields to map)
    - src/modules/products/domain/entities/ProductVersion.ts (entity fields to map)
    - src/modules/products/domain/entities/ProductPlan.ts (entity fields to map)
    - src/lib/db/schema.ts (table column names and types for mapper toDomain/toData)
    - src/modules/products/infrastructure/index.ts (current stub to replace)
    - src/modules/products/index.ts (current barrel export to update)
  </read_first>
  <action>
Create the following files:

1. **`src/modules/products/infrastructure/repositories/mappers/ProductMapper.ts`** — Implements `IMapper<Product, typeof products.$inferSelect>`:
   - `toDomain(row)`: returns `new Product(row.id, row.name, row.slug, row.description, row.currentVersion, row.createdAt, row.updatedAt)`
   - `toData(domain)`: returns `{ id: domain.id, name: domain.name, slug: domain.slug, description: domain.description, currentVersion: domain.currentVersion }` (omit createdAt/updatedAt — DB handles defaults)
   - Import Product from domain entity, products from schema

2. **`src/modules/products/infrastructure/repositories/mappers/ProductVersionMapper.ts`** — Implements `IMapper<ProductVersion, typeof productVersions.$inferSelect>`:
   - `toDomain(row)`: returns new ProductVersion with all fields from row
   - `toData(domain)`: returns object with all fields except id (generated), createdAt, updatedAt
   - Import ProductVersion from domain entity, productVersions from schema

3. **`src/modules/products/infrastructure/repositories/mappers/ProductPlanMapper.ts`** — Implements `IMapper<ProductPlan, typeof productPlans.$inferSelect>`:
   - `toDomain(row)`: returns new ProductPlan with all fields from row
   - `toData(domain)`: returns object with all fields except id, createdAt, updatedAt
   - Import ProductPlan from domain entity, productPlans from schema

4. **`src/modules/products/infrastructure/repositories/ProductRepository.ts`** — Extends `BaseRepository<Product, typeof products.$inferSelect>`:
   - Constructor calls `super(products, new ProductMapper())`
   - `findBySlug(slug: string): Promise<Product | null>` — queries where slug matches, returns first or null
   - `findByProductId` is NOT needed (that's for version/plan repos) — this is the product-level repo

5. **`src/modules/products/infrastructure/repositories/ProductVersionRepository.ts`** — Extends `BaseRepository<ProductVersion, typeof productVersions.$inferSelect>`:
   - Constructor calls `super(productVersions, new ProductVersionMapper())`
   - `findByProductId(productId: string): Promise<ProductVersion[]>` — queries where productId matches, ordered by createdAt desc
   - `findLatestStable(productId: string): Promise<ProductVersion | null>` — queries where productId matches AND status='stable', ordered by releasedAt desc, limit 1

6. **`src/modules/products/infrastructure/repositories/ProductPlanRepository.ts`** — Extends `BaseRepository<ProductPlan, typeof productPlans.$inferSelect>`:
   - Constructor calls `super(productPlans, new ProductPlanMapper())`
   - `findByProductId(productId: string): Promise<ProductPlan[]>` — queries where productId matches, ordered by sortOrder asc
   - `findBySlug(productId: string, slug: string): Promise<ProductPlan | null>` — queries where both productId and slug match, limit 1

7. **`src/modules/products/infrastructure/index.ts`** — Replace stub with barrel export:
   ```
   export { ProductRepository } from "./repositories/ProductRepository";
   export { ProductVersionRepository } from "./repositories/ProductVersionRepository";
   export { ProductPlanRepository } from "./repositories/ProductPlanRepository";
   export { ProductMapper } from "./repositories/mappers/ProductMapper";
   export { ProductVersionMapper } from "./repositories/mappers/ProductVersionMapper";
   export { ProductPlanMapper } from "./repositories/mappers/ProductPlanMapper";
   ```

8. **`src/modules/products/index.ts`** — Update to also export infrastructure:
   ```
   export * from './domain';
   export * from './infrastructure';
   export * from './application';
   ```
   (It already has these lines — just verify they're correct, no change needed if already matching.)
  </action>
  <acceptance_criteria>
    - `grep "extends BaseRepository" src/modules/products/infrastructure/repositories/ProductRepository.ts` returns a match
    - `grep "extends BaseRepository" src/modules/products/infrastructure/repositories/ProductVersionRepository.ts` returns a match
    - `grep "extends BaseRepository" src/modules/products/infrastructure/repositories/ProductPlanRepository.ts` returns a match
    - `grep "findBySlug" src/modules/products/infrastructure/repositories/ProductRepository.ts` returns a match
    - `grep "findByProductId" src/modules/products/infrastructure/repositories/ProductVersionRepository.ts` returns a match
    - `grep "findLatestStable" src/modules/products/infrastructure/repositories/ProductVersionRepository.ts` returns a match
    - `grep "findByProductId" src/modules/products/infrastructure/repositories/ProductPlanRepository.ts` returns a match
    - `grep "findBySlug" src/modules/products/infrastructure/repositories/ProductPlanRepository.ts` returns a match
    - `grep "IMapper" src/modules/products/infrastructure/repositories/mappers/ProductMapper.ts` returns a match
    - `grep "IMapper" src/modules/products/infrastructure/repositories/mappers/ProductVersionMapper.ts` returns a match
    - `grep "IMapper" src/modules/products/infrastructure/repositories/mappers/ProductPlanMapper.ts` returns a match
    - `grep "export { ProductRepository }" src/modules/products/infrastructure/index.ts` returns a match
  </acceptance_criteria>
  <verify>
    <automated>cd "D:\Devsroom-Work\conversionflow-website" && npx tsc --noEmit 2>&1 | grep -i "products" | head -20</automated>
  </verify>
  <done>Three mapper implementations (IMapper for Product, ProductVersion, ProductPlan), three repository implementations extending BaseRepository with custom query methods, and barrel exports updated for infrastructure layer. All TypeScript compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema -> DB | Database schema defines constraints; no user input at this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | ProductPlan.validateInvariants() | mitigate | Domain entity validates all invariants (lifetime/subscription, maxActivations >= 0, features boolean-only) — prevents invalid data from reaching database |
| T-15-02 | Tampering | ProductPlan.features JSONB | mitigate | Validate in entity that all values are boolean — prevents injection via JSONB field |
| T-15-03 | Tampering | productPlans compound unique (productId, slug) | mitigate | Database constraint prevents duplicate plan slugs per product |
</threat_model>

<verification>
1. `npx tsc --noEmit` — TypeScript compilation passes with no errors related to products module
2. Schema file contains all three tables, three enums, and three relations
3. Domain entities enforce invariants (ProductPlan rejects lifetime with billingCycle, ProductVersion validates semver)
4. Repositories extend BaseRepository and import correct table references
5. Barrel exports allow `import { Product, ProductRepository } from '@/modules/products'`
</verification>

<success_criteria>
- Three new tables in schema.ts (products, product_versions, product_plans) with correct enums, relations, and constraints
- Three domain entities with constructor validation (especially ProductPlan with D-04 lifetime/subscription invariants)
- Three repositories with mappers extending BaseRepository with custom query methods
- All barrel exports updated, `import { Product, ProductRepository } from '@/modules/products'` resolves
- `npx tsc --noEmit` passes with zero errors in products module
</success_criteria>

<output>
After completion, create `.planning/phases/15-products-context/15-01-SUMMARY.md`
</output>
