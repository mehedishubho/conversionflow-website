---
phase: 04-checkout-payments
plan: 05
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-04
files_modified:
  - src/app/(portal)/dashboard/billing/[id]/page.tsx
  - src/components/invoice/InvoiceHTML.tsx
  - src/components/invoice/InvoicePDF.tsx
  - src/lib/invoices.ts
  - src/app/api/invoices/[id]/pdf/route.ts
  - src/lib/emails/order-confirmation.ts
autonomous: true
requirements:
  - PAY-05
  - PAY-06

must_haves:
  truths:
    - "Customer can view full invoice with line items, VAT breakdown, payment details at /dashboard/billing/[id] (D-17)"
    - "Customer can download invoice as PDF from the invoice detail page (D-18)"
    - "Confirmation email is sent after successful payment with order details and license key (D-19)"
    - "HTML invoice shows: company info, customer info, line items, VAT, discount, total, payment method, status"
  artifacts:
    - path: "src/app/(portal)/dashboard/billing/[id]/page.tsx"
      provides: "Invoice detail page with HTML view and PDF download"
      min_lines: 50
    - path: "src/components/invoice/InvoiceHTML.tsx"
      provides: "HTML invoice component for browser display"
      min_lines: 60
    - path: "src/components/invoice/InvoicePDF.tsx"
      provides: "@react-pdf/renderer document component"
      min_lines: 50
    - path: "src/lib/invoices.ts"
      provides: "PDF generation utility"
      exports: ["generateInvoicePDF"]
    - path: "src/lib/emails/order-confirmation.ts"
      provides: "Order confirmation email sender"
      exports: ["sendOrderConfirmationEmail"]
    - path: "src/app/api/invoices/[id]/pdf/route.ts"
      provides: "PDF download API route"
      exports: ["GET"]
  key_links:
    - from: "src/app/(portal)/dashboard/billing/[id]/page.tsx"
      to: "src/components/invoice/InvoiceHTML.tsx"
      via: "Renders HTML invoice component"
      pattern: "InvoiceHTML"
    - from: "src/app/api/invoices/[id]/pdf/route.ts"
      to: "src/lib/invoices.ts"
      via: "generateInvoicePDF function"
      pattern: "generateInvoicePDF"
    - from: "src/lib/invoices.ts"
      to: "src/components/invoice/InvoicePDF.tsx"
      via: "renderToBuffer(InvoicePDF)"
      pattern: "InvoicePDF"
---

<objective>
Invoice generation (HTML + PDF) and order confirmation email.

Purpose: Complete the post-purchase experience -- customers view detailed invoices in the browser, download PDF copies, and receive confirmation emails. These are the final pieces of the payment flow that make the transaction official.

Output: Invoice detail page, HTML/PDF invoice components, PDF download route, order confirmation email template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-checkout-payments/04-CONTEXT.md
@.planning/phases/04-checkout-payments/04-RESEARCH.md
@.planning/phases/04-checkout-payments/04-UI-SPEC.md
@.planning/phases/04-checkout-payments/04-01-SUMMARY.md

@src/lib/db/schema.ts
@src/lib/db/index.ts
@src/lib/emails/verification.ts
@src/components/common/ComponentCard.tsx
@src/components/common/PageBreadCrumb.tsx
@src/components/portal/InvoiceTable.tsx
@src/app/(portal)/dashboard/billing/page.tsx
</context>

<interfaces>
<!-- Existing patterns and schema the executor needs -->

From src/lib/db/schema.ts:
```typescript
export const orders = pgTable("orders", { id, userId, centralOrderId, productId, plan, amount (integer BDT), currency, paymentMethod, paymentRef, status, couponCode, discountAmount, taxAmount, createdAt, updatedAt });
export const user = pgTable("user", { id, name, email, phone, centralUserId });
export const licenses = pgTable("licenses", { id, userId, centralLicenseId, orderId, productId, plan, licenseKey, status });
```

From src/lib/emails/verification.ts (pattern to follow):
```typescript
const resend = new Resend(process.env.RESEND_API_KEY);
// Pattern: resend.emails.send({ from, to, subject, html })
```

From src/components/portal/InvoiceTable.tsx:
```typescript
const paymentMethodMap: Record<string, string> = { bkash: "bKash", nagad: "Nagad", rocket: "Rocket", bank_transfer: "Bank Transfer", ssl_commerz: "SSL Commerce" };
const statusBadgeMap: Record<string, string> = { completed: "success", pending: "warning", failed: "error", refunded: "light" };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create InvoiceHTML and InvoicePDF components plus PDF generation utility</name>
  <files>src/components/invoice/InvoiceHTML.tsx, src/components/invoice/InvoicePDF.tsx, src/lib/invoices.ts, src/app/api/invoices/[id]/pdf/route.ts</files>
  <read_first>
    - src/lib/db/schema.ts
    - src/components/common/ComponentCard.tsx
    - src/components/portal/InvoiceTable.tsx
    - .planning/phases/04-checkout-payments/04-UI-SPEC.md
  </read_first>
  <action>
Before starting, install the required package:
```bash
pnpm add @react-pdf/renderer
```

**File 1: `src/components/invoice/InvoiceHTML.tsx`** -- per D-17, UI-SPEC Invoice Detail Page:
- Server component (no "use client")
- Props: `{ order: OrderWithUser }` where OrderWithUser includes order fields + userName, userEmail, userPhone
- Render a printable invoice layout inside ComponentCard:
  - Header: "INVOICE" title + "ConversionFlow by Devsroom"
  - Invoice metadata: Invoice # (order id first 8 chars), Date (formatted with date-fns)
  - Two-column info row: "Bill To" (customer name, email, phone) | "Company" (Devsroom, Dhaka, Bangladesh)
  - Line items table: Description column + Amount column
    - Row 1: "{plan} Plan" + base price formatted BDT (amount + taxAmount - discountAmount calculation)
    - Row 2: "VAT ({rate}%)" + taxAmount formatted (show actual computed rate)
    - Row 3 (if coupon): "Discount ({couponCode})" + discountAmount as negative in green
  - Total row: bold, larger text, formatted BDT total
  - Payment section: Method (from paymentMethodMap), Transaction ID (paymentRef), Status (Badge)
- Use Tailwind classes consistent with dashboard styling
- Include a "Download PDF" button at top right that links to `/api/invoices/{order.id}/pdf`

**File 2: `src/components/invoice/InvoicePDF.tsx`** -- per D-18:
- Import from `@react-pdf/renderer`: `Document, Page, Text, View, StyleSheet, Font`
- Props: `{ order: OrderWithUser }` (same type as HTML version)
- Define StyleSheet with consistent styling:
  ```typescript
  const styles = StyleSheet.create({
    page: { padding: 40, fontSize: 10, fontFamily: "Helvetica" },
    header: { fontSize: 24, marginBottom: 20, color: "#0047FF" },
    section: { marginBottom: 15 },
    row: { flexDirection: "row", justifyContent: "space-between", marginBottom: 5 },
    label: { color: "#666", fontSize: 9 },
    value: { fontWeight: "bold" },
    divider: { borderBottomWidth: 1, borderBottomColor: "#eee", marginVertical: 10 },
    total: { fontSize: 14, fontWeight: "bold", color: "#0047FF" },
  });
  ```
- Document structure:
  - Page size A4
  - Header: "INVOICE" + "ConversionFlow by Devsroom"
  - Invoice # and Date
  - Bill To section + Company info
  - Line items (same rows as HTML version)
  - Total
  - Payment method + transaction ID + status

**File 3: `src/lib/invoices.ts`** -- per D-18:
```typescript
import { renderToBuffer } from "@react-pdf/renderer";
import { InvoicePDF } from "@/components/invoice/InvoicePDF";

export async function generateInvoicePDF(order: OrderWithUser): Promise<Buffer> {
  const pdfBuffer = await renderToBuffer(<InvoicePDF order={order} />);
  return pdfBuffer;
}
```
- Type the OrderWithUser interface (or import from shared types)

**File 4: `src/app/api/invoices/[id]/pdf/route.ts`**:
- Export GET function
- Auth check: get session, verify user owns this order (userId match) OR is admin
- Query order from DB with user join
- If not found or unauthorized: return 404
- Call generateInvoicePDF(order)
- Return new Response(pdfBuffer, { headers: { "Content-Type": "application/pdf", "Content-Disposition": `attachment; filename="invoice-${orderId.slice(0,8)}.pdf"` } })
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && grep -c "InvoiceHTML" src/components/invoice/InvoiceHTML.tsx && grep -c "Document" src/components/invoice/InvoicePDF.tsx && grep -c "renderToBuffer" src/lib/invoices.ts && grep -c "generateInvoicePDF" src/app/api/invoices/\[id\]/pdf/route.ts && grep -c "application/pdf" src/app/api/invoices/\[id\]/pdf/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/invoice/InvoiceHTML.tsx exists and renders invoice layout with "INVOICE" heading
    - InvoiceHTML.tsx shows line items: plan name, VAT amount, discount, total
    - InvoiceHTML.tsx contains "Bill To" section and company info
    - InvoiceHTML.tsx has "Download PDF" link/button
    - src/components/invoice/InvoicePDF.tsx imports Document, Page, Text, View from @react-pdf/renderer
    - InvoicePDF.tsx has StyleSheet.create call
    - InvoicePDF.tsx renders A4 page with invoice content
    - src/lib/invoices.ts exports generateInvoicePDF function
    - invoices.ts calls renderToBuffer with InvoicePDF component
    - src/app/api/invoices/[id]/pdf/route.ts exports GET function
    - PDF route checks auth and order ownership
    - PDF route returns Content-Type: application/pdf
    - PDF route sets Content-Disposition: attachment header
  </acceptance_criteria>
  <done>InvoiceHTML component renders full invoice in browser with line items, VAT breakdown, payment details, company info, and PDF download button. InvoicePDF component generates identical layout as PDF using @react-pdf/renderer. PDF generation utility wraps renderToBuffer. API route at /api/invoices/[id]/pdf serves authenticated PDF download.</done>
</task>

<task type="auto">
  <name>Task 2: Create invoice detail page and order confirmation email</name>
  <files>src/app/(portal)/dashboard/billing/[id]/page.tsx, src/lib/emails/order-confirmation.ts</files>
  <read_first>
    - src/app/(portal)/dashboard/billing/page.tsx
    - src/lib/emails/verification.ts
    - src/components/common/PageBreadCrumb.tsx
    - src/components/invoice/InvoiceHTML.tsx
    - src/lib/db/schema.ts
  </read_first>
  <action>
**File 1: `src/app/(portal)/dashboard/billing/[id]/page.tsx`** -- per D-17:
- Server component (no "use client")
- Auth check: `auth.api.getSession({ headers: await headers() })`, redirect if no session
- Role check: redirect admin roles to admin dashboard
- Get params.id from route params
- Query order with user join: `db.select().from(orders).where(and(eq(orders.id, params.id), eq(orders.userId, session.user.id))).leftJoin(user, eq(orders.userId, user.id))`
- If order not found or doesn't belong to user: show "Invoice not found" error message per UI-SPEC
- Also query license linked to this order: `db.select().from(licenses).where(eq(licenses.orderId, params.id)).limit(1)`
- PageBreadcrumb: "Home > Billing > Invoice {id.slice(0,8)}"
- Render InvoiceHTML component with order data
- Page has a "Download PDF" button at top right that links to `/api/invoices/${params.id}/pdf`

**File 2: `src/lib/emails/order-confirmation.ts`** -- per D-19:
- Follow the same pattern as `src/lib/emails/verification.ts`
- Import Resend from "resend"
- Export `sendOrderConfirmationEmail(params: { to: string; orderNumber: string; planName: string; amount: number; currency: string; paymentMethod: string; licenseKey?: string; status: string })`
- Create HTML email template:
  - Header: ConversionFlow logo/branding with #0047FF accent
  - Subject: "Order Confirmation - ConversionFlow"
  - Order details section: Order #, Plan, Amount (formatted), Payment Method, Status
  - If licenseKey is provided: show license key in a styled box with monospace font
  - "What's Next" section:
    - For completed: "Your license is ready! You can download the plugin from your dashboard."
    - For pending: "We're verifying your payment. You'll receive your license key within 24 hours."
  - CTA button: "Go to Dashboard" linking to `${APP_URL}/dashboard`
  - Footer: ConversionFlow branding, support email
- Call `resend.emails.send({ from: process.env.EMAIL_FROM, to: params.to, subject: "Order Confirmation - ConversionFlow", html })`

Also update the billing page (`src/app/(portal)/dashboard/billing/page.tsx`) to make order IDs clickable links to `/dashboard/billing/${order.id}`:
- Modify the InvoiceTable component or wrap order IDs in Link components
- Actually, modify the existing page to pass a `basePath` or add a link wrapper. Simplest approach: update the InvoiceTable to accept an optional `onRowClick` or wrap the order ID cell in a Next.js Link to `/dashboard/billing/${order.id}`.
- Add this change to the billing page by creating a custom order row renderer or by updating InvoiceTable to support linked order IDs.

To keep file ownership clean, update the billing page to pass a flag or render a modified version. The simplest approach that doesn't touch InvoiceTable: add a note in the billing page to wrap InvoiceTable rows. Actually, modify InvoiceTable.tsx directly to make order IDs into links:
- Add `import Link from "next/link"`
- In the order ID TableCell, wrap the span in `<Link href="/dashboard/billing/${order.id}">`
- This makes every order ID in the table clickable, navigating to the invoice detail page
  </action>
  <verify>
    <automated>cd "D:/Devsroom-Work/conversionflow-website" && grep -c "InvoiceHTML" src/app/\(portal\)/dashboard/billing/\[id\]/page.tsx && grep -c "Download PDF" src/app/\(portal\)/dashboard/billing/\[id\]/page.tsx && grep -c "sendOrderConfirmationEmail" src/lib/emails/order-confirmation.ts && grep -c "Order Confirmation" src/lib/emails/order-confirmation.ts && grep -c "Link" src/components/portal/InvoiceTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(portal)/dashboard/billing/[id]/page.tsx exists as server component (no "use client")
    - billing/[id]/page.tsx has auth check and user ownership verification
    - billing/[id]/page.tsx queries order by ID filtered by userId
    - billing/[id]/page.tsx renders InvoiceHTML component
    - billing/[id]/page.tsx shows "Download PDF" link/button
    - billing/[id]/page.tsx shows "Invoice not found" for missing orders
    - src/lib/emails/order-confirmation.ts exports sendOrderConfirmationEmail
    - order-confirmation.ts uses Resend with EMAIL_FROM and RESEND_API_KEY
    - order-confirmation.ts contains "Order Confirmation" in subject
    - order-confirmation.ts HTML includes order details (plan, amount, payment method)
    - order-confirmation.ts handles licenseKey presence (shows when available)
    - src/components/portal/InvoiceTable.tsx wraps order ID in Link to /dashboard/billing/${order.id}
  </acceptance_criteria>
  <done>Invoice detail page at /dashboard/billing/[id] showing full invoice with HTML view, PDF download button, and ownership check. Order confirmation email template sends HTML email with order details and optional license key. InvoiceTable order IDs are now clickable links to invoice detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer -> invoice detail page | Authenticated user accessing their own order |
| customer -> PDF download API | Authenticated user downloading their own invoice |
| email -> customer's inbox | Resend API delivers to customer email |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Information Disclosure | invoice detail page | mitigate | Query filters by session.user.id -- user can only see their own orders. IDOR protection established in Phase 3. |
| T-04-20 | Information Disclosure | PDF download API | mitigate | Auth check + userId verification. Admin can access any invoice (role check). Return 404 for unauthorized access. |
| T-04-21 | Information Disclosure | order confirmation email | accept | Email goes to the email address on the order. Standard email security -- no PII beyond what's in the order. |
</threat_model>

<verification>
- `grep "InvoiceHTML" src/app/(portal)/dashboard/billing/[id]/page.tsx` confirms component usage
- `grep "Download PDF" src/app/(portal)/dashboard/billing/[id]/page.tsx` confirms download button
- `grep "sendOrderConfirmationEmail" src/lib/emails/order-confirmation.ts` confirms email function
- `grep "Link" src/components/portal/InvoiceTable.tsx` confirms order IDs are linked
- `grep "generateInvoicePDF" src/app/api/invoices/[id]/pdf/route.ts` confirms PDF route
- `grep "application/pdf" src/app/api/invoices/[id]/pdf/route.ts` confirms content type
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Invoice detail page at /dashboard/billing/[id] with auth + ownership check
- HTML invoice shows company info, customer info, line items, VAT breakdown, total, payment details
- PDF download button generates and serves PDF via @react-pdf/renderer
- Order confirmation email template sends via Resend with order details and license key
- InvoiceTable order IDs are clickable links to invoice detail page
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-checkout-payments/04-05-SUMMARY.md`
</output>
