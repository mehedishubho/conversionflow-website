---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/auth/LogoutButton.tsx
  - src/app/(portal)/dashboard/page.tsx
  - src/app/(admin)/admin/dashboard/page.tsx
autonomous: true
requirements: [UAT-logout, UAT-role-default]
must_haves:
  truths:
    - "Clicking logout ends the session and redirects to /login"
    - "New users registered get role 'customer' not 'user'"
    - "Both customer and admin dashboards show a logout button"
  artifacts:
    - path: "src/components/auth/LogoutButton.tsx"
      provides: "Reusable client-side logout button component"
      exports: ["LogoutButton"]
    - path: "src/app/(portal)/dashboard/page.tsx"
      provides: "Customer dashboard with logout button"
    - path: "src/app/(admin)/admin/dashboard/page.tsx"
      provides: "Admin dashboard with logout button"
  key_links:
    - from: "src/components/auth/LogoutButton.tsx"
      to: "src/lib/auth-client.ts"
      via: "signOut import"
      pattern: "signOut"
    - from: "LogoutButton"
      to: "/login"
      via: "router.push after signOut"
      pattern: "router\\.push.*login"
---

<objective>
Fix 2 UAT issues: add logout functionality to dashboard pages and ensure customer role defaults to "customer".

Purpose: Users and admins currently have no way to sign out from their dashboards. Additionally, new registrations may get role "user" instead of "customer" if the database column default is stale.

Output: Reusable LogoutButton component embedded in both dashboards; verified role default in schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/auth-client.ts
@src/components/auth/SignInForm.tsx
@src/app/(portal)/dashboard/page.tsx
@src/app/(admin)/admin/dashboard/page.tsx
@src/lib/db/schema.ts
@src/lib/auth.ts

<interfaces>
<!-- Auth client exports the executor needs -->
From src/lib/auth-client.ts:
```typescript
export const authClient = createAuthClient({ baseURL: ... });
export const { signIn, signUp, signOut, useSession, requestPasswordReset, resetPassword, sendVerificationEmail } = authClient;
```

<!-- Role default in schema -->
From src/lib/db/schema.ts (line 75):
```typescript
role: text("role").default("customer"),
```

<!-- Role default in Better Auth config -->
From src/lib/auth.ts (lines 99-104):
```typescript
additionalFields: {
  role: {
    type: "string",
    defaultValue: "customer",
    input: false,
  },
```

<!-- Dashboard pages are server components -->
Both dashboard pages use `export default async function` with server-side session checks via `auth.api.getSession`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LogoutButton component</name>
  <files>src/components/auth/LogoutButton.tsx</files>
  <action>
Create a new client component at `src/components/auth/LogoutButton.tsx`.

Mark it `"use client"` at the top. Import `signOut` from `@/lib/auth-client` and `useRouter` from `next/navigation`.

The component should:
1. Have local state `loading` (boolean, starts false)
2. Define an async `handleLogout` function that:
   - Sets loading to true
   - Calls `await signOut()` (no arguments needed -- Better Auth handles session cleanup)
   - Calls `router.push("/login")` to redirect after sign-out
   - In the catch block, sets loading to false (signOut failure shouldn't leave the button stuck)
3. Render a `<button>` with:
   - `onClick={handleLogout}`
   - `disabled={loading}`
   - Visual styling matching the existing dashboard aesthetic: `"mt-6 px-4 py-2 text-sm font-medium rounded-lg bg-red/10 text-red border border-red/20 hover:bg-red/20 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"`
   - Text content: `{loading ? "Signing out..." : "Sign Out"}`

Export as named export: `export function LogoutButton()`.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/conversionflow-website && npx tsc --noEmit src/components/auth/LogoutButton.tsx 2>&1 | head -20</automated>
  </verify>
  <done>LogoutButton.tsx exists as a client component that calls signOut() and redirects to /login. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add LogoutButton to both dashboard pages and verify role default</name>
  <files>src/app/(portal)/dashboard/page.tsx, src/app/(admin)/admin/dashboard/page.tsx, src/lib/db/schema.ts</files>
  <action>
**Part A -- Add LogoutButton to Customer Dashboard** (`src/app/(portal)/dashboard/page.tsx`):

Import the LogoutButton at the top of the file:
```typescript
import { LogoutButton } from "@/components/auth/LogoutButton";
```

In the JSX return, inside the `<div className="text-center">` block, add the LogoutButton after the "Portal features coming in Phase 3" paragraph:
```tsx
<LogoutButton />
```

Since the page is a server component and LogoutButton is a client component, this works -- Next.js handles the boundary automatically.

**Part B -- Add LogoutButton to Admin Dashboard** (`src/app/(admin)/admin/dashboard/page.tsx`):

Same pattern as Part A. Import LogoutButton and add it after the "Admin features coming in Phase 5" paragraph.

**Part C -- Verify role default is "customer"** (`src/lib/db/schema.ts`):

Confirm line 75 reads: `role: text("role").default("customer"),` -- this is already correct in the source code. The UAT issue likely stems from a stale database column default from a previous migration. Also verify in `src/lib/auth.ts` that the Better Auth `additionalFields.role.defaultValue` is `"customer"` (already confirmed at line 101).

Since this project uses `drizzle-kit push` (no migration files found), the schema source IS the source of truth. Running `pnpm db:push` will sync the column default to "customer". No code change needed for this part -- just confirmation.

After changes, both dashboards should show: name/email, role, phase placeholder text, and a "Sign Out" button.
  </action>
  <verify>
    <automated>cd D:/Devsroom-Work/conversionflow-website && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Both dashboard pages render a LogoutButton. TypeScript compiles clean. Role default confirmed as "customer" in schema.ts and auth.ts.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. Visit /dashboard while logged in as customer -- "Sign Out" button visible and functional
3. Visit /admin/dashboard while logged in as admin -- "Sign Out" button visible and functional
4. Click "Sign Out" on either page -- session ends, redirect to /login occurs
5. Register a new user -- check database that role column is "customer" not "user"
</verification>

<success_criteria>
- LogoutButton component exists and is imported in both dashboard pages
- Clicking sign out clears the session and redirects to /login
- New user registrations default to role "customer" (verified in schema source)
- `pnpm build` succeeds
</success_criteria>

<output>
After completion, create `.planning/quick/260516-bw9-fix-uat-issues-add-logout-system-and-fix/260516-bw9-SUMMARY.md`
</output>
