/**
 * Shared Domain barrel export
 *
 * Re-exports all shared domain primitives (value objects).
 * Usage: import { LicenseKey, Money, Email, Domain } from '@/shared/domain';
 */

export { LicenseKey, Money, Email, Domain } from "./valueObjects";
